# 合并后的未入 PR 产物核对

在八个研究 PR 及汇总 PR #16 合入 main 后，按用户要求只读检查各研究线程、主工作副本和服务器研究源码。未向研究实例发消息、唤醒任务或启动新的研究作业。

## 补回的两项内容

1. **R1 二维阶段漏入库的复现源码**：原运行目录保有 `n2core.py`、`attack.py`、`audit.py`，而 PR 只带入了相应结果、符号检查和审计摘要。现原样补入 [phase3](../research/R1/param_opt/phase3/recovered_sources_20260909.md)。它们不是新的数学结果；只完成语法与三个固定点的轻量检查，不声称重现整批搜索。
2. **T3 复核状态只在 issue 中更新**：公开 [issue #3](https://github.com/randomcat4/dpp-entropy-tools/issues/3#issuecomment-5571508890) 已记有限种子新上下文 `CORRECT`，原 PR 文档仍称待审。现新增 [作用域补记](../research/T3/integration_status_20260909.md)，保持整体 CANDIDATE，列出 witness checker、acceptance cases 与独立公式路径的缺口。

## 各路核对结果

| 路线 | 已检查的差异 | 处理 |
| --- | --- | --- |
| T1 | 主副本与合入的 `475f82d` 相同、干净 | 未发现该主副本有新成果遗漏。 |
| T2 | 本地发布目录的 24 个文件与已合入内容一致；服务器 commit ID 不同但 `research/T2` 子树完全相同 | 属于提交运输差异，无新结果遗漏。 |
| T3 | 最终副本是 `905f66d`；旧副本只有缓存与参考运行输出。服务器 22 个研究 Markdown/Python 源文件内容都在 main 中有对应 | 补记 issue 状态，不把缓存或旧快照再当成果发布。 |
| R1 | 当前主副本 `a1c5999` 干净；旧发布副本和服务器仍有早期稿件、工具及重复状态 | 补回三个有复现用途的 phase3 源码；二维候选稿的结论已由最终二维证明覆盖。旧“未推送”状态被最终同步记录取代。 |
| R2 | 主副本 `686e2e8` 干净；服务器检查到的三份研究源码在 main 中已有内容对应 | 线程旧结尾“未推送”不再代表当前状态，最终 C2 及两份审查已入 main。 |
| R3 | 主副本 `b4804c0` 干净；服务器源码差异主要是早期总账、分组笔记与运行记录，非新一轮定理 | 最新交换三角形闭合已入 main；早期一般性措辞的作用域由路线台账限定。 |
| A1 | 主副本 `ace25e5` 干净；服务器的未跟踪条件检查脚本在 main 已有同内容文件，剩余六份不同 Markdown 是早期冻结状态 | 无需把早期 INCOMPLETE 总账覆盖最终已复核结论。 |
| A2 | 主副本与服务器 head 均为 `e42954f`；服务器所显示 ahead 相对其陈旧远端引用 | 已入 main，不是遗留五个新提交。 |

没有发现这些已检查主副本中另有一项完成却尚未入库的新定理。该结论不等于所有缓存、原始数值数据库、网页端未交付压缩包或未返回的会话内容都已穷尽。部分最近线程回合没有可读取正文，核对以实际工作树和公开固定对象补足。未用线程最后一句“未推送”代替 Git 内容比对。

此处的源码内容对照只回答“是否已入库”，不属于新一轮数学认证。原研究工作树、服务器文件和分支均未修改；没有把私有对话原文、连接信息或私库文件复制到公开库。
