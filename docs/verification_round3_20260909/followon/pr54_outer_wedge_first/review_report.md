# PR54 outer-wedge / observed-flow / finite-rate bridge first review

Reviewed PR/head: PR54 at `c8486bcdb18a85f93dd27930686cc1d4146804f5`.

Reviewed delta: `research/I05-23-20260909/ADDENDUM_OUTER_WEDGE_FLOW_RATE.md`, `code/verify_outer_wedge_and_flow.py`, and `output/verify_outer_wedge_and_flow.json` only. I used `RESULT.md` only for dependency and scope checks. I did not use another review report, private author code, LP solving, scouting, or C2 determinant/global-sign computation.

Verdict: `ACCEPTED_SCOPED` for the append-only unit. No precise critical gap was found. The addendum proves the displayed outer-wedge normal form, a sufficient sign interface for `W(s)`, the four-ring determinant-sign decomposition and stochastic-order sufficient condition, the visible-linear-operator obstruction in the strict correlated two-point/invertible-frame scope, and the finite Jensen-to-rate transfer criterion. It does not prove a universal sign for `W(s)`, a hidden-state dilation mechanism, a whole legal chord theorem for dense correlated rank-two blocks, or any open PR43-style mechanism.

## Dependency and scope check

The addendum states that it uses the already reviewed rank-two likelihood

`q_s = 1 - s a + s^2 b`, with `a = tr(G_A G_C)` and `b = det(G_A) det(G_C)`, plus zero means of the first and second exterior features (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:5-17`). Those dependencies are exactly the likelihood/normalization facts recorded in `RESULT.md:192-220`, in particular `sum mu q_s=1`, `E_mu u=0`, and `E_mu v=0` (`RESULT.md:200-208`). The addendum also says the PR43 nonreversible-flow and special `3+3` claims are not used as premises (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:17`), matching the original scope separation in `RESULT.md:7-12` and `RESULT.md:323-328`.

One status phrase is stale rather than mathematical: `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:17` describes PR43 as open, while root reports it is now merged. Since the same sentence excludes PR43 as a premise, this does not affect the proof.

The full-event boundary is preserved. The addendum explicitly places all expectations under the complete product law and says no complete event, rare event, three-point event, or Fisher term is removed (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:19`). The public checker also enumerates all four complete states in the two-point fixture and records positive event probabilities (`verify_outer_wedge_and_flow.py:50-76`; `verify_outer_wedge_and_flow.json:19-26`).

## Per-claim verdicts

| Claim | Source lines | Verdict | Review notes |
|---|---:|---|---|
| Exact outer-wedge curvature normal form | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:23-95` | `ACCEPTED_SCOPED` | With `q=1+u`, `y=s^2 b`, and `s=t^2`, the identities `q_t=(2/t)(u+y)` and `q_tt=(1/t^2)(2u+10y)` are correct. Differentiating full relative entropy gives the displayed Fisher-plus-acceleration formula after dropping `E_mu q_tt` by normalization. Expanding `4(u+y)^2/q+(2u+10y)log q` gives `Phi(u)+4y^2/q+y psi(u)`. |
| Pointwise nonnegative terms and sufficient `W(s)` sign | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:60-75` | `ACCEPTED_SCOPED_AS_SUFFICIENT_ONLY` | For strict legal events, `q=1+u>0`. Then `4u^2/(1+u)>=0`, `u log(1+u)>=0`, and `4y^2/(1+u)>=0`. Also `psi'(u)=8/(1+u)^2+10/(1+u)>0` on `u>-1`, and `psi(0)=0`, so `psi` has the sign of `u`. Therefore `W(s)>=0` implies `I''(t)>=0` and hence `H''(t)<=0` along the affine radial path. This is only a sufficient condition; no equivalence or universal sign law is asserted. |
| Eventwise mixed-discriminant sufficient condition | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:97-111` | `ACCEPTED_SCOPED_AS_SUFFICIENT_ONLY` | If `b u_s>=0` eventwise, then `b psi(u_s)>=0` eventwise because `psi` has the sign of `u_s`. The stronger condition `ab<=0` gives `b u_s=s[-ab+s b^2]>=0`. This is correctly presented as sufficient, not necessary. |
| Determinant-sign four-ring identity | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:115-160` | `ACCEPTED_SCOPED` | Zero means imply equal positive and negative masses for each nonzero feature, so the four tilted laws are probability measures. Expanding `alpha=alpha_+-alpha_-` and `beta=beta_+-beta_-` gives the stated four-term contrast for every test function `f`. |
| Stochastic-order interface for `W(s)>=0` | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:162-176` | `ACCEPTED_SCOPED_AS_SUFFICIENT_ONLY` | First-order stochastic dominance of the `++` law over `+-`, and of `--` over `-+`, implies the two expected `psi(u_s)` differences are nonnegative because `psi` is increasing. On a finite configuration space this is exactly a finite CDF-inequality interface. The addendum does not claim this condition is universal or equivalent to `W(s)>=0`. |
| Any-visible-linear-operator collision | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:180-213` | `ACCEPTED_SCOPED` | If `d` is a nonzero linear combination of `G11,G12,G22`, linearity forces `T d = theta d` whenever `T Gij = theta Gij`. This contradicts `T d = theta^2 d` for `0<theta<1`. The generator version with eigenvalues `-1` and `-2` is the same one-line contradiction. No positivity, reversibility, or Markov property is used. |
| Strict correlated two-point / invertible-frame obstruction | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:214-259` | `ACCEPTED_SCOPED` | For a strict two-point kernel with off-diagonal `c != 0` and `V in GL_2`, `G=V^T Y^{-1}V` gives `Y=V G^{-1}V^T=V adj(G)V^T/det(G)`. The off-diagonal entry of `Y_T=C-E_{T^c}` is always `c`, hence `d_T=(1/c)e_1^T V adj(G_T)V^T e_2`. Expanding the `2 x 2` adjugate yields the displayed linear combination of `G11,G12,G22`. The conclusion is limited to this visible-state linear semigroup mechanism and does not rule out hidden dilations or entropy curvature by other routes. |
| Exact Farkas one-line fixture | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:261-284` | `ACCEPTED_SCOPED` | For the fixture `C=[[1/2,1/10],[1/10,1/2]]`, the public output lists all four complete states with positive probabilities and `d+10G12=0` (`verify_outer_wedge_and_flow.json:19-26`). In the directed-flow equations from `RESULT.md:682-727`, the stated multipliers make every flow-variable coefficient zero, while the right-hand side is `1-2=-1<0`. This is a valid rational Farkas infeasibility certificate for the visible-flow LP; no LP solve is needed. |
| Subextensive bad-`W` finite Jensen bridge | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:288-350` | `ACCEPTED_SCOPED` | From the outer-wedge form, the nonnegative terms give `t^2 I_n''(t) >= t^4 W_n(t^2) >= -t^4 rho_n`, hence `H_n''(t)<=t^2 rho_n<=T^2 rho_n`. Therefore `H_n(t)-(T^2 rho_n/2)t^2` is concave and finite Jensen gives exactly the displayed quadratic defect. Dividing by `N_n` and passing to pointwise limits at the fixed parameters proves rate concavity when `rho_n/N_n -> 0`. No differentiation of the volume limit is used. |
| Final route/status table | `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:354-370` | `ACCEPTED_SCOPED_AS_STATUS` | The status table preserves the split between proved identities/obstructions/finite-rate criterion and incomplete universal mechanisms. The final route preference is methodological, not a theorem. |

## Public checker and output audit

The checker is theorem-facing only for exact symbolic/rational identities (`verify_outer_wedge_and_flow.py:1-5`, `verify_outer_wedge_and_flow.py:25-36`, `verify_outer_wedge_and_flow.py:37-48`, `verify_outer_wedge_and_flow.py:50-76`). It verifies the algebraic expansion of the curvature normal form, the derivative of `psi`, the `2 x 2` adjugate linearity formula, the strict correlated two-point fixture relation `det(G)=-10G12`, and the Farkas right-hand side `-1`. It does not attempt to prove universal `W(s)>=0`, hidden-state dilation, whole-chord concavity, or a global determinant/sign certificate. The JSON output matches that scope and records a rational `PASS` (`verify_outer_wedge_and_flow.json:1-13`, `verify_outer_wedge_and_flow.json:19-31`).

I did not rerun this checker. Its displayed output is consistent with the proof text and my hand checks, but the accepted mathematical content above rests on the public formulas and line-referenced derivations, not on trusting a private or unreviewed computational environment.

## Boundary and attack checks

- Legal domain: The curvature normal form is asserted for strict legal `t != 0` (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:49`). The division by `t` is harmless away from zero, and the addendum handles `t=0` by even quartic onset and continuity (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:95`). No boundary case with `q=0` is claimed.
- Full events: The proofs keep the full product law and complete Fisher term (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:19`, `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:79-84`, `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:328-348`). The finite two-point obstruction checks all four states.
- Rank-two scope: The outer-wedge formula depends on the accepted rank-two likelihood and exterior-feature zero means only; it is not a general arbitrary-rank statement.
- Correlated two-point scope: The visible-linear-operator obstruction requires `c != 0` and `V` invertible (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:214-227`). It should not be conflated with accepted fixed three-point nonreversible-flow material or with a hidden-state construction.
- Open mechanisms: The addendum keeps universal dense-correlated whole-chord concavity, universal `W(s)>=0`, and hidden-state dilation incomplete (`ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:5`, `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:259`, `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:284`, `ADDENDUM_OUTER_WEDGE_FLOW_RATE.md:362-364`).

No blocker was found for the scoped append-only unit. Merge eligibility, from this bounded first-review perspective, is `ACCEPTED_SCOPED`: safe to integrate as proof of the stated normal form, sufficient interfaces, visible-flow obstruction, and finite-rate bridge, with the incomplete mechanisms and stale PR43 status note preserved.
