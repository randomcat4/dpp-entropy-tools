# PR24: independent scoped integration decision

Status: **ACCEPTED_SCOPED** for the certificates and auxiliary results specified below. The global real three-dimensional entropy-concavity claim is **INCOMPLETE**. The fixed weak-edge expansion remains an author candidate beyond the explicitly audited identities and probes.

Frozen PR head: `e988aa3003484f6368133b8bc0c668331629e369`. Merged by merge commit `494949f473b2eaa9a1176a71525fab628ef9d641`, retaining the N3 ancestry for PR30 and PR33. No CI checks were configured; the absence of checks is not recorded as a CI pass.

Reviewer: C3 integration main, not an author of N3. The pre-existing N3 nonauthor reports are used only in their actual scope; the two historical review units came from one reviewer context.

## Evidence and fresh execution

- Read the [final second-round audit](../../research/N3/round2/review/round2_final_review_v2.md), [root proof](../../research/N3/round2/falsification/beta_zero_existence.md), [locked-projection obstruction](../../research/N3/round2/falsification/locked_obstruction.md), and [degeneracy correction](../../research/N3/round2/inequality/locked_odds_degeneracy_addendum.md). Historical pending labels are superseded only within the linked final audit's scope.
- Re-executed `verify_beta_root_certificate.py` and `verify_locked_obstruction.py` from a fresh review-owned Linux checkout, with one CPU thread per process, no GPU, and fixed inputs. Both returned PASS and their nested certificate runs exited 0. This is a new execution of existing independent checking code, not a third independently written interval implementation.
- The root check rebuilt all eight event masses and six-coordinate Jacobians on five rational samples, including 120 off-diagonal factor-two checks; checked five scaled-H/log samples and the complete root bracket. The result is `0.661527925881764341226497437142 < d alpha < 0.661527926096708267576247678209` throughout the bracket. Opposite rigorous endpoint signs imply at least one root; uniqueness is not asserted.
- The locked check used one exact kernel and direction, three projection forms, six conditional slices, two feasible chord endpoints, and four log terms. It confirmed `max Qlock-C < -0.482450470481640973` and `F-C > 53.17428270279292381`. Thus the proposed projection dominance and convex combinations of those same three forms are **REFUTED**, while the actual entropy curvature remains negative.
- The degeneracy addendum follows from the weighted Gram identity `R=||psi-(m/V)g||^2`. At zero residual the actual locked score forces equal projected odds derivatives; the additional lower-bound term is defined as zero. No arbitrary-path limit is assumed.

The first-round auxiliary identities and exact obstructions retain the narrower decisions in the [existing verification index](../../research/N3/verifications/index.md). This batch did not rerun all historical searches or independently formalize their asymptotic remainders.

## Remaining obligation

The full event Fisher/cofactor comparison at arbitrary beta-zero kernels is still open. The audited root has a large safety margin below one and is not a counterexample. The weak-edge audit is algebra and fixed-parameter testing; it does not certify uniform remainder control in joint boundary degenerations. Novelty and Lean formalization are not certified by this integration.
