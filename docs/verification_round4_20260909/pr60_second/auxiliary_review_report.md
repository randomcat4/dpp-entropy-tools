# PR60 U4 auxiliary SECOND review report

Overall verdict for U4: **CORRECT**.

This is a separate bounded review of the fixed-diagonal radial obstruction in `continuation.md` section 3. It does not change the earlier SECOND reports. U1-U3 and U5 are outside this addendum.

## Per-obligation verdicts

| Obligation | Verdict | Evidence and review notes |
|---|---:|---|
| Frozen target and literal objects | CORRECT | The author target is `input/continuation.md` lines 72-158. The matrices and radial definition are stated at lines 76-90; the base event law and `exp(Lambda)` at lines 97-104; the radial derivative claim at lines 106-118; all-event jet derivation at lines 124-140; actual curvature/Jensen at lines 142-156. The auxiliary request binds the same literals at `aux_input/inputs/REQUEST.md` lines 3-20, and the raw matrix packet repeats `C*`, `D*`, `K*`, `h` at `aux_input/outputs/run01/02_matrices.json` lines 6-57. |
| Source binding and independence | CORRECT | The author-only input binding fixes PR60 head `f869fd251c0d6fdad737b6d5efa287307795a87d` and the relevant blobs at `input_binding.json` lines 2-29. The auxiliary binding fixes PR71 head, executable head, author head, and states no FIRST/C1/C3 material at `aux_input_binding.json` lines 2-4 and 163-165. The auxiliary source binding fixes the same author commit and says the author checker is not imported/executed at `aux_input/inputs/SOURCE_BINDING.json` lines 2-22. The implementation independently uses literal rational matrices and says it does not import/execute the author reference checker at `aux_input/implementation/pr60_aux52_certificate.py` lines 2-7 and 571-584; the input checkpoint records the same at `aux_input/outputs/run01/01_inputs_resolved.json` lines 5-15. |
| Complete event/Mobius identity | CORRECT | The implementation contains both exact-event formulas: signed determinant with absent-coordinate diagonal shift at `pr60_aux52_certificate.py` lines 256-267 and inclusion-exclusion/Mobius at lines 270-279. It also has direct rational event-probability versions at lines 282-299. `build_event_polynomials` checks signed and inclusion-exclusion polynomials agree for all eight masks and that the total event polynomial is 1 at lines 587-604. The base-event checker also compares polynomial and direct determinant laws at lines 898-914. |
| All eight complete events and six mixed jets | CORRECT | The jet extractor records exactly `p`, `pD`, `pDD`, `pC`, `pDC`, and `pDDC`, with the second-derivative factors included, at `pr60_aux52_certificate.py` lines 317-325. The raw event packet has eight masks with six jet entries at `03_event_polynomials_and_jets.json` lines 7-16, 85-94, 163-172, 241-250, 319-328, 397-406, 475-484, and 553-562; the checkpoint status is line 631. Each event also has an eight-term signed determinant polynomial in the same per-mask blocks. |
| Legality of radial endpoints and three Jensen kernels with complements | CORRECT | The code checks all principal minors and leading Sylvester minors at `pr60_aux52_certificate.py` lines 348-378. It derives the radial squared bound from the two Schur complements and checks `999/1000` and `1001/1000` strictly inside it at lines 620-663. The raw legality packet records strict positivity of `K*`, `I-K*`, the bound `442775/434223`, endpoint containment, and endpoint kernel/complement positivity at `05_legality.json` lines 36-80, 111-146, 181-216, and 222-226. The Jensen checker builds `K*±hD*` and checks all six kernel/complement packets at code lines 829-861 and raw output `09_jensen.json` lines 1318-1518. |
| `exp(Lambda)` and complete base event law | CORRECT | The author display is at `input/continuation.md` lines 97-104. The implementation fixes the expected vector, compares it to polynomial and direct determinant laws, computes the Lambda ratio, and checks the displayed fraction and `>1` at `pr60_aux52_certificate.py` lines 898-931. The raw output records the mask order, probabilities, sum `1`, displayed match, fraction `65729622186464/3466472577089`, and greater-than-one field at `04_base_probability_and_lambda.json` lines 6-33 and 69-71. |
| Rational log/atanh/log2 method, negative-exponent reversal, outward decimals | CORRECT | The author states the rational atanh/log2 method and endpoint reversal at `input/continuation.md` lines 135-140. The implementation uses `Fraction`, not floating arithmetic, at `pr60_aux52_certificate.py` lines 20 and 29; the log engine range-reduces to `2^k y`, bounds the atanh tail, caches `log2`, and reverses endpoints for negative `k` at lines 58-140. Decimal rounding is outward at lines 397-433, and JSON float atoms are rejected at lines 456-468 and 514-524. The raw log-method packet records 80 terms, the rational tail formula, range reduction, and the `log2` interval at `06_log_method.json` lines 6-30. |
| Full radial derivative of complete negative entropy Hessian | CORRECT | The author formula and interpretation are at `input/continuation.md` lines 106-133. The implementation evaluates every event contribution with `2 pD pDC/p - pD^2 pC/p^2 + pDDC log(p) + pDD pC/p`, requires positive base atoms, verifies the rational term `R`, verifies the log-cube coefficient pattern, compares the final interval to display (33), and requires strict negativity at `pr60_aux52_certificate.py` lines 677-754. The raw packet records eight event contributions, the exact formula, `expected_R_matched=true`, the log coefficient identity, interval containment, and strict negativity at `07_radial_derivative.json` lines 6-9, 480-516. |
| Actual complete negative entropy curvature | CORRECT | The author claims the favorable concave sign at `input/continuation.md` lines 142-148. The implementation uses the full Fisher/log formula `sum[pD^2/p + pDD log(p)]` at `pr60_aux52_certificate.py` lines 757-805. The raw packet records eight event contributions, Fisher terms and log coefficients, the formula, display containment, and strict positivity at `08_true_curvature.json` lines 6-10, 432-467. |
| Actual complete three-kernel Jensen gap | CORRECT | The author states `tau=1/100000`, legality of the three kernels and complements, and the negative complete entropy Jensen gap at `input/continuation.md` lines 150-156. The implementation computes the complete event probabilities for `K*`, `K*+hD*`, `K*-hD*`, verifies polynomial/direct agreement for endpoints, computes entropy intervals from all eight events, compares to display (36), checks all six kernel/complement legality packets, and requires strict negativity at `pr60_aux52_certificate.py` lines 808-895. The raw output records all three probability vectors at `09_jensen.json` lines 1263-1296, the negative gap interval and display containment at lines 6-9 and 1296-1300, and strict kernel/complement positivity at lines 1318-1518. |
| Execution chronology and bounded computation | CORRECT | The run guard enforces a lock, one deadline window, thread-limit environment, CPU selection, memory cap, and final exit at `aux_input/execution/run_guard.sh` lines 1-54. The ledger records one run, zero failed runs, zero repairs, Python 3.12.3 standard-library `Fraction`, no SymPy/package changes, start `2026-09-09T15:59:05Z`, deadline `2026-09-09T16:09:05Z`, finish `2026-09-09T15:59:06Z`, exit code 0, and no live arithmetic at `RUN_LEDGER.json` lines 2-42. The run files agree: `start_utc.txt` line 1, `deadline_utc.txt` line 1, `exit.json` line 1, `stdout.log` line 1, and empty `stderr.log`. |
| Scope interpretation | CORRECT | The author explicitly says the fixed-diagonal radial sufficient condition is false without contradicting the Lambda-zero theorem at `input/continuation.md` line 120, and says the result is neither an entropy counterexample nor a positive-Jensen candidate at lines 150-156. The final auxiliary certificate repeats the restricted scope and `not_claimed` list at `certificate.json` lines 2546 and 3074-3076. |

## Mathematical proof review

The exact-event formulas are the standard Mobius inversion formulas for a marginal-kernel DPP and their signed determinant equivalent. The auxiliary implementation checks those identities as polynomials in the affine plane `K* + epsilon D* + delta C*`, then separately checks direct determinant event laws for the base and Jensen endpoints. That closes the event-law side of the certificate, subject to the supplied machine arithmetic record.

The derivative formula is also the correct derivative of the complete negative entropy Hessian. For an event atom `p(epsilon,delta)`, the negative Hessian in the `D*` direction is `pD^2/p + pDD log p`; differentiating in `delta` gives exactly `2 pD pDC/p - pD^2 pC/p^2 + pDDC log p + pDD pC/p`. The inspected implementation retains all four terms and all eight events. I found no deleted-event, wrong-sign, or leaf-only substitution.

The log enclosure method is mathematically adequate for the finite certificate: every positive rational log argument is reduced to `[1,2)`, `log2` is bounded by the same atanh tail at `y=2`, negative powers of two reverse the scaled interval endpoints, and final decimal displays are outward-rounded. The raw intervals are compared to the author’s displayed intervals before sign conclusions are recorded.

## Static code inspection

I inspected the independent auxiliary implementation, not just the pass labels. The source imports standard-library `Fraction`, has no SymPy import, builds the literal matrices internally, reads the author text only to verify frozen target snippets, and records `continuation_exact_reference.py` as forbidden/not imported/executed. It writes checkpoints after rejecting Python float atoms. The final certificate says `sympy: not used`, but that is consistent with the actual imports and implementation structure.

The implementation/output pairing is coherent: the code writes checkpoints 00-09 and a final certificate at `pr60_aux52_certificate.py` lines 935-1024, and the run directory contains the corresponding JSON packets bound by `aux_input_binding.json` lines 49-160. I also checked every listed SHA-256 in the 26-file auxiliary packet against the local files; all matched, with no missing or extra files.

## Independent execution, formal coverage, and novelty coverage

- Independent execution by me: **none**. I did not run the author checker, the auxiliary checker, arithmetic scripts, SymPy, entropy jobs, remote jobs, or formal tools.
- Formal proof coverage: **none**. This is a static mathematical/code/raw-certificate review, not a formal verification.
- Novelty coverage: **none**. This addendum makes no novelty assessment.

## Final U4 status

U4 is **CORRECT** within the accepted scope. The fixed-diagonal radial derivative is a certified negative obstruction to that auxiliary monotonicity method, while the actual complete entropy curvature and the exact three-kernel Jensen check have the concave sign. I do not classify this as an entropy counterexample or as evidence against the Lambda-zero `M` theorem.
