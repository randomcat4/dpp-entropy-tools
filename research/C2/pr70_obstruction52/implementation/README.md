# Independent implementation notes

Prepared by the C2 root after the two intended preparation workers remained pending initialization without files. No author executable or output is imported. This is implementation documentation, not a mathematical review.

The inclusion construction expands each principal minor by permutations and then uses subset Mobius inversion. The complete-event construction subtracts 1 from each absent diagonal, expands by recursive Laplace minors and applies the absent-count sign. Both produce exact polynomials in the true parameter of K+tD; jets are their first three coefficients with the factor 2 for the second derivative. A separately built two-site law verifies the four marginal polynomials.

Reciprocal/product jet rules and the displayed quotient identity independently produce Phi0'', Phi1'' and their sum. Side entropies are differentiated both as r*log(r)-r*log(P) and as P*q*log(q), q=r/P, retaining r'' and P''. Rational constants plus formal rational log coefficients certify exact structural equality before any log approximation. Full, marginal, conditional, Fisher and acceleration terms remain visible.

Every natural logarithm uses fixed N=80 rational atanh terms after argument=2^k*y normalization, with 1<=y<2. The nonnegative remainder is bounded by 2*w^(2*N+1)/((2*N+1)*(1-w^2)); log2 uses w=1/3. Negative coefficients reverse interval endpoints. Exact rational intervals and integer floor/ceiling 40-place decimal displays are emitted. No float enters a mathematical bound. Width, sign, and literal containment are distinct ordered gates; any failure terminates the run.

The code was written and manually inspected without execution before its public preparation freeze. All eventual validation runs, imports and any eligible mechanical repair belong to the external guard's single original 600-second deadline. Incremental JSON checkpoints, JSONL comparisons, each log series certificate and a terminal status preserve evidence through failures.
