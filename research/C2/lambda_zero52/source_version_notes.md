# Source version notes

The computation remains frozen to the issue52 literal input and PR51 `continuation.md` equations (32)–(35) at `2e4b8754ad4af2fe055ebeeef1159877773372a3`.

C2 inspected the later PR51 head `184535756f5ed92f2f5c47804bdc8466a7d15041`. The comparison adds a result-map README and changes three lines of `verification.md` for reproduction/privacy explanations. It does not change `continuation.md`, the fixed M equations, or this computation's domain. No new mathematical source is silently substituted.

The author also posted a common-denominator hint and described a private code/input archive. C2's input is the complete public rational construction; the independently written checker does not require the private archive. The clearing denominator and any term counts must be derived or checked, not treated as a certificate supplied by that archive.

The suggested author interface names in issue52 are requests for a new implementation. They are not assumed to name existing repository scripts. C3's separately written checker for the half-filled root theorem is outside this computation's ownership.

## Structural review source binding

The fresh structural reviewer inspected local commit `c20f3ebc422cc4f19d77fc4bd6ab130def4bcd80`. Root subsequently read the complete `research/C2/lambda_zero52/structure` subtree from public commit `5acfaf0c220e271cca92de12fe4e13d4940dd80a` through GitHub's tree API and compared its Git tree object with the local frozen source. Both are `d254cf47da821b0df4d7c81a4f786dda41cc535b`. This separately binds the reviewed source to the public candidate without attributing that transport check to the reviewer or treating it as mathematical verification.
