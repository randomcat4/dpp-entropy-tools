# Source version and coordination notes

## PR43 document successor

This packet's computations remain frozen at `4e1369ef2a59ccfaba3ca8fce95d85e78857bf78`. The later head `7bd5962bbb2020ce47fbe286adda7dfe02f9645d` modifies twelve documentation files under `continuation/`. C2 inspected the GitHub commit comparison and the revised task file: the underlying `proof/` theorem files, verifier code, exact JSON inputs and saved computation outputs are unchanged. Changes to statement summaries and review entry points are not automatically accepted as mathematics by this observation.

The original directed-flow task D becomes task G in the revised document. Its displayed C,V and density-adjoint eigenfeature equations agree with the frozen computational object. Renamed headings do not start new computations. The original detailed equations and explicit state-bit convention remain part of the frozen input contract.

## Avoiding duplicate work

C1 confirmed in issue 45 that its root PR43 unit owns the indefinite-rank-two / correlated 3+3 fixture and its 64-event checks. C2 therefore runs only the two nested continuation verifiers and the originally claimed B/C fixture reconstruction; no root-verifier replay is counted in this packet.

## Stale generated payload

C3's interface audit at the frozen head found reversible conductance language in `continuation/HANDOFF.md`, `continuation/inputs/rational_examples.json`, and the payload writer in `continuation/code/verify_continuation.py`. Replaying that writer is evidence only about the original program. Its payload is not the directed LP input. C2 constructs the 56 ordered-flow system independently from the literal task equations and labels its input nonreversible.

The author helper's rightmost label bit is matrix coordinate 1. That map is recorded in the C2 contract and certificate; a state permutation must be applied consistently to every event, feature, edge and equation.

## Unsettled dependent contract

The revised task still inherits the previous closed-interval, positive-event-bound and 1e-20 width requirements without resolving the questions in issue 45. It supplies no new finite counterexample search box. These dependent jobs remain unclaimed pending their exact contract and prerequisite outcome.
