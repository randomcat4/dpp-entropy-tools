# Provenance and independence boundary

The theorem/formula/P/chart proposal belongs to the frozen PR60 author
packet. C2 root froze the exact source, extracted six literal RHS strings as
text only, owns the resource guard and publication, and coordinates machine
evidence. A fresh independent implementation worker constructs the chain
from displayed formulas; it does not import or execute author scripts and
has no arithmetic-launch authority. Root executes the frozen new code once
ready and returns exact outputs for inspection.

Source object bindings identify the reviewed text. Mathematical evidence is
provided by complete symbolic identities and coefficient arrays, not by a
checksum or a reproduced success message. The P comparison strings are
opened only downstream of independently extracted P.

C1 separately reviews mathematical bridging; C2's machine result is not that
FIRST verdict. C3 controls integration and any later independent gate. The
old r=0 FIRST records remain unchanged. Novelty/formal verification are not
part of this specific machine-evidence contract.