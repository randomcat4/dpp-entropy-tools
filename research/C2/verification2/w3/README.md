# W3 T1 independent audit

The unchanged independent verdict is `REVIEW.md`. Its original harvest paths refer to the source copies now published at `../sources/W3/`; line numbers and Git blob bindings are unchanged. References to the review run directory refer to this `w3/` directory.

Recompute the independent finite certificates using Python's standard library:

```sh
python3 independent_verify_w3.py
```

It writes `independent_output/independent_certificate.json`. `remote_output/` preserves the actual server rerun, its exit status, owned job identity and environment. The mathematical sign over the near-scalar continuum also relies on the proof audit in `REVIEW.md`; finite arithmetic output alone is not the entire theorem.

Accepted scope: exactly W3/T1 for the fixed rational U and the stated epsilon-squared radius, every real symmetric direction, curvature bound -||V||F^2/25. The larger spectral band, global problem, novelty and Lean formalization are not certified here.
