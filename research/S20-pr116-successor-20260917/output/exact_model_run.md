# Fresh exact-model execution record

Date: 2026-09-17

Command executed in the current isolated ChatGPT runtime:

```sh
python exact_model.py --output report.json
```

Exit code: `0`

`stderr` was empty.

The literal stdout is retained in `exact_model.stdout.txt`.

Scope: finite exact checks only; this run is **not** a continuum certificate and is not independent mathematical review.
