# PR86 source/evidence repair without rewriting the frozen review object

Frozen source: PR86 head `bd12e6094e098499fae7e01729a4b29f021a14e2`, base `65e59a46b49cd2dbb5c779a4cfae8cef26441984`. Latest FIRST finding: analytic units accepted within scope; finite arithmetic remains source-only/pending independent server review. The reviewer requested a separately frozen successor because the source packet promised `full_square_certificate.json` but omitted it, and because its README stopped before later theorem files.

This successor leaves every reviewed PR86 blob unchanged. It supplies the missing output and the following complete navigation map.

## Theorem and evidence map for all frozen PR86 files

| Frozen PR86 file | Mathematical object | Evidence dependency and present gate |
|---|---|---|
| `README.md` | original top-level ledger | superseded for navigation by this map; original file remains frozen |
| `rank2_atoms_and_angular_box.md` | physical-coordinate rank-two atoms; sign comparison; small angle box; method witnesses; bit-flip lift | analytic atoms/sign comparison accepted at FIRST; numerical box, witness, curvature and lift values remain pending independent arithmetic |
| `certify.py` | fixed canonical atom/jet and logarithm checker | author source only |
| `certificate.json` | all sixteen atoms/jets and finite intervals for two witnesses | author output only |
| `certificate_stdout.txt` | annotated execution record | author output only |
| `dilute_chord_theorem.md` | qualitative fixed-pair small-intensity theorem, including equal diagonals and all-event remainder | qualitative theorem accepted at FIRST; embedded numerical fixture intervals remain pending arithmetic |
| `dilute.py` | dense equal-diagonal fixtures and explicit remainder constants | author source only |
| `dilute_certificate.json` | exact q-polynomials and finite fixture bounds | author output only |
| `dilute_stdout.txt` | author execution record | author output only |
| `fresh_recheck_and_edge_extension.md` | two exact full edges of the fixed canonical family | proof strategy read; finite derivative/Bernstein signs pending independent arithmetic |
| `extend_edges.py` | edge interval generator | author source only |
| `edge_extension_certificate.json` | edge certificate | author output only |
| `equal_angle_diagonal.md` | equal-angle diagonal of the fixed square | finite 256-box derivative certificate pending independent arithmetic |
| `extend_diagonal.py` | diagonal interval generator | author source only |
| `diagonal_extension_certificate.json` | diagonal certificate | author output only |
| `thinning_star_and_orthogonal_support.md` | thinning-star entropy theorem and disjoint actual-coordinate support theorem | accepted analytically at FIRST; no finite certificate required |
| `full_canonical_square.md` | entire fixed canonical angle square, all sixteen events and rare-face regularization | finite 4096-box conclusion pending independent arithmetic |
| `certify_full_square.py` | exact 64-by-64 rational cover generator | frozen source copied here for direct reproduction |

## Supplied missing files

The following files in this successor bind directly to the immutable formulas and script above:

- `certify_full_square.py`, copied from the frozen PR86 source;
- `full_square_certificate.json`, freshly generated by executing that script without `-O`;
- `full_square_stdout.txt`, the direct stdout from the same run.

The output reports:

```text
PASS full_square_certificate.json
4096 boxes
minimum lower = 0.012067200390564330
minimum box = (63,63)
q range = [576/390625, 558671015947/1638400000000]
```

It therefore supplies the archive object promised by `full_canonical_square.md`. It does **not** upgrade the fixed-square theorem to independently accepted finite evidence. A Sol/server reviewer must regenerate the complete atom factors, rational boxes and logarithm enclosures independently or under an explicitly claimed reproduction task.

## Reproduction and exact-error semantics

Run from this successor directory:

```sh
python certify_full_square.py
```

The input matrices are embedded exactly:

\[
A=\begin{pmatrix}2/5&6/25\\6/25&2/5\end{pmatrix},\qquad
B=\begin{pmatrix}3/5&9/25\\9/25&3/5\end{pmatrix}.
\]

The physical parameters are `c1,c2 in [0,1]` with `s_j=sqrt(1-c_j^2)` and the only midpoint is `(EAE^T+VBV^T)/2`. The checker reconstructs all sixteen signed event determinants, removes the two rare-coordinate factors algebraically, and covers the entire square by 4096 closed rational boxes. It uses a 32-term rational `atanh` logarithm expansion with the written geometric tail. Negative polynomial lower enclosures are clipped only to the exact legal lower bound zero; no positive probability floor is introduced.

Author environment for this repair run: Python 3.11.8 and SymPy 1.13.1 in the present container. The frozen source previously recorded a different author environment; equality of environments is not required by the proof, but the independent gate should record its own versions. No checksum procedure is requested or used.
