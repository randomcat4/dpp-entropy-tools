# Round-two units and premise versions

v1 freezes the four-point rank-three face with noncoordinate nullspace.
P1 ran 12 centers/36 chords using direct proper events and exact support checks.
F1 independently explores 72 centers on six rational frames, using stable
precision and arbitrary face directions. Support/lift and certificate work
run independently of those constructors.

Main froze the exact 12-symbol coarsening and a maximum-cardinality geometric
entropy bound. The latter has coefficient (2/3)H(q), below one for n4r3.
An explicit real rational n5r3 frame has preliminary H(q)>1.5, supplying the
support-mechanism reason required by the user for v2's bounded n5 probe.
v2 adds this exploratory scope; it does not weaken or overwrite v1 or the
original strict real-kernel target. Independent rational confirmation completed
before F2: H(q) lies between 1.900161876460949643 and 1.900161876460949644.

F1 completed 72 centers/216 chords and F2 completed 24 centers/144 chords,
without failures or positive candidates. F2 realizes the proposed geometric
budget failure in 10 noncommuting directions, but lower events compensate.
These distinct units leave the same coupled sign obligation unresolved;
the route is STOPPED_SUBSTANTIVE with no further batch scheduled.

Support/lift v2, coarse identities and the top Fisher bound received one
non-author reviewer's scoped acceptance. A review supplement corrects decimal
interval presentation and explicitly distinguishes the general identity proof
review from a finite polynomial check. The original proofs/reviews remain.
