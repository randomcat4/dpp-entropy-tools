# R1 provenance

- The R1 route owner froze the statement, integrated the artifacts, and owns
  the public branch and Draft PR.
- The `param_opt` sublane authored the unrestricted spectral parameterization,
  bounded optimizer, denominators, and the independent Hessian-proof review.
- The `prob_deriv` sublane authored the independent event/derivative reference
  implementation and the diagonal-center finite-chord candidate proof.
- The `certificate` sublane authored the corrected rational chord verifier,
  its three status tests, the diagonal-center Hessian candidate proof, and the
  independent finite-chord-proof review.
- Proof authors do not certify their own candidate. Final status is tied to a
  frozen Git commit and separate review files.
- Prior complex-Hermitian work was used only as a warning and arithmetic
  template. It is not R1 evidence because its successful direction is pure
  imaginary, not real symmetric.

No private connection strings, private paths, credentials, process IDs, or
conversation history belong in this public repository.
