# Freeze transport record

The reviewers read the two theorem/proof pairs from local Git commit
`a49051d2f768ec2b926a2e4d68d5286b054f9656`. Its tree is
`c282e54a8221dc8546b5c8da3c23a768038b9025`.

The ordinary smart-HTTPS push endpoint was unavailable at publication time.
Every changed blob was therefore uploaded through the GitHub Git data API and
checked against its local Git blob ID before the tree was created. The API tree
ID was exactly
`c282e54a8221dc8546b5c8da3c23a768038b9025`. GitHub normalized commit metadata,
so the public transport commit ID is
`8ae16152670e768acc4bd5792b5db71822c42c86`, while the complete tree and all
reviewed mathematical blobs are byte-identical.

Reviewed blobs:

| Object | Git blob ID |
| --- | --- |
| D1 frozen statement | `5deb1c8f8d11628904f6da37d9d1e259644f9a88` |
| D1 candidate proof | `ed7607aca2aa4384a4d8a467b038c0ece388889d` |
| D2 frozen statement | `4421e7665c7a897a13d4ad89e9fc2caf517b074f` |
| D2 candidate proof | `77a43df36487120e2ca4c125d9faff448a115d99` |

This record concerns transport identity only. Mathematical correctness is
addressed by the two commit-bound review reports.
