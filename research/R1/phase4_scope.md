# R1 第四阶段：三维最小障碍

- 启动日期：2026-09-08。
- 墙钟：沿用用户已授权的 9 小时总窗口，不重新计时或扩容。
- 状态：`FINITE_COMPLETE / EXACT_REDUCTION / ANALYTIC_OPEN`。
- 前提：P3-01 与 P3-02 只有在 commit-bound 非作者审查通过后才作为可用引理；审查前不得循环引用。
- 完整事件语义不变：包含概率必须先经 Boolean Möbius 反演。

## P4-01：三维全域反例/证明

- 冻结命题：`frozen_n3_concavity_v1.md`。
- 首要目标仍是证伪；有限扫描只作探针，不能晋级全称结论。
- 第一批上限：2 CPU、10 GiB、90 分钟、最多 200,000 次实际目标调用；不用 GPU，不续跑未完成的旧作业。

## P4-02：条件熵/事件坐标分解

- 用 `H(X_1,X_2,X_3)=H(X_3)+H(X_1,X_2|X_3)` 或八事件切空间写出精确二阶式。
- 检查条件 DPP 主核/Schur 补及混合权重的导数；不得把二维凹性直接套到非仿射条件核上。
- 成功门：显式反例、可审查 PSD/SOS 证书，或严格更小的剩余标量/矩阵不等式。

## P4-03：第一未覆盖中心

- 块对角中心已由 P3 覆盖；优先检查连通三点路径中心（恰两条非零边）及三角形中心。
- 先找对称/稀疏切片的严格反例；若无，只冻结能完整证明的特殊族，不把切片结果推广到一般三维。
- 首个冻结特殊族为 `frozen_n3_equicorrelation_v1.md`：复合对称中点、任意六维实对称方向；利用 `S_3` 表示分块攻击。

## 停止规则

- 严格反例立即进入高精度/有理证书。
- 同一缺口两个工作单元无新对象则退休。
- 9 小时总授权到期时停止新作业，收集终态并暂停心跳。
