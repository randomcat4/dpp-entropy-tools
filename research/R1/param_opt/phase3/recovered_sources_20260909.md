# 二维阶段复现源码补回

2026-09-09 主线整理发现：本目录已有 `result.md`、精确符号检查和 `formal_audit.json`，但相应运行副本中的三份源码尚未随 PR #8 入库。本次从原运行目录只读取回并原样归档：

- [n2core.py](n2core.py)：事件坐标参数化、完整概率、Fisher/Hessian 及高精度复核接口。
- [attack.py](attack.py)：有调用编号、资源限额和 SQLite 账本的有限探针入口。
- [audit.py](audit.py)：停止后的原始 SQLite 账本检查入口。

这是复现材料补齐，不是新定理或新搜索结果。数学依据仍是已入库的二维证明及其独立审查。源码未因恢复而获得新的完整正确性认证。

本次检查：三份文件语法解析通过；核心在三个固定参数点 `(0,0,-1)`、`(1,-1,0)`、`(-2,1,-3)` 的概率为正且总和为一，浮点 Hessian 最大特征值均负。没有重跑原 122,832 次攻击，也没有重放完整 SQLite 审计。

运行依赖：核心使用 NumPy，高精度接口使用 mpmath，优化阶段使用 SciPy；符号入口另需 SymPy。完整运行会在脚本目录内写结果。`audit.py` 依赖原 `formal/ledger.sqlite` 和 `formal/summary.json`；原始大账本不在本次补回清单内，已有 `formal_audit.json` 是历史摘要。读取 SQLite 是只读的，但脚本会写审计 JSON；它不是无副作用的证书检查 API。

本次没有执行默认长搜索，新增源码也不构成自动继续计算的授权。
