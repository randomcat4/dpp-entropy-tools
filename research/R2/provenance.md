# Provenance

- Problem and route assignment: user, 2026-09-08.
- Frozen-family selection and statement ownership: R2 main instance, informed
  by a separate multiscale-structure worker.
- Asymptotic proof authorship: isolated multiscale-structure worker; the main
  instance mechanically integrated the worker's directed revision after
  freezing the public statement and did not certify it.
- Independent boundary/certificate work: separate fresh worker context; it
  returned `CORRECT` and reproduced two strict rational log enclosures.
- Independent coefficient proof and Hessian audit: separate asymptotic worker
  which did not read the author proof before completing its derivation.
- Semantic audit: R2 main instance.  Proof correctness was reviewed by the
  boundary verifier and then by the asymptotic worker acting as a second domain
  reviewer after its independent proof was complete.
- Formal verification: none claimed.  The main instance ran an independent
  exact-rational law diagnostic with Decimal entropy values; it is not a proof
  or interval certificate.
- Systems: one R2 main context and three direct isolated workers; no recursive
  delegation and no GPU.  Candidate commit `693c29076943f4aa30aff6bf04b9d2ec26a1f42a`
  is bound by SHA256 in both verification reports.
- Search denominator: one frozen structural family; 3 deterministic fixtures,
  30 chords, and 1,200 exact event masses in the first bounded diagnostic.
- Reproducibility limits: no private server address, credentials, or private
  conversation is included in this public tree.

## Round 3 provenance

- Frozen-v2 statement ownership: R2 main instance.
- Structural component theorem and complete written coefficient proof:
  multiscale-structure proof instance.  The main instance independently found
  the same component-pair scalar formula and requested explicit scale,
  Parseval, and singular-boundary checks before integration.
- Independent proof-risk review: asymptotic-coefficients instance, which had
  reached a different weighted quadratic blocker before reading the candidate.
- Independent domain/computation review: boundary-certificate instance, using
  corrected signed cofactors and an exact-event check on a non-paired
  `n=8,r=4` datum.
- Counterexample-search denominator in Round 3: 892 fully gated signed-formula
  trials; zero positive values.  This finite non-hit is supporting evidence,
  not the proof.
- Formal status: candidate commit
  `3ae3323ae958feb733b78f5b425c6d9b540524e9` received two independent
  commit-bound `CORRECT` reviews.  Both matched the SHA256 values of the
  frozen statement, prior finite decomposition, component theorem,
  component-pair proof, and hazard list.
