# PR66 FIRST mathematical review

Verdict: **INCOMPLETE/CRITICAL_GAPS at the load-bearing Dobrushin import**.

I find the internal DPP/localization/equilibrium/curvature chain correct on the frozen assumptions, but PR66 cannot be marked ready from this FIRST review because the load-bearing external theorem was not verified from the actual full primary theorem statement. The accessible primary article record confirms Dobrushin's paper and abstract-level scope, but I could not read the theorem body/page identifying the exact interaction norm/class, analytic-family domain, and pressure/local-response conclusion needed for `P(lambda U_s)`. Novelty is not certified.

## Scope checked

The frozen PR66 theorem is exactly the polynomial weighted Wiener result for `p>4`, real `c,g in A_p`, half-period parity, strict center margin, arbitrary nonzero half-period-odd direction, any nonzero odd Fourier coefficient, and an existential local interval; it makes no mean-one-half, small-norm, reflection-evenness, uniform-radius, global-interval, unweighted-Wiener, measurable-symbol, or PR59 premise claim (`source-snapshots/pr66/frozen_statement.md:48-86`, `source-snapshots/pr66/README.md:7-16`, `source-snapshots/pr66/README.md:56-64`). The packet consistently uses complete-event atom determinants, not inclusion determinants (`source-snapshots/pr66/frozen_statement.md:36-44`, `source-snapshots/pr66/proof.md:50-64`).

## Claim dispositions

| Claim | Disposition | Review notes |
| --- | --- | --- |
| Frozen source and dependency scope | CORRECT | `SOURCE_BINDING.json` freezes eight added files at author head `af1edaad69c4e1f5e4bbd1239b8463b56bf64075`; accepted-main inputs are separately bound to `9dcb6e9079ca57f94e0e30d63161cda89ca61fae`. The author text disclaims PR59 use (`source-snapshots/pr66/README.md:7`, `source-snapshots/pr66/README.md:56`). |
| Complete-event singular gap, including rare words | CORRECT | For every finite `I` and complete event pattern, `J M` is accretive with real part at least `delta`; this gives a dimension/configuration-uniform inverse bound. The quadratic-form bound `-(1-delta)I <= M <= (1-delta)I` is also valid for any event projection. The argument only needs Hermitian positive contraction structure, so real non-even symbols and complex Hermitian Toeplitz kernels are covered (`source-snapshots/pr66/proof.md:66-104`). |
| Polynomial weighted inverse bound | CORRECT | The band-truncation proof is finite-section and configuration-uniform. With `q=(p+2)/4`, the truncated inverse has Schur norm `O(W^(q+1))`, the tail has size `O(W^(q-p))`, and the product exponent `2q+1-p=(4-p)/2<0` allows one fixed `W` for all windows and events (`source-snapshots/pr66/proof.md:106-179`, `source-snapshots/pr66/verification.md:61-103`). No infinite-matrix inverse theorem is being silently substituted. |
| Common complex response disk | CORRECT | The second Neumann argument in the weighted Schur algebra gives a disk independent of conditioning window and event, since `T_I(g)` is uniformly bounded in `S_q` (`source-snapshots/pr66/proof.md:181-205`, `source-snapshots/pr66/verification.md:105-125`). Legality on the real axis is separately obtained by shrinking using `||g||_infty`. |
| One-sided conditional and two-propagator influence | CORRECT | The conditional is the complete-event Schur complement. A remote bit flip changes the event matrix by `+- e_j e_j^*`, so the resolvent difference factors into two origin-to-`j` weighted propagators and gives `O(j^(-2q))`; adding the last future site has the same two-leg Schur form (`source-snapshots/pr66/proof.md:207-318`, `source-snapshots/pr66/verification.md:127-145`). |
| Uniform non-nullness and logarithmic potential | CORRECT | Enlarging by an occupied or vacant origin identifies the inverse entry with `q_R^{-1}` or the vacant analogue, giving lower bounds at `z=0`; the common holomorphic estimates allow a smaller common disk and consistent logarithm branches (`source-snapshots/pr66/proof.md:319-333`, `source-snapshots/pr66/verification.md:146-149`). |
| Telescoped finite-first-moment interaction | CORRECT | The telescope against the reference future has increments controlled by single-coordinate influence. Counting the `n+1` intervals of diameter `n` through the origin gives `sum n(n+1)n^(-2q)<infinity` exactly when `q>3/2`, hence under `p>4`. This is stronger than orbit-normalized first-moment conventions (`source-snapshots/pr66/proof.md:335-387`, `source-snapshots/pr66/equilibrium_bridge.md:20-42`). |
| Exact DPP equilibrium identification | CORRECT | The cross-entropy identity `h(rho)+rho(phi_t)=-int KL(r_rho||G_t)` is the right future-conditional entropy-rate identity for stationary finite alphabets. The `1/|A|` specific-energy normalization then gives `rho(e_U)=-rho(phi_t)` by stationarity, so the variational pressure is zero and the DPP is an equilibrium state (`source-snapshots/pr66/proof.md:389-455`, `source-snapshots/pr66/equilibrium_bridge.md:44-149`). |
| Dobrushin response theorem application | INCOMPLETE/CRITICAL_GAPS | The PR applies Dobrushin only after proving the stronger first-moment norm and Banach-holomorphic interaction curve (`source-snapshots/pr66/proof.md:457-467`). The accessible MathNet primary article page identifies Dobrushin 1974 as Math. USSR-Sb. 23:1, pages 13-44, DOI `10.1070/SM1974v023n01ABEH001712`, and its abstract says one-dimensional lattice/continuous Gibbs systems with power-law-decaying potentials have analytic specific free energy and correlation functions ([MathNet article page](https://www.mathnet.ru/php/archive.phtml?wshow=paper&jrnid=sm&paperid=3631&option_lang=eng), [Geodesic/MathDoc mirror record](https://geodesic.mathdoc.fr/item/SM_1974_23_1_a1/)). That is not enough to verify the exact theorem statement requested here: finite-first-moment threshold, arbitrary finite-alphabet interval/block interactions, Banach-neighborhood convention, holomorphic family dependence, and pressure derivative/local-response conclusions for the two-parameter family `lambda U_s`. A process-scoped network-bypass retry of the MathNet English full-text endpoint produced only an incomplete PDF prefix that standard text extraction could not parse; Project Euclid was blocked; and the Springer PDF endpoints returned HTML landing/paywall pages rather than theorem-body PDFs. Cassandro-Olivieri is only a corroborative coverage check from accessible metadata: its DOI page/abstract coverage concerns one-dimensional many-body potentials with finite first moment and analytic parameter dependence, but I did not obtain a full theorem body that would substitute for Dobrushin or prove the arbitrary-block pressure statement ([Springer DOI page](https://link.springer.com/article/10.1007/BF01213013)). This import remains a critical unclosed gate. |
| Parity gauge and analyticity in `s=t^2` | CORRECT | Half-period support gives diagonal gauge conjugacy under `z -> -z`; complete-event determinants, Schur complements, `G_z`, `phi_z`, and `U_z` are even and hence factor through `s=z^2` as Banach-holomorphic objects (`source-snapshots/pr66/proof.md:469-513`, `source-snapshots/pr66/verification.md:203-212`). |
| Entropy/pressure and relative-entropy bridge | CORRECT | The formula `h_s=P(U_s)-d/dlambda P(lambda U_s)|_{lambda=1}` follows from the zero-pressure equilibrium identity and the sign convention `DP(U)[V]=-nu_U(e_V)`. The specific relative-entropy identity has the stated sign, and its first derivative at `s=0` cancels (`source-snapshots/pr66/proof.md:493-558`, `source-snapshots/pr66/equilibrium_bridge.md:151-215`). No derivative of finite-window entropy per site is exchanged with a volume limit. |
| Finite-volume parity KL identity | CORRECT | Even and odd finite-block marginals are fixed in `t`; at `t=0` they are independent. Thus finite-block relative entropy against `t=0` is exactly the entropy deficit before taking the rate limit (`source-snapshots/pr66/proof.md:515-532`; accepted antecedent at `source-snapshots/accepted_main/research/I05-DPP-21-20260909/finite_range_local_theorem.md:54-86`). |
| PR53 matching lower bound import | ACCEPTED_SCOPED | The accepted PR53 record states the matching/negative-association lower bound for every fixed legal `t` and records that it is independent of analyticity (`source-snapshots/accepted_main/docs/verification_round3_20260909/accepted_pr53.md:35-39`). The underlying accepted proof uses only parity, fixed marginals, DPP negative association on disjoint odd-step edges, and entropy variational duality (`source-snapshots/accepted_main/research/I05-DPP-21-20260909/finite_range_local_theorem.md:88-128`). PR66 imports exactly this regularity-free component after proving response analyticity (`source-snapshots/pr66/proof.md:560-590`). |
| Quartic floor and local concavity | CORRECT | Analyticity gives `R(t)=A t^4+O(t^6)`. The accepted matching inequality forces `A >= |g_hat(k)|^4/[4 mu^2(1-mu^2)]`; shrinking the interval gives `R''(t)>=6C_k t^2`, and `alpha_k=C_k/2` makes the corrected entropy second derivative nonpositive (`source-snapshots/pr66/proof.md:549-621`). |
| Strictness beyond exponential weighted Wiener classes | CORRECT | The displayed power-law example has finite `A_p` norm and strict margin, but its infinitely many nonzero coefficients decay only polynomially, so it is in no positive exponential weighted Wiener class (`source-snapshots/pr66/frozen_statement.md:88-116`, `source-snapshots/pr66/verification.md:279-287`). |
| Smooth-approximation obstruction | CORRECT | The scalar entire family demonstrates that uniform value convergence plus approximant-dependent local concavity intervals does not imply local concavity of the limit. The packet correctly limits this to an obstruction to the proof mechanism, not a DPP counterexample (`source-snapshots/pr66/route_comparison_and_obstruction.md:30-104`, `source-snapshots/pr66/verification.md:289-309`). |
| Remaining lower-regularity/global questions | CORRECTLY OUTSIDE SCOPE | The packet leaves sharpness of `p>4`, unweighted Wiener, arbitrary measurable symbols, direct negative-association Jensen, global legal-interval concavity, and genuine entropy-rate counterexamples open (`source-snapshots/pr66/attempts.md:80-109`, `source-snapshots/pr66/README.md:64-82`). |

## Minimal repair

Required repair or review closure:

1. Obtain the actual Dobrushin full text and cite the theorem/page that states the applicable one-dimensional result.
2. Identify the exact interaction class or norm in that theorem and show it contains PR66's finite-alphabet interval/block interaction satisfying `sum_{A contains 0} diam(A)||U_A||_infinity < infinity`.
3. Identify the theorem's analytic dependence domain: whether it gives holomorphic dependence for a Banach-neighborhood interaction family, and whether it covers the two-parameter family `lambda U_s` near `(s,lambda)=(0,1)`.
4. Identify the theorem's conclusions: pressure/specific free energy analyticity and enough local-response or pressure-derivative analyticity to justify `h_s=P(U_s)-d/dlambda P(lambda U_s)|_{lambda=1}`.

If the full theorem supports these items, this import can be upgraded without changing the internal PR66 lemmas. If it does not, PR66 needs a replacement response theorem or a revised proof of the true entropy-rate analytic bridge.

## Review limitations

I performed a source-only mathematical review. I did not run arithmetic, symbolic algebra, numerical jobs, entropy computations, formal verification, or tests. I did not assess novelty or priority beyond the repository's accepted PR53 baseline.

Access limits recorded: MathNet article page was readable, but the English PDF endpoint produced only an incomplete PDF prefix after a bounded retry; standard text extraction failed on that partial file. Project Euclid/CMP access was blocked by a security/interstitial path; the app browser was unavailable; Springer DOI/PDF endpoints returned HTML landing/paywall pages rather than theorem-body PDFs. Therefore I did not establish a Dobrushin theorem/page pin from actual full text.
