# D10-M4 非作者验缝

日期：2026-09-08。最终裁决：**CORRECT（n=2定理与n=3证明法阻碍）**。
n≥3的固定特征向量PSD谱速率熵凹性问题仍为 **INCOMPLETE**。

作者文件只读。独立脚本没有导入作者代码，也未修改作者稿或主README。
以下区分解析证明、精确算术和未覆盖结论。

## 1. n=2精确事件及任意Q范围：通过

对任意Q∈O(2)，令x=Q11²。行列正交性给Q12²=Q21²=1−x、Q22²=x，
故不仅旋转矩阵，反射、置换、坐标符号改变也全部被同一个x∈[0,1]
参数覆盖。det K=lambda1 lambda2，而单点精确事件为Kii−det K，所以
作者四个原子公式正确；空事件是det(I−K)。

这些确为包含概率的Möbius反演，不是把det K_S当作精确事件。严格谱
lambda_i∈(0,1)给r=lambda1(1−lambda2)>0、q=lambda2(1−lambda1)>0，
因此x=0或1处两个单点概率也严格正，不存在split熵的隐藏零分母。
重复谱lambda1=lambda2同样无需排除，Q可以任意固定。

K(t)=Q diag(lambda+t v)Q^T本身是K-affine。任意有限非零v≥0下，严格
谱基点均有双向的小可行区间；步长可以依赖lambda,v，作者没有错误地
要求跨所有未归一化速率使用同一步长。D=Qdiag(v)Q^T≽0且非零。

## 2. count/split分解：通过

基数N=|Y|是Y的函数，因此链式法则给
`H(Y)=H(N)+s H(Y|N=1)`；N=0、2的配置唯一。
而后项正是作者的
`Phi(p1,p2)=−p1 log(p1/s)−p2 log(p2/s)`。

这里没有将全配置熵偷换为基数熵；singleton层的剩余熵已经完整保留。
该分解在所有Q、严格谱以及x边界都有效。

## 3. count项严格性：承重AM–GM界通过

若v1v2=0且v≠0，count原子对t仿射，且
`p_empty'=−v1(1−lambda2)−v2(1−lambda1)<0`，所以不是常数分布；
`H(N)''=−Σ(p_k')²/p_k<0`严格成立。没有漏掉秩一边界。

若v1v2>0，作者符号
`H(N)''=−F_N+2v1v2 log(s²/(ab))`
正确，其中a=p_empty、b=p_full。为独立核对未写出的AM–GM细节，置
z=v1/v2。丢掉F_N中非负的singleton-count项后，余下两项精确展开为

```
F_N/(v1 v2) >= z C + z^(-1) E + 4,
C = (1-lambda2)/(1-lambda1)+lambda2/lambda1
  = s/[lambda1(1-lambda1)],
E = (1-lambda1)/(1-lambda2)+lambda1/lambda2
  = s/[lambda2(1-lambda2)].
```

CE=s²/(ab)，故zC+z^−1E≥2sqrt(CE)=2X，得到作者所用的2X+4。
又ab=rq、s=r+q，故X≥2。函数f(X)=X+2−2logX在X≥2上非减，且
f(2)=4−2log2>0。因此

`2X+4>4logX`，即 `F_N>2v1v2 log(s²/(ab))`。

最终count曲率严格负。这个严格差来自标量不等式，并不要求AM–GM
本身严格，故lambda1=lambda2、v1=v2等相等情形没有严格性漏洞。

## 4. split项符号：通过

Phi的梯度为(log(s/p1),log(s/p2))，两项实际上均严格正；Hessian为
作者所列的负半定矩阵，行列式0，非零特征值负。由于两个混合单点
原子都满足 `p1''=p2''=−2v1v2≤0`，二阶复合求导给Phi''≤0。
因此count严格负＋split非正，对所有非零v≥0都推出H''<0。

v=0被明确排除；若纳入则H''=0。严格谱边界0、1未被纳入证明；没有
把log奇点处的陈述自动延拓。作者的局部结论成立，不涉及n≥3。

## 5. n=3谱展开与p_{1}''：精确算术通过

作者任意事件谱展开可直接由
`L=Q diag(lambda_i/(1−lambda_i))Q^T` 的Cauchy–Binet展开，再乘
`det(I−K)=Π(1−lambda_i)`导出。因此全配置层公式正确，不只是单点
包含概率公式。对singleton的三重乘积求两次导，外层因子2及三个
正/负项也正确。

独立脚本从有理Q直接构造K(t)，对每个包含主子式用置换展开完整
多项式，再作Boolean Möbius反演；另从Q子式平方谱展开独立计算全部
事件，两路8个事件的全部系数精确一致。Q Q^T=I也由有理算术检查。

对作者的Q、lambda=(3/4,1/2,1/2)、v=(1/100,1,1)，得到

`p_{1}(t)=121/784−(10777/19600)t+(2339/4900)t²+(1/100)t³`，

故确有 **p_{1}''(0)=2339/2450>0**。
其余singleton二阶导为−829/2450、−167/1225。

非阻断措辞提醒：n3_blocker.md第71–74行称(37/25,−1/2,−1/2)是
“bracket”里的列贡献。这三个数已经包含公式外层的2；裸方括号值
实际为(37/50,−1/4,−1/4)。后续p''计算已正确吸收外层2，最终结果
没有因子错误。建议称“含外层因子2的列贡献”，避免读者再乘一次2。

## 6. 它确实仅是方法阻碍，不是熵反例

本例所有速率严格正，D满秩3；lambda+t v在|t|≤1/10内的严格谱裕量
至少249/1000，故p''的正号不是不可行点或边界效应。但单个p_i''>0
只能否定n2证明所用的“每个层内原子沿PSD速率都凹”性质，不能推出
层split熵凸，更不能推出总配置熵凸。

本验证还独立认证了该例的总曲率实际上严格负。由全8事件精确
p,p',p''，计算
`H''=−Σ(p')²/p−Σp''logp`，采用有理atanh级数的带余项对数界，得到

`−6.5704807903 ≤ H'' ≤ −6.5704807902 <0`。

证书使用24项级数：将p=2^−k z归一化到1≤z<2，设u=(z−1)/(z+1)，
以
`2Σ_(j=0)^(N−1) u^(2j+1)/(2j+1)`作log z下界，余项上界为
`2u^(2N+1)/[(2N+1)(1−u²)]`，并对log2同样处理。所有界与符号传播
使用Fraction精确算术，最后向外取整到分母10^10。并非浮点负号冒充
区间证书。完整原子多项式、Fisher有理值及区间保存于验证JSON。

因此作者“blocker，不是counterexample”的结论准确。最小性仅针对
这个原子二阶导符号方法：n2中已证明该符号非正，n3即可失败；不能
解释为最小维数的熵凹性反例。

## 7. 验证分母、命令与裁决范围

运行命令（仓库根）：

```
& 'C:/Users/UIO/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' research/R3/deepening_10h/dense_hessian/commuting_spectral/low_dim_exact/verifications/fresh_exact_check.py
```

实际使用已配置Python3.12.14运行时；仅标准库，退出0，
0.005713224411010742秒。无服务器、GPU或依赖安装。

独立分母为5个n2有理案例（旋转、反射、x端点的两类秩一、重复谱），
每例4个完整事件多项式；另1个n3阻碍例，8事件，总28个事件多项式。
所有n2案例和n3阻碍例都另有有理对数界的负总曲率证书。0随机提议、
0失败、0熵正候选。有限案例只检验算术；n2全称结论依据上述解析证明。
未重跑作者20万次scout，不把该历史样本数计入本验证。

裁决分层：

- n2任意固定Q、非零v≥0严格负曲率定理：CORRECT。
- n3的p_{1}''=2339/2450及“证明法失效”定位：CORRECT。
- n3或更高维的一般PSD谱速率熵凹性：INCOMPLETE。

冻结作者材料SHA256：

- theorem.md：`DA10ADDEF3CBA14761C2B5F14857085CA529B2A8568DB0B01AD1D2ACE679CC85`
- n3_blocker.md：`BB8B7178EB50274B000EE4BE1D01093727C945128D3329C2475CAA6247023A19`
- verdict.md：`C4BAEEEB528FC863F8D19F5E3B4F4F8AC750537B240E3E5F0EF48144BDA06099`

本报告使用math-theorem的非作者验缝角色；不对新颖性、会议价值或
一般全域问题作认证，也未运行Lean。
