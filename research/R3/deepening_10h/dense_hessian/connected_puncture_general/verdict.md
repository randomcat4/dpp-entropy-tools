# D10-U7 verdict

STATUS: `CORRECT`. A fresh non-author audit is preserved under
`verifications/`.

For every fixed finite n, every strict diagonal X and every real zero-diagonal
A with connected support and nonzero supported weights, the full Sym(n)
entropy Hessian at X+epsilon A is strictly negative definite for all
sufficiently small nonzero epsilon. The graph-distance-scaled Hessian limit is
strictly negative diagonal, with coefficients given by six times the positive
shortest-path squared-product sum.

The proof also gives necessity of connectedness via exact cross-block flat
directions. Thus the small-puncture classification is connected iff in every
finite dimension. All trees are included; no restriction to one rational
diagonal, equal weights, PSD directions, or commuting directions is used.

The essential new mechanism is residual-support Hessian locality plus a minimum vertex-degree-four rule for entropy coefficients. Together they eliminate ALL same-scale mixed entries, not just selected same-distance blocks. The universal doubled-cycle coefficient is proved by a matching/coloring polynomial identity.

What is established here versus excluded:

- Analytic argument: `CORRECT` after a fresh non-author review of all three
  load-bearing combinatorial bridges and the analytic remainder orders.
- Eight cycle matching tests, five determinant-identity tests, five n6 graph-jet probes, one disconnected-support cancellation and one direct-Mobius gate: exact finite sanity, SCOUT only.
- Independent correctness review: complete. The audit specifically checks
  Lemma 3.2, the equality case in section 4, and the doubled-cycle coefficient.
- Novelty and relationship to any external theorem: not assessed in this unit.
- Global entropy concavity away from small punctures, a parameter-uniform explicit radius, or behavior at epsilon=0: not proved or claimed.

No counterexample was produced. No finite non-hit is used as a theorem. Existing U4/U5/S8c documents and shared indexes were left unchanged; nothing was submitted.
