# D10-U10h: completing the exchangeable-triangle determinant gate

AUTHOR RESEARCH. New structural and boundary claims are candidates pending
independent review; the entire exchangeable domain remains INCOMPLETE.

For K=xI+a(J-I), set alpha=x+2a and beta=x-a. Work on
0<alpha,beta<1 with alpha!=beta. All directions in the conclusion are
arbitrary real symmetric directions, not just the invariant-plane tangents.
The exact atoms and the full-Hessian reduction to Delta_T=det C>0 are the
scoped results checked in
[U10f fresh audit](../exchangeable_triangle_subfamily/fresh_audit.md).

This unit seeks a full proof or a new genuine continuous subdomain. It does
not change the frozen question if the scalar inequality remains unresolved.
No finite grid, boundary sequence, or author self-check can certify a whole
interval. No novelty claim is made.

Prior inputs frozen by the accompanying script: U10f proof and fresh audit.
The global domain excludes all spectral boundaries and the diagonal ridge.
Boundary laws are used only to derive limits from the strict interior.
