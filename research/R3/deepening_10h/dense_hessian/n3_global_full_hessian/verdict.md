# D10-U8/M10 verdict

GLOBAL n=3 FULL-HESSIAN QUESTION: INCOMPLETE.

STRUCTURAL RESULTS: `CORRECT` after separate fresh non-author reviews of the
main reduction and the rank-one boundary asymptotic.

- An exact positive-form-minus-rank-one representation reduces connected-global full-Hessian positivity to the explicit scalar threshold rho<=1 (strictly <1 for definiteness).
- At every connected strict kernel, entropy curvature is strictly negative on a specified five-dimensional weighted-trace-zero hyperplane; at most one Hessian direction can remain nonnegative.
- A dense rank-one boundary subclass and its complement have eventual full-Hessian negativity, with rho approaching one from below at deficit 1/(theta log(1/epsilon)).
- The Lambda=0 connected strictness is proved analytically, not sampled.

All finite searches remain SCOUT. There were 510 initial targeted rational centers, no positive-curvature candidate, and 30 additional explicitly parameterized boundary probes. A single frozen near-boundary scout point also passed an exact rational-log congruence gate. None of these finite facts is promoted to a global theorem.

The remaining blocker is the DPP-specific Fisher inequality in
proof_or_blocker.md, equivalently the global scalar bound `rho<=1`. The main
audit independently verifies the rank-one representation, `N>0`, the Schur
threshold and coordinate factors. A separate audit verifies the moving-space
boundary asymptotic and its sign. No novelty claim is made.
