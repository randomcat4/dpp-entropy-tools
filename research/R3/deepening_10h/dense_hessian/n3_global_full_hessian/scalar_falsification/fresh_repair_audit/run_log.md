# Fresh non-author repair audit run log

Scope: repaired U10c scalar_falsification frozen data. This reviewer did not
author U10c. No author search or previous audit module will be imported.
Outputs are restricted to fresh_repair_audit. Finite conclusions remain SCOUT.

The independent reconstruction and exact input hashes will be recorded below.

Command, with working directory fixed to this fresh_repair_audit directory:

```powershell
& 'C:/Users/UIO/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' ./audit.py
```

Exit code 0. Elapsed independent reconstruction: 18.51419186592102 seconds.
No author/search/audit implementation was imported. No author file changed;
all six input hashes were rechecked at completion. The only Python imports
are standard-library modules.

Inputs read: repaired compact search ledger, near-threshold candidate
artifact, repair note, author verdict/run log, and author source solely for
fixed grid metadata and the rational orthogonal Q. No previous audit
implementation was read or imported.

Independent arithmetic: exact rational signed-event adjugate jets, separate
exact inclusion-Möbius reconstruction, Fraction strict principal minors,
and Decimal Fisher/N/Schur scalar at 240 to 1716 digits. All 280 fixed-grid
boundary parameters were regenerated; all 150 saved path parameters and
15 saved top-float matrices were replayed. Of 445 attempts, 444 are strict
and below threshold; one is an exact nonstrict rejection matching the
author's recorded failure.

Discovery failures retained: a metadata-inspection command initially
assumed every top recheck had a rho field and raised KeyError (exit 1).
Inspection then exposed the explicitly recorded rejected point. This was
an inspection-schema mistake, not a failed reconstruction or dropped row.
The actual audit.py computation had no failed run.

The independent near-threshold count is 69, matching the compact ledger
but not the author verdict's stale 54. The timing discrepancy and the
14-valid/15-attempted distinction are reported without editing source prose.
The compact 38,436/22,023 float accounting is arithmetically consistent;
the full raw float seed trajectory was not regenerated and remains outside
this fresh recheck's verified coverage.
