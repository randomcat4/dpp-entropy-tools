# Run log

Only symmetric_path_subfamily was written. No imports of previous author
research modules, no dependency installation, no remote computation, no
subagent, and no commit/shared-index change occurred.

1. Read the reviewed U8 full-Hessian identity and froze the exact two-
   parameter domain and full-Sym(3) direction scope.
2. Derived reflection splitting and the centered-line second symmetry.
3. A read-only probe for SymPy availability returned ModuleNotFoundError,
   exit code 1. No package was installed. A small standard-library formal
   polynomial implementation was used instead.
4. Derived the 3 by 3 core, its 2 by 2 Schur form, and the two logarithm
   inequalities. Exact polynomial expansion independently checked the
   determinant identity before the complete positivity argument was written.
5. Executed the bounded 16-center sanity script, exit code 0. JSON records
   every input, atom, exact feasibility pivot, floating Hessian pivot,
   formula residual, script SHA256 and elapsed time.

Command from the assigned repository root:

```powershell
& 'C:/Users/UIO/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' research/R3/deepening_10h/dense_hessian/n3_global_full_hessian/symmetric_path_subfamily/sanity.py
```

Denominator: x in {1/10,1/4,1/2,3/4,9/10}, a=min(x,1-x) times each of
{1/4,1/2,2/3}, plus (x,a)=(1/2,7/20): 15+1=16 centers. Four of these
lie on the centered line and check the closed-form core. No random point
generation and no adaptive increase of the finite denominator was used.
