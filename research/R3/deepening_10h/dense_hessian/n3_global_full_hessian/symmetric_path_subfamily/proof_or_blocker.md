# Result and remaining obstruction

FULL TWO-PARAMETER FAMILY: **INCOMPLETE**.

COMPLETE ANALYTIC SUBCLASS ARGUMENT, PENDING REVIEW:

For every nonzero a with 8a²<1, the centered path K(1/2,a) has B>0 on all
Sym(3). The proof covers the entire open feasible a interval, including
arbitrarily near its excluded endpoints. It is not a theorem only about
directions tangent to K(x,a).

Reflection gives an automatically positive two-dimensional odd block at
every (x,a). At x=1/2 a second entropy symmetry separates the even edge
direction. The remaining three-dimensional form has a two-dimensional
Schur determinant equal to

[m(8-(1-r²)n²)+8r²z+16r²-(2-r²)z²]/(2-r²),

with r=8a², n=log((1+r)/(1-r)), m=-log(1-r²), z=rn-m.
The elementary bounds (1-r²)n²<4 and 0<z<2r² make it strictly positive.

Compactness then proves a full two-parameter neighborhood around every
compact nonzero centered-edge interval, uniformly over that interval and
all unit symmetric directions. No explicit neighborhood width is claimed.

The exact unresolved general-x condition is sigma(x,a)>0 in equation (10)
of derivation.md. It is a scalar Schur complement of an explicitly positive
three-dimensional restriction of the reflection-even block. The remaining
domain can be restricted to 0<x<1/2, 0<a<x/sqrt(2) by complement/sign
symmetry. Neither this scalar's positivity over the whole domain nor a
counterexample is supplied.

The boundaries a=0, x=0/1, and a=min(x,1-x)/sqrt(2) remain outside the strict
theorem. The a=0 boundary can have flat directions and must not be included
by a blanket strictness/continuity statement.
