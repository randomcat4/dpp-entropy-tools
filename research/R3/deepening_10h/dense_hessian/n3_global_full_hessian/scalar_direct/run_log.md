# Run log

Workdir: the assigned repository root. Only scalar_direct was written.
No author U8 module was imported; no remote computation, installation,
subagent, commit, or shared-index change was performed.

Input: read the reviewed U8 derivation, boundary_asymptotic and final verdict.
Used the math-theorem workflow to separate identities, sufficient subclasses,
surrogate counterexamples, and the unresolved general statement.

Command:

```powershell
& 'C:/Users/UIO/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' research/R3/deepening_10h/dense_hessian/n3_global_full_hessian/scalar_direct/sanity.py
```

Sequence and failure ledger:

1. Initial 22-point bounded sanity: exit 0. Derived the trace-capacity and
   five-category formulas; both identities matched independent matrix solves.
2. Added four complementary boundary points, total 26: exit 0. The fixed
   orientation failed, consistent with its analytic limit three.
3. Tested exactly three further path parameters 1/4,3/10,1/3 (not a random
   expansion). The latter two defeated both five-category orientations.
   They are retained in the final 29-point denominator.
4. Added a rational logarithm certificate for the 3/10 path. The mathematical
   assertions passed but JSON serialization failed on tuple-valued Fraction
   intervals, exit 1. No successful numerical certificate was claimed from
   that failed output operation. Fixed the recursive encoder to handle
   tuples, retained this failure record, and replayed all checks.
5. Added the three exact conditional-score rank-one updates and the first
   contrast's rational witness recovery. Final full replay is exit 0; the
   JSON records elapsed time, source SHA256, all 29 inputs, spectra-feasibility
   pivots, exact atoms, exact score-contrast data and high-precision outputs.

No finite denominator is interpreted as a theorem. The stronger adaptive
five-category hypothesis was considered provisionally, actively attacked,
and rejected by an exact proxy counterexample within this unit. It was not
silently retained as an assumption of the remaining proof.
