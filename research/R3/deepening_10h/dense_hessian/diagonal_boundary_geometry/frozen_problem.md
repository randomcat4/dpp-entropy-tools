# D10-U6/S10 frozen geometric corollary

Let `Omega_n={K in Sym(n): 0<K<I}` and let

```text
N_n={K in Omega_n : Hess H(K) is negative definite on Sym(n)}.
```

Using only the independently verified D10-U3/S9 theorem, determine the local
topology of `N_n` at a strict observation-coordinate diagonal kernel
`X=diag(x)`.

The candidate must distinguish:

1. topological closure/boundary statements;
2. angular genericity among zero-diagonal puncture directions;
3. a positive-measure conical sector with a uniform radial threshold over a
   compact diagonal box;
4. what is and is not claimed about density in the entire strict kernel
   domain.

No finite sample or numerical grid may justify a measure or topology claim.
