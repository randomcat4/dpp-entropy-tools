# R3 十小时深化：当前结论

原始目标仍为 `INCOMPLETE`：尚未得到严格正的实对称 DPP 熵中点 gap，
也没有证明全实域凹性。本目录的进展不是“更多负随机点”，而是把若干
近零来源、不可行结构和仍可能翻号的机制分开了。

## 最初 `7.11e-15` 正号后来查到了什么

它不是只被贴上“舍入噪声”标签就丢下。原记录的正 gap 恰好是中点熵
尺度上的 2 个 ULP；140 位重算把符号改为
`-9.8554731864878665e-16`，中心二阶曲率约 `-0.0438911`。沿同一方向把
步长从原值的 `2^-8` 扫到 `2^2`，高精度 gap 全为负，且小步二阶差收敛
到解析 Hessian 的 `-0.0438679`。

随后围绕原点和另一最弱曲率点做了 800000 条带检查点的可行弦提议，
没有一条越过预设正候选门槛。附近确实反复出现 `1e-14` 量级的“正
Hessian 特征值”，但它们都沿着被压到近零的跨块耦合坐标；同方向的
可解析有限弦 gap 为明确负数。后续固定块全局不等式又从理论上排除了
保持两块边缘不变、只改跨块耦合的整个截面。因此这里不是一个孤立正点；
周围存在的是一片近解耦、曲率接近平坦而真实下降的区域，浮点本征求解会
在这片区域两侧抖动。详尽原始数值保存在 `../noise_followup/`。

## 已经独立核验的结果

1. **固定块边缘的全局排除。** 对
   $K=\begin{psmallmatrix}A&X\\X^T&B\end{psmallmatrix}$，
   \[
   H(K)\le H(A)+H(B)=H(A\oplus B),
   \]
   且等号当且仅当 $X=0$。因此只改变跨块耦合、不改变两个边缘核的
   路线在整个可行截面上都不可能产生正 gap。另有安全但粗糙的定量界
   $H(A)+H(B)-H(K)\ge8\max_{ij}X_{ij}^4$。
2. **路径稀疏 `L` 的精确 K-仿射闭包。** 固定单位双对角 $R$，令
   \[
   S(\tau)=R^{-1}\operatorname{diag}(\tau)R^{-T},\quad
   L(\tau)=R^T\operatorname{diag}(1/\tau)R-I,
   \]
   则 $K(\tau)=I-S(\tau)$ 对 $\tau$ 线性，而 $L(\tau)$ 始终是
   三对角路径核。在明确开凸域内，这给出任意维、connected、heterogeneous
   的精确 K 中点；方向秩等于改变的 $\tau_i$ 个数。`n=5..30` 的
   三类方向共 12480 条有限弦没有正 gap，但这只是侦察，不是凹性定理。
3. **互信息分解。** 对任意二分 $E=A\sqcup B$，
   \[
   \Delta_E=\Delta_A+\Delta_B+I_0-\frac{I_-+I_+}{2}.
   \]
   等价的概率单纯形分解为
   \[
   \Delta=-\langle q-p_0,\log p_0\rangle
          -\mathrm{KL}(q\|p_0)-\mathrm{JS}(p_-,p_+).
   \]
   它精确指出正 gap 必须由事件分布的几何推动压过 KL 和 JS 两项。
4. **一般稠密 Hessian 工具。** exact-event 公式、完整实对称坐标 Hessian、
   Fisher/事件加速度分解、Möbius 对照和可行弦检查均已非作者复核。
5. **均匀核的近零来源。** 在 $K=I/2$，
   \[
   H''[D,D]=-4\sum_iD_{ii}^2.
   \]
   二阶零空间恰为零对角方向；但任意非零零对角方向满足
   \[
   H(I/2+tD)=n\log2-8t^4\sum_{i<j}D_{ij}^4+O(t^6).
   \]
   所以这是一个二阶变平、四阶严格向下的特异点。沿每条固定射线，附近
   去掉中心后曲率严格为负；相应的机器精度微小 gap 不能当成正候选。
6. **任意维 twin-pair 定号方向排除。** 若只改变两个具有相同外耦合的
   坐标，二点方向为 `[[d,e],[e,d]]` 且 $d^2\ge e^2$，则在整条严格
   可行区间上
   \[
   H''\le-4(d^2-e^2)\log2.
   \]
   该类允许 rank 2、真实外耦合、任意异质余部和 $n\ge11$；不是直和或
   thinning。证明经条件化 Schur 补与独立有理事件复算通过。一般
   PSD/NSD 方向是否均凹仍未解决。
7. **路径族的二阶递推与一次被追清的伪阳性。** 对上面的 fixed-beta
   路径族，selected-run 递推现可在 $O(n^2)$ 时间直接算出
   $H,H',H''$，不用有限差分。它和 `n=5,6,7` 的全事件 Möbius Hessian
   对照通过。旧实现曾在 `n=93` 报出 $H''\approx184.745$；冻结全部参数
   后发现，$Z\approx1.34\times10^{118}$ 使浮点 `Z^-3` 下溢成零，凭空
   漏掉约 `184.75184379`。稳定商递推与 110 位 Decimal 均给
   $H''\approx-0.007024918934$，实际可行弦也从负侧收敛。该点不是不可行：
   精确有理 LDL 还证明整段 $|t|\le1/10$ 的 DPP 谱裕量至少为 `1/50`。
   修正后 `n=5..100`、rank2/full-rank 共 4608 个方向没有正号；递推、
   异常归因和重跑统计均经非作者独立复现。有限未命中仍不是凹性定理。
8. **半正定方向的又一个精确排除锚点。** 若 K 和方向 D 在原观察坐标
   中都为对角矩阵，则 DPP 是独立 Bernoulli 乘积，且整条严格可行线上

   \[
   H''=-\sum_i\frac{d_i^2}{x_i(1-x_i)}<0\quad(D\ne0).
   \]
   这覆盖任意秩对角 PSD/NSD 方向，但不能把“同时可正交对角化”偷换成
   “原观察坐标对角”，因为配置熵不是谱函数。另有 22511 次冻结中心
   PSD 提议/控制和 940 个小维中心、14440 个 PSD 方向的有限重放，未见
   正候选；这些计数已经复现，但不证明一般 PSD/NSD 凹性。
9. **路径族存在整片弱耦合负曲率区。** 固定维数、紧盒
   $\tau\in[a,b]^n\subset(0,1)^n$ 和非空异质性裕量后，存在一个统一的
   $\varepsilon>0$：只要每条路径耦合满足 $0<|\beta_i|<\varepsilon$，
   每个三对角 $L$ 的路径图都 connected、核严格可行且 heterogeneous，
   并且对所有非零 $\tau$ 方向同时有

   \[
   H''\le-2\|\delta\|_2^2<0.
   \]
   这是由独立 Bernoulli 端点的统一谱隙和紧致连续性推出的连续区域
   定理，不是有限抽样。初稿曾把端点的 `-4` 界误写成处处严格；非作者
   给出等号例后，作者修正了等号条件，第二轮复审判定 `CORRECT`。任意
   耦合大小的 fixed-beta 路径族仍未解决。
10. **二维固定谱基的 PSD 速率全部严格向下。** 对任意
    $Q\in O(2)$、$0<\lambda_i<1$ 和非零 $v_i\ge0$，固定特征向量的
    仿射线

    \[
    K(t)=Q\operatorname{diag}(\lambda_i+t v_i)Q^T
    \]

    满足 $H''(0)<0$。证明把四个精确事件拆成基数熵与 singleton 层的
    perspective 熵：前者严格凹，后者非增曲率。它覆盖反射、置换、重复
    谱和秩一边界。三维不能照搬：一个精确有理例已有单个 singleton
    事件 $p_{\{1\}}''=2339/2450>0$，但该例总熵曲率仍被区间证书压在
    `[-6.5704807903,-6.5704807902]`，所以只是证明法阻断，不是反例。
    二维定理与三维阻断均经非作者核验；$n\ge3$ 一般问题保持未完成。
11. **固定谱基路线被精确化成一个条件通道问题。** 若
    $K(t)=Q\operatorname{diag}(\theta_i(t))Q^T$，可以先独立抽取谱子集
    $Z_t$，再经与 $t$ 无关的 projection-DPP 通道产生观察集 $Y_t$；
    通道系数正是 $\det(Q_{S,R})^2$。因此

    \[
    H(Y_t)=H(|Z_t|)+H(Y_t\mid |Y_t|).
    \]

    第一项是已知凹的 Poisson-binomial 基数熵；若总曲率要转正，唯一
    可能来源就是第二项的固定基数通道熵。任意二维固定谱块、由一维和
    二维观察坐标块组成的直和，以及 cardinality-uniform 投影通道都已
    闭合为凹子类。generic $Q$ 的条件通道曲率仍未控制。表示、导数公式、
    熵分解和这些子类均经独立 exact-event 核验。
12. **三维条件通道已约化并做定向排查。** 对 $3\times3$ 正交基，令
    $P_{ai}=q_{ai}^2$，singleton 与 pair 层的 exact atoms 分别是
    $Pr$ 和 $Ps$，所以未知条件项精确变成 $G_P(r)+G_P(s)$。202000 个
    PSD 谱速率方向没有正总曲率；singleton 或 pair 条件层各自都能明显
    向上弯，但每次都被另一个层的强负曲率抵消。最好总条件曲率只有
    `-4.75484e-9`，靠近零却未越过。这支持“补集两层存在抵消”的下一
    定理靶点，但仍只是经复核的 `SCOUT`，三维全称结论保持未完成。
13. **固定谱基机制比继续升到 0.5464，但仍稳定为负。** 十二维四片
    单线程定向扫描共 20000 个中心，浮点正候选为 0；最好正谱速率点的
    $\rho$ 从上一轮约 `0.5261` 提高到 `0.5464281988`。独立 80 位
    exact-event/Möbius 复算给
    $H''=-32.4367046423\ldots$，三个步长的实际 gap 全负；精确有理 LDL
    证明十进制有理化后的 PSD 线在 $|t|\le1/200$ 上至少有 `1/2000`
    谱裕量。需要严格区分：数值 scout 按同一个浮点 $Q$ 构造 fixed-Q
    方向；逐项十进制有理化后交换子约 `1.87e-16` 而非精确零，所以 exact
    证书认证的是 near-commuting PSD K-仿射线，不是精确 commuting 有理
    对象。冻结点和账目已核验，20000 次未命中仍只标 `SCOUT`。
14. **联合改变谱与谱基后，机制比升到 0.5686，仍未翻号。** 从上一
    fixed-Q 冻结点出发，在不同中心之间小幅旋转谱基并扰动谱，每个中心
    内仍优化正谱速率；四片共 20000 个新提议，正曲率和正弦差计数均为
    0，最大弦差约 `-5.59e-5`。最强点的
    `rho=0.5685905197`，独立 90 位 exact-event 重算给
    `H''=-34.3715675595...`，三个步长的实际弦均负，精确有理 LDL 又
    证明有理化 PSD 线在 `|t|<=1/200` 内至少保留 `1/2000` 谱裕量。
    产生脚本曾漏记已知为负的 source 行；该缺陷不影响 20000 个新提议
    的逐行零命中结论，且后续脚本已修复并经单独预审。冻结点与账目为
    `CORRECT`，有限外层搜索仍只为 `SCOUT`。
15. **任意异质对角盒周围都有一片 PSD/NSD 严格负曲率区。** 对观察
    坐标下任意严格对角核 `K0=diag(x_i)` 和任意实对称方向 `D`，精确有

    \[
    H''_{K_0}[D,D]=-
    \sum_i\frac{D_{ii}^2}{x_i(1-x_i)}.
    \]

    非对角项虽然会进入各事件的二阶加速度，却因总质量与 singleton
    边缘约束在熵中完整抵消。任意非零 PSD/NSD 矩阵必有非零对角，且
    `sum D_ii^2>=||D||_F^2/n`；因此固定维数和紧对角盒后，由连续性得到
    盒子周围一个统一开邻域，其中全部非零 PSD/NSD 方向均严格向下。
    这是真正的连续区域定理，已由非作者以异质有理 exact-event 复算
    核验，不是从有限随机点外推。
16. **三维条件层的全锥侦察经纠错后仍未见正总曲率。** 固定
    `(Q,theta)` 后，总 Hessian 在非负谱速率锥上的符号可化成 3x3
    copositivity / 单纯形二次型问题；修订扫描检查 60384 个基点和
    416691 个 support 候选，总正号为 0。初稿曾漏写 copositivity 公式
    加号，并把一个会漏奇异 KKT 点的数值函数说成“一般精确最大值”；
    审计给出反例后，作者撤回过强声明、加入奇异和大尺度回归并全量重跑，
    修订复审通过。另有精确有理例严格证明条件和 `Psi''>0`，所以“条件
    层本身凹”已被否定；仍可能成立的只剩它被负的基数熵屏障抵消的联合
    不等式。有限批次保持 `SCOUT`，一般三维结论仍 `INCOMPLETE`。
17. **修复记账后的第二轮联合细化达到 0.57154，仍严格为负。** 新脚本
    在每片账本中显式写入 1 个 source 行和 5000 个新 proposal，四片
    合计 20004 行、其中恰有 20000 个新提议；所有 status 均为
    `NO_HIT`，所有弦差均负，最大弦差约 `-6.17e-5`。最强点的
    `rho=0.5715404865`；非作者另写实现作 90 位全 4096 事件复算，得到
    `H''=-29.1832216087...`，三个步长实际弦全负，Fraction LDL 证明
    有理化 D 正定且 `|t|<=1/200` 保留 `1/2000` 可行裕量。完整 seed
    再生成未重跑，故账目/冻结点为 `CORRECT`，外层搜索仍为 `SCOUT`。
18. **异质对角核的二阶平坦方向已得到全局与四阶的完整刻画。** 固定
    所有对角元素 `x_i` 后，任意严格 DPP 核满足

    \[
    H(K)\le\sum_i h(x_i)=H(\operatorname{diag}x),
    \]

    且等号仅在 K 对角时成立；所以对角核是整个固定对角可行截面的唯一
    全局熵最大点。局部若 `diag D=0`，则 `H'=H''=H'''=0`，但

    \[
    H^{(4)}(0)=-12\sum_{i<j}
    \frac{D_{ij}^4}{x_i(1-x_i)x_j(1-x_j)}<0
    \]

    对每个非零方向成立。紧对角盒上还有统一四阶负界和统一小步径向下降
    半径。它说明特异结构不是一个孤立噪声点，而是一整条二阶平坦脊；
    平坦只到二阶，真实熵从四阶严格向下。非作者独立 Fraction 复算通过；
    Taylor 展示漏号经审计指出并修正后复审通过。
19. **第三轮联合谱/谱基细化把机制比推到 0.57259，但仍严格为负。**
    修复 source 记账后的脚本从上一冻结点再作四片各 5000 个新提议；
    四份账本均含 source 行，合计 20004 行、恰有 20000 个新提议，正
    status 和正 gap 均为 0，最大存储 gap 约 `-6.09e-5`。最好机制比为
    `rho=0.5725909001`。非作者未导入作者 gate，另写实现逐行核对四份
    账本，并以 90 位全 4096 事件重算 strongest，得到
    `H''=-45.3758897236...`；三个实际弦全负，Fraction LDL 证明有理化
    D 正定且 `|t|<=1/200` 仍有 `1/2000` 严格可行裕量。冻结点与已存
    账目为 `CORRECT`，有限搜索为 `SCOUT`；未重跑完整 seed proposal
    再生成，故该层标记 `INCOMPLETE`，不外推一般结论。
20. **三维补集层已圈出一个连通、非退化的严格负曲率区域。** 对固定
    谱基和同号谱速率，把总负曲率屏障拆成基数熵、两项 Fisher 平方和、
    `2 log(3)` 补集基线与一个残差。成对的二阶 jet 给出残差的精确
    `L1` 常数 6。因此，只要 singleton 与 pair 两个条件层的每个原子都
    落在 `[1/4,4/9]`，所有非零同号谱速率都有严格负熵曲率，且谱裕量
    `epsilon` 上有显式常数
    `2 log(81/64) epsilon(1-epsilon)/9`。这里无需重谱、等对角或交换
    对称。一个三异谱、异质对角、全连接的有理核与 rank-3 non-thinning
    PSD 方向在整个 `|t|<=1/20` 区间满足 12 条精确不等式，因而获得
    连续区间证书。非作者独立重建 8 个事件、全部系数和区间界后判为
    `CORRECT`；一般三维 fixed-Q 全域仍为 `INCOMPLETE`，任意非对易 PSD
    方向不在定理量词内。
21. **第四轮联合谱/谱基细化达到 0.573584，仍未翻号。** 四片各含一个
    source 行和 5000 个新 proposal，合计 20004 个存储行、20000 个新
    提议；所有 status 和 gap 均非正，最大 gap 约 `-4.52e-5`。最强点
    经非作者 90 位全 4096 事件重建得到
    `H''=-42.1913416922...`、`rho=0.5735840442<1`，三个实际弦全负；
    Fraction LDL 再证 D 正定及 `|t|<=1/200` 的 `1/2000` 谱裕量。账本
    与冻结点为 `CORRECT`，有限搜索为 `SCOUT`；未做完整 seed 再生成，
    该层仍为 `INCOMPLETE`，且有理化线只称 near-commuting。
22. **三维补集屏障进一步扩大到乘积条件。** 令 singleton/pair 条件
    原子为 `alpha_a,beta_a`。把互补二阶加速度先精确合并，可写成
    `2 sum v_j v_k C_i`，且
    `C_i>=log(27 min_a alpha_a beta_a)`。因此只要
    `min_a alpha_a beta_a>1/27`，每个非零同号 fixed-Q 谱速率都严格负
    曲率；两层每个原子至少 `1/5` 是一个更易检验、严格大于旧
    `[1/4,4/9]` 盒的子区。一个三异谱、异质、全连接有理核在整段
    `|t|<=1/100` 上有精确六次多项式证书与 `29/150` 谱裕量。非作者
    重建 48 个事件多项式、192 个系数和 12 次区间尝试后判解析结果
    `CORRECT`；六点数据只作 `SCOUT`，条件外全域仍 `INCOMPLETE`。
23. **对角盒附近的 PSD/NSD 排除现在有了显式半径。** 对固定维数与
    `[a,b]^n` 对角盒，令 `s=min(a,1-b)`、`q=s^n`、`m=q/2`，再由逐事件
    三阶导数上界构造显式 `L`，取
    `delta=min(q/(2n),2/(nL))`。凡严格核 K 距某个盒内对角核的
    Frobenius 距离不超过 delta，就对全部非零 PSD/NSD D 有
    `H''_K[D,D]<=-2||D||_F^2/n`。半径极保守但完全可计算；非作者从
    determinant/Möbius、混合导数计数到 trace 不等式逐项复核为
    `CORRECT`。这不是全域定理，也不覆盖不定号方向。
24. **一个非退化三维点的完整 Hessian 已严格负定，并延伸成开邻域。**
    在 M7 的三异谱、异质对角、全连接有理核 `K_*` 上，非作者独立用
    六变量事件多项式与有理 log 区间重建完整 `Sym(3)` Hessian；
    `-Hessian` 的六个 Gershgorin 行裕量全正，最小严格大于 `43/25`。
    因而对任意非零实对称 D——包括非对易 PSD/NSD——有
    `H''[D,D]<=-(43/50)||D||_F^2<0`。矩阵连续性进一步给出 `K_*` 周围
    一整个完整 Hessian 负定的开邻域，不是孤立点。首轮审计还发现可选
    PSD scout 的非对角 Frobenius 梯度少了 `1/2`；修正并全量重跑后，
    同一审计者复核通过，核心证书未漂移，有限优化仍只标 `SCOUT`。
25. **第五轮联合谱/谱基细化达到 0.574047，并暴露出需要单独检查的谱边界效应。**
    四片各含一个 source 行和 5000 个新 proposal，合计 20004 个存储行；
    所有 status/gap 均非正，最大 gap 约 `-4.78e-5`。最强点经非作者
    95 位全事件重建得到 `H''=-39.3902501599...`、
    `rho=0.5740468374<1`，三条弦与有理 LDL 可行性证书均通过。这个点
    的搜索记录恰贴着配置的 `0.01` 谱裕量地板，但核本身仍严格可行；
    因此它不是数值奇点，也不能说明边界极限，下一轮已降低配置地板作
    独立机制诊断。冻结点/账目为 `CORRECT`，有限搜索仍为 `SCOUT`。
26. **S5 的 full-Hessian 开邻域已有显式闭球。** 以非退化有理
    `K_*` 为中心，半径
    `36163/16056110400≈2.25229e-6` 的整个闭 Frobenius 球都自动严格
    可行，八个 exact atoms 至少 `87/2500`，且对全部实对称 D 有
    `H''<=-(43/100)||D||_F^2`。非作者从 signed determinant/Mobius、
    三阶链式法则、混合导数计数和范数传递独立复核为 `CORRECT`。半径
    极保守，但它把“附近存在”变成了可直接核验的数值范围。
27. **full-Hessian 负定开区沿整条对角平坦脊积聚，而非只围绕一个点。**
    对任意严格对角 `X`，只要零对角 A 的每条非对角边均非零，就存在
    `epsilon_0(X,A)>0`，使所有足够小的非零
    `K=X+epsilon A` 都有完整负定 Hessian；每个这样的 K 周围又有普通
    开邻域。非作者独立检查四次式极化、余项阶数、Schur 补并作小维
    Fraction exact-event 验算，状态 `CORRECT`。全边非零只是当前证明的
    充分条件；稀疏图能否靠六阶及更高项获得同样结论正在继续分类。
28. **一个非退化 full-Hessian 负定区已沿有理线连续延伸到
    `[-6/25,6/25]`。** 沿 M8 的全连接、异质对角、三异谱
    `K(t)=K_0+tR`，六坐标 `-Hess H` 在 173 个有理子区间上都由 log
    区间和 Gershgorin 严格对角占优认证；最小行裕量约 `2.66e-4`，
    atom 下界 `63029/5000000`，谱裕量 `1/25`。非作者另写实现重建全部
    叶区间并判为 `CORRECT`。这说明已找到的是有长度的连续结构，不是
    单个特异点；`49/200`、`1/4` 处只是当前证书方法未闭合，浮点上仍
    未见正曲率，不能称反例。
29. **稀疏穿孔并不必打开所有边：图直径至多 2 已足够。** 五次熵项
    恒为零，六次 triangle 项使任意有共同邻居的缺失边获得严格负的
    `epsilon^4` Hessian 主项；配合已有边的 `epsilon^2` 主项和三尺度
    合同，可证明任意 connected、diameter≤2 的实加权支撑图在对角脊旁
    都产生 full-Hessian 负定开区。断开图有精确跨分量零曲率方向。
    非作者独立核对 52 个 exact atoms 后判为 `CORRECT`；三维因此是
    connected iff，直径更大的连通图仍待更高阶分析。
30. **预条件证书把同一条 full-Hessian 负定线段扩到
    `[-0.29,0.29]`。** 67 个有理叶区间无缝覆盖，其中 65 个通过固定
    有理上三角合同后的 Gershgorin，2 个直接通过；最小变换后行裕量约
    `4.10e-4`，整段谱裕量 `1/150`。非作者逐叶复算并核对 P 可逆，状态
    `CORRECT`。正端距首个谱退化点 `0.3` 只剩 `0.01`；`0.299` 的严格
    区间运行超时仍只标 `INCOMPLETE`，不当成反例。
31. **降低谱裕量地板后，最强机制反而回到更靠内部的位置。** 第六批把
    配置地板从 `0.01` 降至 `0.002`，20,000 个新提议仍无正曲率；最好
    `rho=0.5746069387`，但实际谱裕量为 `0.0153714`，比上一 source 更
    大。全账本四个 margin bins 的最好 rho 也不单调。非作者以 120 位
    全事件复算得 `H''=-40.2752548454...`，三弦和 Fraction LDL 全过。
    因此“机制提升纯由贴谱边界造成”在这批数据里没有得到支持；这仍是
    `SCOUT` 诊断，不是排除所有边界机制的定理。
32. **固定谱基机制已从近边界一直剖到中心谱盒。** 在强制谱裕量至少
    `0.02,0.05,0.10,0.20,0.30,0.40` 的六批各 20000 个新提议中均无
    正 gap 或 `rho>=1`。连同 H10，冻结最好值依次为
    `.574607,.574595,.572941,.531389,.333772,.152195,.038617`。每批最强
    点均由异人用全 4096 events、高精度实际弦和有理 LDL 复核；H16 的
    `H''=-41.5919048132...`。这只说明当前机制向中心显著衰减，不证明
    单调性或中心盒全域曲率定号。
33. **M8 的非退化三维线已经覆盖整个最大严格可行区间。** 预条件区间
    证书先把中段扩到 `[-0.29,0.29]`；正端用六个 bridge 叶与解析
    Schur 模型覆盖 `[0.29,0.3)`，负端用 162 个叶及奇异 full-atom
    模型覆盖 `(-1,-0.29]`。三段拼成整条 `t∈(-1,0.3)`，每一点的完整
    `Sym(3)` Hessian 都严格负定。它是有长度且顶到两个谱退化端点的
    连续结构，但仍不是整个三维核域。
34. **一般连通三维问题被严格压成一个标量。** 条件 odds 组成的正定
    `N` 给
    `B=Fisher+det(N)G_N-det(N)eta eta^T`，所以五维超平面已经严格正，
    只剩 `rho=det(N)eta^TA^{-1}eta<1`。稠密秩一边界族满足 `rho→1^-`，
    排除了统一远离 1 的捷径，却没有越过门槛。
35. **标量门已获得信息投影表达，并排除了三条看似自然的捷径。**
    `rho` 等价于 exact-event score 的 ridge 最小能量；Fisher-only、
    trace capacity 与补集五分类代理都有严格 blocker。补回三个正交
    条件 score 后，精确式为 `rho=R_T-c1-c2-c3`；缺失不等式与原问题
    等价，尚未证明。blocker 只否定证明法，真实总曲率仍严格为负。
36. **最接近 1 的三维数值点已经追到具体实现错误和病态来源。** 首次
    Decimal 复算把空事件的一阶 jet 错写为六个 `-1`；正确值是
    `(-1,-1,-1,0,0,0)`。修复后异人重建 445 次高精度尝试，444 个严格
    可行点均无 `rho>1`，最好约 `0.9950999446`；另一个浮点伪近 1 点真值
    约 `0.5`，来源是 `Lambda≈0` 与 N 近奇异。这里的噪声已被定位、修复
    和重跑，而不是按阈值略过。
37. **中心交换对称路径是完整六维定理，并带有环境厚度。** 对
    `K=[[1/2,a,0],[a,1/2,a],[0,a,1/2]]` 的每个 `0<8a²<1`，完整
    `Sym(3)` Hessian 严格负定。任意避开 `a=0` 与谱边界的紧路径片段
    都有统一 x 向开厚化。因此这里找到的特异结构既不是单个点，也不是
    只沿族切向成立，而是一维族及其局部二维/环境开邻域。
38. **一般对称路径已缩为单个 even-block 标量，最危险的是双边界尺度。**
    reflection-odd 块全域严格，一般 `(x,a)` 只剩 `sigma(x,a)>0`；
    `c=2a²/x²` 把半域矩形化。`c→0` 极限至少为 4；`c=1-s` 时唯一
    full atom 为 `x³s`，Fisher 的正 rank-one 极点迫使危险方向切向该
    atom。幂律 `s=x^p` 仍使 `sigma→∞`，真正有限尺度是
    `s=exp(-beta/x)`。异人已重建 exact atoms、分块、Schur 与 full-atom
    pole，并确认稳定公式和有限 profile 自洽；但极限函数及一致余项仍待
    闭合，故全域依旧 `INCOMPLETE`。
39. **交换三角形族的四个方向已被解析消去。** 对 `K=xI+a(J-I)`，S3
    把完整六维 Hessian 分成二维 invariant 块和四维 standard 块；后者
    对所有 strict `a!=0` 自动严格正。全域只剩一个显式二维 log 行列式。
    去心对角的任意紧片都有统一小 `|a|` 严格负定邻域。最差浮点值
    `-4.55e-13` 经两种输入精确化均为约 `+6.98e-13`，12 个有理 log
    区间证书也全正，明确属于大尺度消去误差。
40. **连通 `Lambda=0` 已显出高维连续结构而非孤点。** 经 sign gauge，
    限定核验通过的结构把全部连通核参数化为加权 Cauchy
    `L_ij=w_iw_j/(z_i+z_j)`；外场 tilting 精确保留 `Lambda=0`，并把
    full-Hessian 问题缩为保留条件信息的 `3x3` Schur 门。围绕
    `K(1/2,0.3)` 的显式半径 `49/214688160` 球内候选下界为
    `B>=||D||_F²/6`，且含三参数稠密外场盒。异人用 exact Fraction 与
    90 位全 Hessian 最小复放核验 Kstar、18 个 field identity 和全部
    半径常数；全子流形仍 `INCOMPLETE`。
41. **交换三角形的四条谱边界已从采样升级为统一邻域定理。** 二维
    Fisher 精确写成 complete-data 对角项减一个 missing-information
    rank-one 项。对任意横向紧区间 `[r,1-r]`，`alpha` 或 `beta` 趋近
    0/1 的四条边都存在统一内邻域，其中完整 `Sym(3)` Hessian 严格负定；
    关键正规化 determinant 极限均严格为正。异人核对 factor 2、odds
    对数放置、complement 和统一余项后通过。四角双尺度及分离紧中区仍
    开放，所以这不是整个交换三角形域的定理。
42. **一般对称路径最危险的指数双尺度已有闭式正极限和紧参数楔形。** 令
    `s=exp(-beta/x)`，作者从 Schur 标量的受限能量极小推出
    `phi=(beta+log2+2)(beta+log2)^4/[2beta²(log2)²]`，并证明
    `phi>8(log2+2)`、两端发散、唯一极小值约 `26.60376012`。进一步的
    一致 `O_J(sqrt x)` 余项给每个紧 beta 区间一整片连续指数楔形，而非
    离散射线。异人已核对受限极小、coercivity、forcing 与量词，判定该
    限定结论 `SCOPED_CORRECT`；`beta(x)->0/∞` 与全路径仍开放。

## 已独立核验的连续区间证书

一个固定、非 thinning、远离解耦的十一维秩二半正定族已经得到连续区间
证书：对全部 $|t|\le1/32$，

\[
\Delta(t)\le-\tfrac12t^2.
\]

它使用 2048 个精确有理事件系数、对数的有理上下界和统一四阶余项，而
不是抽查有限网格。非作者已经独立重建全部事件系数、Möbius 语义、LDL
谱证书、对数取整方向和余项链条，状态为 `CORRECT`。它只覆盖这个冻结
族，不外推为一般秩二半正定定理。

## 数值机制诊断

一般完整 Hessian 侦察累计 1822 个 `n=3..13` 稠密中心，另有 20000 个
十二维 fixed-Q 谱速率定向中心，均未见正曲率。为避免
把微负数笼统归为噪声，程序还最大化

\[
\rho=\frac{\text{正的事件加速度}}{\text{Fisher 损失}}.
\]

只有 $\rho>1$ 才能翻成正曲率。随机侦察最好值从约 0.35 随维数提高到
0.486；保存完整核后的定向优化把十一维值推到 0.5014，round 6 的十二维
冻结点达到 0.5261，fixed-Q 谱扫描推到 0.5464，随后联合扰动谱与谱基
依次达到 0.5686、0.57154、0.57259、0.573584、0.574047，降低地板的
边界诊断再到 0.574607。继续强制谱裕量从 0.02 提高到 0.40 后，冻结
最好值依次为 0.574595、0.572941、0.531389、0.333772、0.152195、
0.038617，仍没有越过 1。这些冻结值均已通过高精度全事件重算、实际
可行弦和 PSD 方向证书；中心谱盒点的高精度
`H''=-41.5919048132...` 仍严格为负。0.5464
之后各点的精确
有理化证书须按 near-commuting 对象解读。极值方向与最
接近零的总 Hessian 方向是两种不同现象：前者是
稳定但尚未翻号的事件加速度机制，后者接近均匀核的秩二不定号平坦结构。
两者已被分开，不再混为“舍入噪声”。

## 仍可能的路径

- 利用已核验的路径二阶 jet，把 $H''$ 化成可定号的递推、条件熵或
  log-partition 表达；弱耦合连续子类已经圈定，下一步须控制任意耦合
  下两个符号不定的协方差残差；
- 从已核验的 twin-pair、显式对角盒和三维 full-Hessian 邻域，向一般
  半正定/半负定 K-仿射方向推进；
- 固定特征向量 Q，研究异质正谱速率的低维通道表示；正在进行的定向
  服务器扫描只用于找候选，不代替解析或高精度闸门；三维下一步须直接
  控制新乘积/C_i 充分区之外的 singleton/pair 补集残差，而不是逐
  事件定号；
- 搜索并冻结满足局部系数 $L_2<0$ 的真正不定号秩二结构；
- 对任何正信号继续执行精确可行性、Möbius exact-event 和区间熵三重闸门。

入口按路线见 `route_registry.md`；依赖状态见 `lemma_ledger.md`；有限搜索
永远不作为一般定理。
