# D10-B4 修订稿复审

日期：2026-09-08。修订后最终裁决：**CORRECT**。

范围：固定维数、紧tau盒、充分小非零beta的连续弱耦合排除子类，以及
此前核对的曲率恒等式。一般固定beta路径族的全局凹性仍为INCOMPLETE；
不据此认证新颖性、数值epsilon或原始DPP全域问题。

原 fresh_audit.md 对旧稿的INCORRECT裁决及其反例保留，不回写历史。
本次只读作者修订的theorem.md、decomposition.md、verdict.md、run_log.md，
核对三项修订和承重论证；作者文件未修改。

## 1. 等号条件与严格负曲率：已修好

theorem.md 第78–83行及第105–109行已明确：

`H0''≤−4||delta||²`，等号当且仅当每个delta_i≠0的坐标满足tau_i=1/2；
但delta≠0时仍有 `H0''<0`。

这精确修复旧稿把第一个不等号写成处处严格的问题。delta=0的支撑
为空，条件真空成立，等号0=0也被覆盖。旧反例
tau=(1/2,1/2)、delta=(1,1)现在是合法等号例，H0''=−8<0，不再与
定理冲突。verdict.md中的总结与此一致。

## 2. eta范围与非空内部：已修好

theorem.md 第116行现要求 `0<eta<1/a−1/b`，第218–220行及边界段
明确解释非空性；verdict及run_log同步修订。

独立核对该条件确实给两分支的内部，而不只是一个边界点。令

`r=1/b, s=1/a, w=s−r, gamma=(w−eta)/4>0`。

在倒数坐标z_i=1/tau_i中取

`z1=s−gamma, zn=r+gamma`，其余z_i=(r+s)/2。

所有z_i严格位于(r,s)，且

`z1−zn=w−2gamma=(w+eta)/2>eta`。

因此正分支在tau的(a,b)^n中有开邻域，负分支交换首末坐标同理。
这比仅有相对内点更强。n=2时无中间坐标，构造仍成立。无需借助
12个数值样例，也没有将作者的定性epsilon变成某个数值认证。

## 3. 图连通与参数分量：已修好

定理标题、条款2、第152–159行、结尾以及decomposition/verdict均明确：
“connected”指每个三对角L的路径图连通，而不是完整参数域拓扑连通。
每个边为−beta_i/tau_(i+1)，beta_i≠0且tau>0，故图连通无误。

作者新增“如需参数连通，固定一个beta符号室及一个tau分支”也成立。
倒数坐标将正分支映到

`[r,s]^n ∩ {z1−zn≥eta}`，

这是非空凸集；负分支同理。倒数映射在正盒上为同胚，所以各tau
分支路径连通。固定beta符号室本身为开矩形，取乘积仍路径连通。
不固定这些分量时，符号室和正/负tau分支彼此分离，修订稿未再把
整个集合称为连通。

## 4. 主统一弱耦合谱界：未被修坏

修订保留了以下正确量词链：

1. 固定n,a,b,eta，R_beta^-1对beta为有限多项式，给整个tau盒上一致的
   严格谱裕量；eta只进一步约束异质性和非空范围。
2. 紧集严格位于DPP域，所有有限精确事件概率正，Hessian
   M(beta,tau)=nabla_tau²H连续且一致连续。
3. 第241行仍使用正确的非严格 `M(0,tau)≼−4I`。
4. 第248–255行从紧致性取得同一个epsilon，使整个
   `||beta||∞≤epsilon, tau∈[a,b]^n`上lambda_max M≤−2。
5. 因此对全部非零delta同时有
   `delta^T M delta≤−2||delta||²<0`。谱界已经包含单位方向归一化及
   二次齐次恢复，不需要按方向再选epsilon。

K_affine恒等式、秩等于delta支撑大小以及eta/2异质裕量推导均保留。
小耦合阈值对整个tau盒、所有方向、全部符号图样一致，但仍可依赖n。
没有提出对任意放大的delta都使用同一参数t步长的错误量词。

decomposition中的ell'=A、ell''=B及三个负号没有改变；此前验证的
`H''=−Var(A)+R`和`R=−Σp''logp`仍成立。全局残差符号仍未解决，
修订稿明确保留这个范围边界。

## 5. 本次验证与冻结版本

本次为解析修订复审，没有新数值采样或重跑旧脚本。既有独立24事件
算术检查仍只作为旧/新共有恒等式的交叉核对，不用来证明连续区域。
run_log保留旧失败、旧审查和后续作者复跑，没有将重复样例扩大分母。

本次CORRECT适用于下列SHA256版本：

- theorem.md：`A782B5814CD1BC6825B075C0F8E1FAB679475D4C11FC496CDF9AA4F49176301C`
- decomposition.md：`D835438303B47A6D192A1C34F6CE14789C44A2A0641E3DC75000928B278E2AAD`
- verdict.md：`29846B2744446D4AF1CEDE8D1D6E1B251041D5E3CE1D76DF660F2B6E23E4AF28`
- run_log.md：`DA6E2FFCAF5A7EA48C976A5F4FBA80D0EE28FFDE6F625B74E9E52B7E2371D57D`

使用math-theorem的非作者定向验缝角色。该裁决不等于Lean机械认证，
也不证明一般实DPP熵凹性或本子类的研究新颖性。
