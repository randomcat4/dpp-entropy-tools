# 深化轮次

## Round 0：冻结

- 日期：2026-09-08
- 冻结对象：FT-A 固定块边缘全局排除、FT-B 路径 `L` 的 `K` 仿射三点
  闭包、FT-C 互信息驱动正 gap 必要条件。
- 首轮并发：主实例加三个直属子智能体；子智能体不得派生。
- 首轮规则：每路一个工作单元，只凭证明、反例、精确构造或明确断点续费。
- 状态：已执行。

## Round 1：三条冻结路线

- FT-A：固定块全局不等式及等号条件完成，非作者结论 `CORRECT`。
- FT-B：`n=3,4` exact rational K 中点完成；非作者新增 `n=5` 复算，
  结论 `CORRECT`。
- FT-C：恒等式和秩二机制完成；78 个提议、231 条主弦无正 gap。

## Round 2：推广与独立核验

- FT-B 推广到任意维 fixed-beta `S(tau)` 族，非作者 `n=6` 的 64 atoms
  逐点 exact-event/DP 对照通过，结论 `CORRECT`。
- `n=5..30`、rank1/rank2/full-rank 每类 4160 条弦均无正 gap；这是有限
  scout，不升级为凹性结论。
- FT-C 加入 9 个完整 MI Hessian 与 54 个 PSD/indefinite 配对诊断；总计
  294 次评价，正几何推动最多只达到总散度代价的 0.0598742。

## Round 3：一般稠密 Hessian

- 全 exact-event Hessian 与 Fisher/加速度分解由独立实现核验，状态
  `CORRECT`。
- 五批 `n=3..11` 侦察共 1662 个中心，最大曲率仍负。
- 加入坐标无关的 generalized Rayleigh 比值 rho；随机最好 0.486478，
  十一维保存核的谱定向优化达到 0.501403，仍未达到翻号阈值 1。

## Round 4：近零结构与连续区间

- 均匀核二阶平坦脊被精确分类：零对角方向四阶严格向下，非作者结论
  `CORRECT`。这解释一类机器精度近零，不是孤立点误判。
- 固定十一维非 thinning 秩二 PSD 族得到 `|t|<=1/32` 上
  `Delta<=-t^2/2` 的连续区间证书；非作者独立重建 2048 事件、谱/对数/
  余项证书并判定 `CORRECT`。
- 一般半正定方向符号与路径族 tau-Hessian 仍在推进。

## Round 5：定号方向子类

- 一般 PSD/NSD 方向仍 `OPEN`；Loewner 单调耦合和独立加点都没有自动
  控制事件加速度项。
- 任意维 twin-pair 条件化子类完成整区间负曲率证明，非作者新增 n=4
  有理例并判定 `CORRECT`。
- 更强的“PSD 秩一方向两两混合 Hessian 非正”捷径被 2x2 精确例否定；
  这只排除错误证明法，不构成一般 PSD 主命题反例。

## Round 6：路径二阶 jet 与异常追根

- fixed-beta 路径族完成 `O(n^2)` selected-run 二阶 jet，并与 n=5,6,7
  全 exact-event Möbius Hessian 对照。
- 初始 n=93/rank2 的巨大正号被冻结到完整参数；独立分析定位为旧商公式
  显式形成 `Z^-3` 后下溢。稳定 float、110 位 Decimal 和实际中点弦均给
  `H''≈-0.007024918934`，而不是正值。
- 冻结弦本身严格可行：精确有理 LDL 给整段 `|t|<=1/10` 至少 `1/50`
  的谱裕量，故异常不能用“样本不可行”敷衍掉。
- 修正后重跑 n=5..100、rank2/full-rank 共 4608 个方向，正号为 0；
  非作者完整重放统计并判定递推与异常归因为 `CORRECT`。有限未命中仍只
  标记 `SCOUT`。

## Round 7：稠密机制越过 1/2，但仍未翻号

- 隔离单 CPU 的 n=12,13 长扫描新增 160 个中心，累计稠密 Hessian 分母
  1822；无正曲率，最佳机制比 `rho=0.5261099452`。
- 65/70 位全 4096 事件重算确认 `H''=-20.4712207967`；精确有理 LDL
  证明冻结 D 正定、rank 12，且 `|t|<=1e-4` 真实 K-仿射弦有
  `1/20000` 谱裕量。该信号稳定但不是反例。
- 中心内移到谱裕量约 0.01 后 `rho` 仍为 0.5217604，排除“只由边界
  稀有事件地板制造”的解释。
- 限制到与 K 对易的正定方向仍得 `rho=0.5133580`，于是下一轮改用固定
  特征向量、异质正谱速率；正在运行的定向有限扫描不升级为定理。

## Round 8：PSD 专门搜索与坐标对角锚点

- 独立 PSD 参数化在冻结十二维中心执行 22511 次提议/控制；另检查
  n=2..5 的 940 个中心、14440 个 PSD 方向。原种子重放和实际调用监测
  均无正号；这仍只是有限 scout。
- 原观察坐标下的对角 K、对角 D 得到全维精确排除：DPP 为 Bernoulli
  乘积，`H''=-sum d_i^2/[x_i(1-x_i)]`，非零方向严格负。
- 该定理不能靠正交旋转推广到一般对易 K,D，因为 DPP 配置熵绑定观察
  坐标而不是只由谱决定。一般 PSD/NSD 方向保持 `OPEN`。

## Round 9：路径弱耦合连续区

- fixed-beta 路径族在 beta=0 退化为独立 Bernoulli 乘积，完整 tau-Hessian
  在整个紧盒上一致满足 `<=-4I`；紧致连续性把它推广到一个统一的非零
  小耦合区域，并可同时保证严格可行、三对角图连通和异质性。
- 因而该区域内所有非零 K-仿射方向都有 `H''<=-2||delta||^2<0`。这是
  连续区域定理，不依赖 n<=100 的有限未命中。
- 首稿错误地把 `-4||delta||^2` 写成处处严格；非作者用
  `tau_i=1/2` 的等号例否决。修订后精确记录等号条件、非空 eta 范围及
  图连通与参数分量的区别，第二轮复审判定 `CORRECT`。
- 一般耦合的瓶颈被压缩为两个符号不定的协方差残差是否能超过
  `Var(A)`；全 fixed-beta 路径族仍 `INCOMPLETE`。

## Round 10：固定谱基低维闭合与三维阻断

- 二维固定特征向量 PSD 谱速率方向得到全称严格负曲率定理。证明保留
  四个 exact-event 原子，并将全配置熵分成严格凹的基数项与非正曲率的
  singleton split 项；任意正交基、秩一、重复谱和反射边界均覆盖。
- 三维出现最小方法阻断：有理 Householder 例给
  `p_{1}''=2339/2450>0`，说明“每个层内事件概率都凹”的二维论证无法
  提升。独立有理对数证书同时给该例总 `H''<0`，故它不是反例。
- 非作者以 28 个完整事件多项式和解析不等式验缝，二维定理与三维阻断
  判为 `CORRECT`；`n>=3` 一般固定谱基问题保持 `INCOMPLETE`。
- 同一路线另得到精确 latent-channel 表示：先抽独立谱子集，再经固定
  projection-DPP 通道输出观察集，且通道保持基数。于是未知符号被压缩
  为 `H(Y||Y|)` 的曲率，Poisson-binomial 基数项本身已知凹。
- 该表示、完整二阶导数和 cardinality-uniform/低维直和子类由第二位
  非作者独立核验为 `CORRECT`；generic Q 的条件通道项仍未闭合。

## Round 11：三维条件层联合结构

- 三维 exact atoms 被进一步写成 empty/full、singleton `Pr` 与补集
  pair `Ps` 四层，未决条件熵正是 `G_P(r)+G_P(s)`。
- 180000 个 random 加 22000 个 local PSD 谱速率方向中，singleton 和
  pair 条件层分别出现正曲率 `3.6342`、`3.0505`，但对方层同时强负；
  最好条件和仍为 `-4.75484e-9`，正总曲率计数为 0。
- 非作者以三路 exact atom 表示和四个冻结极值重算核验账目；约化为
  `CORRECT`、有限结果为 `SCOUT`。下一步直接攻击补集两层抵消，不把
  202000 次未命中升级成定理。

## Round 12：十二维 fixed-Q 机制比强化

- 四个一线程分片各扫描 5000 个固定特征向量谱中心，最好正谱速率机制
  比达到 `0.5464281988`，比上一冻结点的 `0.5261099452` 更高，但仍
  未过翻号阈值 1；20000 条浮点状态和实际弦均无正号。
- 最强点经独立 80 位主子式导数再 Möbius 反演，得到
  `H''=-32.4367046423`；三个实际弦负。精确有理 LDL 又证明有理化后的
  D 正定及整个 `|t|<=1/200` 区间严格可行。
- 非作者复核全部 CSV、manifests、日志、seed proposal 重放与四个 NPZ，
  冻结点为 `CORRECT`、全批为 `SCOUT`。有理化后 K,D 精确不交换，故
  不把 near-commuting 可行证书误写成精确 fixed-Q 结构证书。

## Round 13：谱与谱基联合局部细化

- 从已核验的 round-12 冻结点出发，四个单线程分片各评估 5000 个新
  中心；中心之间小幅扰动谱与谱基，每个中心内重新优化正谱速率。
- 20000 个 proposal 中 `FLOAT_CANDIDATE` 与正弦差计数均为 0，最大
  弦差约 `-5.59e-5`；最好机制比提高到 `0.5685905197`，但仍未达到
  翻号阈值 1。
- 独立 90 位复算给 strongest `H''=-34.3715675595...`，三个实际弦
  均负；Fraction LDL 证明有理化 PSD 线在 `|t|<=1/200` 内严格可行。
- 产生版本漏写 source 行的账目缺陷在解释前被发现；source 是已核验
  负点，且 20000 个新提议均逐行在账，所以本批零命中未受影响。当前
  脚本已修复 source/candidate 冻结。非作者裁决为冻结点/账目
  `CORRECT`、有限搜索 `SCOUT`、全局结论 `INCOMPLETE`。

## Round 14：对角盒连续区域与三维锥门纠错

- 对任意异质观察坐标对角核和任意实对称方向，完整 Hessian 精确化为
  `-sum D_ii^2/[x_i(1-x_i)]`；紧对角盒周围因此存在一片对全部非零
  PSD/NSD 方向统一严格负的开邻域。非作者独立 Fraction exact-event
  核验通过，状态 `CORRECT`。
- 三维条件层在固定基点的 PSD 谱速率问题化成 3x3 锥上二次型门。初稿
  被审计发现公式漏号、奇异 KKT 漏项、固定绝对阈值缩放反例和目标强弱
  写反；v3 修订、指定回归和 60384 基点全量重跑后复审通过。
- v3 的 416691 个 support 候选仍无正总曲率，但只标 `SCOUT`。精确
  `Psi''>0` 例已经排除“条件层本身凹”的捷径；一般 n=3 仍未完成。

## Round 15：修复记账后的联合谱/谱基细化

- 四个分片各记录 1 个 source 行与 5000 个新 proposal，合计 20004
  ledger 行、实际新提议 20000；正 status 和正 gap 均为 0，最大 gap
  约 `-6.17e-5`，最好机制比提高到 `0.5715404865`。
- 非作者未导入作者 gate，以 90 位 Möbius exact-event 独立复算 strongest
  `H''=-29.1832216087...`，三个实际弦均负；Fraction LDL 证明有理化
  正定方向及 `|t|<=1/200` 的严格可行裕量。
- 全部存储账目与冻结点为 `CORRECT`；未执行 20000 次 seed proposal
  完整再生成，因此该层为 `INCOMPLETE`，有限搜索总体保持 `SCOUT`。

## Round 16：异质对角平坦脊的全局与四阶闭合

- 固定全部对角边缘后，熵次可加与 DPP pair inclusion 给出对角核是
  整个可行 fiber 的唯一全局熵最大点。
- 对任意零对角实对称方向，exact-event 二阶 jet 显式化；前三阶熵导数
  消失，第四阶为严格负的加权 `sum D_ij^4`。紧对角盒上得到统一负常数
  及统一小步径向下降半径。
- 非作者 n=2,3,4 Fraction 复算判为 `CORRECT`。初稿 Taylor 展示漏两个
  加号，但后续系数未受影响；补号后的哈希已通过短复审。

## Round 17：第三轮联合谱/谱基细化

- 修复 source 记账后的脚本从 round-15 strongest 再作四片各 5000 个
  proposal；每片账本均有 1 个 source 行，因此总计 20004 行、恰有
  20000 个新提议。正 status 与正 gap 均为 0，最大存储 gap 约
  `-6.09e-5`，最好机制比提高到 `0.5725909001`。
- 非作者不导入作者 gate，独立核对四份 CSV、manifests、日志和 NPZ，
  并以 90 位全 4096 事件重算 strongest，得到
  `H''=-45.3758897236...`；三个实际弦均负。Fraction LDL 认证有理化 D
  正定及 `|t|<=1/200` 保留 `1/2000` 可行裕量。
- 已存账目与冻结点判为 `CORRECT`，有限批次仍为 `SCOUT`；未全量再生
  20000 个 seed proposal，故再生成层为 `INCOMPLETE`，一般结论不变。

## Round 18：三维补集残差的连续充分区域

- 从精确层分解中抽出 `2 log(3) sum v_i v_j` 的补集基线；singleton
  与 pair 二阶 jet 成对后的 `L1` 总量至多为 `6 sum v_i v_j`。这把
  “条件层接近均匀”定量成可直接验的严格负曲率充分条件。
- 若两层每个条件原子都在 `[1/4,4/9]`，所有非零 one-sign fixed-Q
  谱速率都有显式严格屏障；该区域包含均匀层的开邻域，但不要求重谱、
  等对角或交换对称。
- 冻结了一个三异谱、异质对角、全连接有理核及 rank-3 non-thinning
  PSD 方向；12 条精确多项式界证明整个 `|t|<=1/20` 区间都在充分区域，
  而非仅由网格外推。最小有理余量 `3133489/37800000`，谱裕量 `19/100`。
- 非作者独立重建 8 个 exact events、通道分解、常数、分类和区间证书，
  结果 `PASS`。子类为 `CORRECT`；充分区域之外的一般 n=3 fixed-Q
  屏障仍 `INCOMPLETE`，任意非对易 PSD 不在量词内。

## Round 19：第四轮联合谱/谱基细化

- 从第三轮 strongest 出发，四个单线程分片各记录 1 个 source 与 5000
  个新 proposal；共 20004 个存储行、恰有 20000 个新提议，所有
  status/gap 均非正。最大 gap 约 `-4.52e-5`，最好机制比提高到
  `0.5735840442`，仍小于翻号阈值 1。
- 非作者不导入作者 gate，独立对账 CSV/manifest/log/JSON/NPZ，并以
  90 位重建全 4096 exact events；strongest 的
  `H''=-42.1913416922...`，三条实际弦全负，Fraction LDL 与
  `|t|<=1/200` 的 `1/2000` 谱裕量通过。
- 冻结点与存储账目为 `CORRECT`，有限批为 `SCOUT`；未重跑完整 seed
  proposals，故再生成层 `INCOMPLETE`。有理化证书仍限 near-commuting。

## Round 20：三维补集屏障的乘积扩区

- 先合并 singleton/pair 的互补加速度，得到三个点态系数 `C_i` 及
  `A=2 sum v_jv_k C_i`。AM--GM 给
  `C_i>=log(27 min_a alpha_a beta_a)`，从而把旧矩形充分区扩成联合乘积
  条件 `min alpha_a beta_a>1/27`。
- 两层每个条件原子至少 `1/5` 是更简单的充分子区；一个三异谱、异质、
  全连接有理核则在该简单盒外仍满足乘积条件，并在 `|t|<=1/100` 整段
  获得精确六次多项式证书，谱裕量 `29/150`。
- 非作者从 rational K+tD 重建 48 个事件多项式/192 系数，核对六中心、
  三系数与 12 个区间尝试；10 通过、2 失败完整保留。解析扩区为
  `CORRECT`，有限分母为 `SCOUT`，条件外一般 fixed-Q 问题 `INCOMPLETE`。

## Round 21：对角盒排除的显式半径

- 对每个 exact atom 的 determinant 一至三阶混合导数作统一列展开界，
  先在对角盒到 K 的线段上保护 atom floor，再界熵 Hessian 的 Lipschitz
  常数 L。
- 显式取 `delta=min(q/(2n),2/(nL))`，其中 `q=min(a,1-b)^n`；该半径内
  对任意非零 PSD/NSD D 有 `H''<=-2||D||_F^2/n`。半径很小但可计算，
  不再只依赖“存在某邻域”的紧致性说法。
- 非作者逐项重推 determinant/Möbius、链式系数、trace 界，并作 n=2,3,4
  exact sanity；状态 `CORRECT`。全域 PSD/NSD 与不定号方向仍未解决。

## Round 22：非退化三维点的完整 Hessian 负定邻域

- 在一个三异谱、异质对角、全连接有理 K* 上，六变量 exact-event
  多项式与有理 log 区间给出 `-Hessian` 的严格 Gershgorin 证书，最小
  行裕量 `>43/25`。因此对全部实对称 D，
  `H''<=-(43/50)||D||_F^2<0`，覆盖非对易 PSD/NSD。
- 由 Hessian 矩阵连续性得到 K* 周围整个 full-Hessian 负定的开邻域；
  这不是单点或仅 fixed-Q 结论，但仍是局部三维区域。
- 首轮审计发现可选 PSD projected scout 的非对角 Frobenius 梯度漏除 2；
  修复后同参数全量重跑，最佳值未变且仍来自 rank-one scan。修订复审
  通过；定理为 `CORRECT`，有限优化仅 `SCOUT`，全域问题仍开放。

## Round 23：第五轮联合谱/谱基细化与谱边界分流

- 从第四轮 strongest 出发，四个单线程分片各记录 1 个 source 与 5000
  个新 proposal；共 20004 个存储行、恰有 20000 个新提议，所有
  status/gap 均非正。最大 gap 约 `-4.78e-5`，最好机制比提高到
  `0.5740468374`，仍远低于翻号阈值 1。
- 非作者未导入作者 gate 或搜索模块，独立对账全部冻结文件，并以 95 位
  重建 4096 个 exact events；strongest 的
  `H''=-39.3902501599...`，三条实际弦全负，Fraction LDL 与
  `|t|<=1/200` 的 `1/2000` 谱裕量通过。
- strongest 的记录谱裕量恰为本轮配置地板 `0.01`，但独立谱检查仍有
  `lambda_min(K)=0.0338924...`、`lambda_max(K)=0.99...`，所以它不是
  奇异核。冻结点/账目为 `CORRECT`，有限批为 `SCOUT`；下一轮降低配置
  地板，专门区分机制改善是否由靠近谱边界驱动。
