# 人机贡献与来源记录

- 问题与 R3 范围：用户于 2026-09-08 指定。
- 冻结陈述与路线整合：R3 主实例（GPT-5.6 Sol，xhigh）。
- 结构构造：直属 GPT-6 Astra high 子实例，独立目录。
- 降维算法：直属 GPT-5.5 xhigh 子实例，独立目录。
- 误差与覆盖审计：直属 GPT-5.5 xhigh 子实例，独立目录。
- 冻结 v1 验证：同一独立审计职责先否决 `19a8271` 的 vector-`a_g`
  精确覆盖缺口；定向修订后对固定提交 `f9a9e23` 输出 `CORRECT`。该结论
  只认证约化工具，不认证实反例或全域命题。
- 公开先行工件：`randomcat4/dpp-entropy-tools` 主分支及 T2/T3 固定提交。
- 计算：远端独立 R3 目录，最多 8 CPU 线程、32 GiB，不用 GPU。
- 未复现部分：SSH 地址、私有对话和私有运行历史不进入公开库。
- 最终正确性责任：没有人工专家认证；分组计数约化已通过独立模型验缝，
  原实反例目标仍为 `INCOMPLETE`。
