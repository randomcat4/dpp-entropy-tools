# 假设与约定

- 底集为 `[n]`，`n<infinity`。
- 核均为实对称矩阵；搜索默认严格满足 `0<K<I`。
- 对数为自然对数，`0 log 0=0`。
- 精确事件概率由包含概率经 Möbius 反演得到：
  `p_K(S)=sum_{T subset S^c} (-1)^|T| det K[S union T]`。
- 对严格正收缩允许使用等价 L-ensemble 公式，但必须给出推导并在小维与
  上述容斥逐事件核对。
- 浮点值只用于侦察；严格符号需要有理或区间证书。
