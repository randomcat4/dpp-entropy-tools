# 轮次与分母

## Round 0：启动审计（2026-09-08）

- 本地/远端 R3 目录在启动前均不存在。
- 远端启动时有两个他人的单核满载进程；未触碰。
- 远端可用内存约 971 GiB；R3 自限 8 CPU/32 GiB，不用 GPU。
- Windows 遗留代理仅在单条命令中清除；未改全局。
- GitHub CLI 登录失效，issue/PR 尚未登记，不能伪报完成。
- 首轮三条直属路线：结构构造、降维算法、误差/覆盖审计。

## Round 1：分组计数工具

- 候选对象：1 个结构族。
- 已开始：公式推导、独立实现、小维逐事件对照。
- `n>=11` 搜索尚未计入；所有失败和退出码将在运行后追加。

## Round 2：冻结候选

- 结构搜索：2960 个 `n=11,12,14,15,24,30` 基点，0 失败、0 候选；
  最大原始正 gap `7.1054e-15` 按舍入量级拒绝。
- 主路线 smoke：120 条 `n=11,12` 弦，0 候选；逐条账本完整。
- 小维验证：结构作者 32 点；主实现 3 项测试；独立有理实现 6 项测试。
- 首次单元测试收集退出码 1（动态模块未登记 `sys.modules`）；修正测试加载器
  后 3/3 通过。失败记录保留在 `artifacts/run_log.md`。
- 首次 `git commit` 退出码 128：独立 checkout 未设置提交者身份；文件未丢失，
  随后只按公开库既有作者信息设置仓库级身份，不修改全局配置。
