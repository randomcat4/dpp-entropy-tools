# 验证状态

## 数学

NC、NC-channel、旧循环平均严格等号/显式gap以及旧径向加强：AUTHOR_PROOF，尚未独立非作者审阅。没有任何本会话重算算作第二位审稿人。本稿不宣称已经委派、启动或取得Codex审核。

作者自检重点：完整配置概率与包含概率区别；复参数下不以p为正分布；闭步重复顶点的奇偶集合；Cauchy半径和模长常数与n无关；KL首项t⁴的消失阶；高阶余项总和1424/27；有限Jensen后才取熵率；非偶Fourier共轭约定；局部通道定理对任意固定A,B的量词。

## 本轮实际轻量计算

执行命令 `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python code/audit_new.py`，退出码0。Python3.13.5、SymPy1.14.0，实际脚本计时0.143秒。完整输入在 `inputs/new.json`，输出在 `output/new_audit.json`；计算全部是有理或Gaussian有理代数，计时浮点数不参与数学判号。

覆盖恰为两个固定四点标量窗口（一个非偶），所有16个配置的包含排除/独立行列式路径、16个Boolean字符矩、偶对称、概率t²/t⁴系数、KL的t⁴/t⁶/t⁸系数、首项下界和参数不等式；一个固定窗口闭步长度1–6的恒等式；一个四点通道障碍、零质量导数张成秩和一个秩一完整配置分解。

没有做n趋于无穷的数值外推；没有构造三符号反例区间；没有大扫描、优化或重型服务器计算。有限自检不证明NC或NC-channel的全称量词。

## 旧代码

旧 `previous/code/verify_round2.py` 与 `previous/inputs/exact_inputs.json` 原样归档，可独立路径运行。旧输出在本地包中保存，标为历史作者自检，不是本轮重新开展的独立审核。旧区间日志使用40项atanh级数、显式有理余项和10^-60网格向外舍入；完整算法在旧代码中。

## Codex审核重点

优先只读审核新稿第3–7节和第9节，特别确认(4.4)复解析界、(7.1)余项和NC-channel的张成量词。旧稿可另行审阅；另一团队已完成的径向凹性不免除本稿径向加强与循环平均gap的审核。审核结论应绑定具体提交，不自动覆盖后续编辑。
