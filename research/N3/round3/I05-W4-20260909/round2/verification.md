# verification.md — 作者自检与独立审阅边界

## 作者侧已执行

运行：

```sh
python code/verify_symbolic.py
```

脚本使用 Python 标准库与 SymPy，完成以下精确检查：

1. 从六个真实 `K` 坐标重建 R2-T1 的八个概率、八个 `p'` 和全部 `p''`。
2. 核对三组 `p''` log 权重和、完整八项 Fisher 矩阵、加速度矩阵及最终块分解。
3. 对 `Gs` 求符号导数，核对三个 Sylvester 主子式恰为 proof.md 式 (15)–(17)。
4. 核对二点条件熵引理中的 `δ` 恒等式与二乘二行列式证书。
5. 核对块分解中心的逐事件一阶乘积切向、二阶行列边际和及 Fisher 精确分解。
6. 核对一般 `K12=0` 路径中心的四个条件概率、四个一阶条件方向和 Jacobian。
7. 核对有理强耦合示例 `κ=1/3` 的事件概率、主子式及旧域外归一化边。

实际输出保存在 `outputs/verify_symbolic.txt`，末行为：

```text
ALL SYMBOLIC CHECKS PASSED
```

## 证明与脚本的关系

主结论是 `proof.md` 中的解析证明。脚本只核对有限代数恒等式与因式分解；它不是对连续参数区间的采样证明，也不是证明助理形式化。

## 发布一致性检查

2026-09-09 对本轮聊天交付包与 PR #41 研究分支逐文件核对。以下实质内容在检查时已与分支版本一致：`CLAIM.md`、`RESULT.md`、`proof.md`、`attempts.md`、`sources.md`、原 `verification.md`、`requirements.txt`、符号脚本、精确输入和规范输出。

本地包中唯一未推送的既有文件是 `outputs/verify_symbolic.local-rerun.txt`；它与 `outputs/verify_symbolic.txt` 逐字节相同，只是同一作者会话的重复运行副本，不提供新的证明、修正或失败证据，故不重复提交。

检查发现原交付协议要求但本地包和 PR 均缺少 `frozen_statement.md` 与 `HANDOFF.md`。现已补入：前者冻结 R2-T1、R2-T2 的完整量词和未证明范围，后者仅列独立审阅任务。除此之外，没有发现只存在于聊天或附件、尚未进入 PR 的最终引理、证明修正或严格失败证书。

本节是作者侧的发布完整性检查，不是数学独立审阅。

## 独立非作者审阅

**尚未进行。** 本轮的推导、脚本编写、运行、发布核对和再次检查均属于同一作者侧会话。Codex 后续若复算，应在单独目录或审阅记录中说明独立重建了哪些步骤，不能把本脚本重跑或本节的一致性核对冒充独立审阅。

优先审阅点：

- proof.md 式 (4)–(9) 的坐标因子与八事件分组；
- `Gs'` 三个主子式；
- 二点条件熵式 (21)–(25)；
- 块分解式 (28)–(31) 对二阶交叉项的边际消去。

当前可审阅状态：**作者证明包完整；R2-T1、R2-T2 待独立非作者审核；新颖性未认证；一般缺边连通族和一般严格实三维命题仍未解决。**