# N3 final verdict

Status: **STOPPED_SUBSTANTIVE / INCOMPLETE**.
The general real three-dimensional full-configuration entropy conjecture
is neither proved nor refuted. No positive entropy chord was certified.
All three child units are closed; no research job or heartbeat is scheduled.

## Established results

1. Exact conditional-score decompositions `F=Q_k+R_k`, `R_k>=0`, specialize
   to the two DPP covariance squares. These are auxiliary Fisher identities.
2. `max Q_k>=2tr(N adj D)` fails at a simple strict rational kernel, and also
   at the **exact true A trace optimizer** of
   `[[51,24,-24],[24,48,-24],[-24,-24,52]]/100`. Every projected gap divided
   by A-energy is below -0.1015, while actual `B/A>0.5475`. No convex
   combination of these three bounds can close the target there. Both
   rational certificates passed independent nonauthor review.
3. Keeping all one/two-point statistics leaves exactly
   `F-F_pair=(Lambda')^2/(sum 1/p_S)`. A reviewed nonexchangeable rank-two
   family has `F_pair(I)->577/18` but cofactor `2log(1/epsilon)+O(1)`.
   The full Fisher term retains a positive `1/(4epsilon)` contribution.
   This refutes another sufficient inequality, not entropy concavity.
4. Sherman-Morrison yields the true optimizer and a general triple-score
   suppression bound. This reviewed algebra supplies no DPP alignment bound.

## Minimum remaining obligation

With `H=F_pair+det(N)G_N`, `v=grad Lambda/sqrt(sum 1/p_S)` and weighted trace c,
put `alpha=c(H^-1c)`, `beta=v(H^-1c)`, `gamma=v(H^-1v)`, `d=det(N)`.
Where `alpha>1/d`, the still-missing DPP-specific bound is

    beta^2/(1+gamma) >= alpha-1/d.

This is **EQUIVALENT_BLOCKER**, not a newly solved scalar theorem. The
strategy of deleting covariance-score residuals is strictly refuted even
at the optimizer. The separate sufficient condition `F_pair(D_A)>=C(D_A)`
remains unproved, with finite non-hits as its only evidence here. A new unit
needs structural control of the residual or alignment terms.

## Stopping reason and evidence

Two distinct projections produced checkable obstructions; the subsequent
optimizer analysis returned to the same unclosed DPP coupling without a
strictly weaker closed lemma. The authorized substantive stopping criterion
therefore applies. Increasing the same scans cannot supply that argument.

The main instance executed 12 and 36 initial centers, 36 reused projection
checks, 512 targeted optimizer evaluations and two exact point certificates.
The falsification child executed 180 original boundary centers, explicitly
labelled diagnostic repeats and high-precision rechecks, and one exact
analytic-family point. These are not added as distinct-center counts.
All mathematical server jobs were single-threaded and exited 0.

First author object: `424b4ef`; [first audit](review/formula_and_score_review_v1.md).
Stationary certificate: `449221b`; boundary proof: `f8a75e0`; optimizer
algebra: `c0964be`. All are checked in the
[second audit](review/second_unit_review_v1.md), review commit `28b5576`.
Later optimizer-note changes only clarify its closure status.

Commands, versions, PIDs, seeds and coverage are in [main ledger](main/run_log.md),
[falsification ledger](falsification/run_log.md),
[inequality ledger](inequality/command_log.md), and [review ledger](review/run_log.md).
Final job status and restart conditions are in `checkpoint.json`.

Both audit units came from one nonauthor context; they are not two reviewers
of a solved open problem. No N3 Lean theorem, full-domain interval proof,
exhaustive novelty search or publication-level contribution is certified.
Elementary projection and inverse-update identities are not claimed as novel.
