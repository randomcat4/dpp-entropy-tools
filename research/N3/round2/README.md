# Round 2: real square constraints and the beta-zero slice

Status STOPPED_SUBSTANTIVE; B0 and the original entropy problem are INCOMPLETE.
Read `verdict.md`, `checkpoint.json` and `verifications/index.md` for the
final scope. The original round's frozen objects are preserved.

Ownership: main owns top-level round2 records and main/; the existing three
isolated child branches own round2/inequality/, round2/falsification/ and
round2/review/. No child delegates. Initial server mathematical jobs are
single-threaded, at most two simultaneously; no GPU.

This round proved a strict connected beta zero exists in a bracket with
0.6615279258817643<det(N)*alpha<0.6615279260967083<1, and derived a positive
beta limit on every fixed dense weak-edge ray with interior means. It also
strictly refuted pure acceleration-sign control on Lambda tangents and
cofactor dominance by the newly strengthened locked-odds projections.
The valid Fisher projection itself remains intact, with a separate exact
zero-residual-norm convention. These are partial results and obstructions,
not a proof or counterexample to B0.

The complete eight-event score and full real Rayleigh polynomials were
retained. No more kernel families, scans or automatic continuation follow
this substantive stop. See `run_log.md` and the child logs for actual
execution counts, failures and fixed certificate sources.
