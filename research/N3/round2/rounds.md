# Work units and retained failures

The coordinator authorized a genuinely smaller beta-zero target after the
round-one substantive stop. B0's premises have not changed during round two.

1. Square/optimizer unit: retained the entire Rayleigh polynomial and its
   affine jets, but derived only the exact H-stationary reformulation of
   B0. This did not close the Fisher-versus-cofactor bound.
2. Reachability unit: fixed rational segments produced opposite beta signs;
   a rigorous interval argument then proved a true zero with det(N)*alpha
   uniformly below one. The proposed universal beta-positive shortcut is
   false. This is partial progress, not a proof for all zero components.
3. Weak-edge unit: a full-score Taylor expansion on fixed dense rays gave
   a positive beta limit. It cannot apply when fixed ratios or means
   degenerate and cannot exclude the certified interior zero.
4. Lambda-tangent sign test: a rational tangent has positive cofactor
   acceleration while its true negative entropy Hessian is positive.
   Thus a pure acceleration-sign proof cannot suffice.
5. Locked-odds unit: a new Fisher Gram projection uses the exact equality
   of two conditional odds derivatives. Its proposed cofactor dominance
   is stronger than B0 and remained unproved. A final fixed-input check
   was authorized because this adds a real nonnegative term, rather than
   repeating the old residual-deleted Qk condition.
6. Final locked-bound test: exactly four existing rational kernels and
   eight deterministic starts, followed by one strict rational-log
   certificate, refuted the new max-Qlock dominance on an exact Lambda
   tangent. The true full Fisher curvature remains strictly positive.
   Thus arbitrary convex weights on these same three bounds cannot repair
   the sufficient condition. No further scan follows this failure.

Non-author review is tied to the frozen author objects in `provenance.md`.
It preserves all failed versions and treats any correction as a new file.
The final verdict records the locked-bound test and the precise stop
reason; no new broad numerical scan or automatic restart is authorized.
