\in[-0.453573880156865070680274635197,
     -0.453573880156865070680274635196],
\]

所以题给曲率式的最后一项严格为正；完整曲率仍为

\[
H''(t)
\in[-73.237129052913423983347727404231,
     -73.237129052913423983347727404230].
\]

第二，以概率 `lambda` 独立刷新一整个相关块，得到的是

\[
(1-\lambda)P_s+\lambda P_0,
\]

而缩放交叉核的 DPP 是 `P_{(1-lambda)s}`；两者精确相差

\[
\lambda(1-\lambda)s^2Q.
\]

这解释了为什么整块刷新在交叉秩一时看似有效，却在真正 rank two 的二阶外幂处失效。

## 计算与复核

`code/verify.py` 使用 Fraction 和 SymPy 精确检查：

- 二维 Hessian 矩阵铅笔的行列式恒等式；
- `2+2` 和 `3+2` 有理例的全部事件、两个完整边际和条件 DPP 分解；
- `P_s=P_0+sR+s^2Q` 及 `R,Q` 的零边际；
- rank-two 外幂似然公式和矩消去；
- 整块刷新差；
- 采用 220 项 `atanh` 级数及纯有理余项界的严格对数区间。

实际运行末行为 `ALL EXACT AND INTERVAL CHECKS PASSED`。没有用浮点正负作为定理证据。

## 未解决范围

仍未解决：

- `m,l>2` 且 `B` 的行、列支撑都超过两个坐标的一般 rank-two 径向族；
- 一般实对称有限核沿任意 `K`-仿射弦的凹性；
- 固定标量平稳熵率；
- 一般复 Hermitian 版本；
- 新颖性与发表优先权。

尚未独立审阅。本会话中的符号实现与第二种精确事件实现只算交叉复算。
