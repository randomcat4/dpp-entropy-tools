# PR80 source and evidence scope

The original three mathematical notes give author analytic claims and finite diagnostics. Independent acceptance must distinguish analytic identities/sufficient conditions from fixed arithmetic and from the still-open general whole-chord theorem. The later two mathematical notes below add an exact obstruction and explicit complete-interval families; they are new author deltas, not covered by an earlier source freeze.

Read `RESULT.md` for the signed-pair identity and ratio-cone criterion, `ADDENDUM_S09_SIGNED_FIBERS.md` for the reserve identity and square completion, and `ADDENDUM_FIBER_WINDOW_MOMENT.md` for the fiber-window lower bound. General curvature conclusions from `t^2 I''` are stated for `s=t^2>0` on the corresponding connected physical t-bands. The diagonal strictness witness and the separate off-diagonal reserve test are compatible.

## Original author output packaging — retained disclosure

The original `output/verify_s09_signed_fibers.txt` and `output/verify_s09_fiber_window_bound.txt` files are edited author summaries, not literal raw stdout from the committed scripts. In particular, importing `verify_s09_signed_fibers.py` emits its top-level output before the window script prints its own section, while the saved window summary omits that prelude. The saved signed summary also edits the minimum-q line and adds explanatory text. These summaries and their PASS labels are not independent numerical certificates; those original files and code are preserved without a new run of those original modules.

In the original window script, the value named `lo` is a conservative lower bound for the theorem's right side. The value named `hi` uses the same upper delta penalty and is only a companion conservative value; it is not a proved upper enclosure of the theorem's right side. Any saved "lower/upper" label for those window columns is withdrawn as an interval claim. Only the conservative lower endpoint is relevant to the proposed positivity certificate. Independent reconstruction must produce its own valid lower bound or both directed endpoints, with raw rational evidence.

The intended prior fixture is pinned to [PR58's immutable joint-additive source](https://github.com/randomcat4/dpp-entropy-tools/blob/89aa874c24dd5a3ea98f8474826392560b1d0397/research/I05-23-middle-20260909/ADDENDUM_JOINT_ADDITIVE.md). The literal matrices embedded in `code/verify_s09_signed_fibers.py` define the original author finite object for this packet. Their equality to the cited fixture, every pair count, all signs and window-moment bounds still require the independent source/arithmetic binding. A previous positive global-curvature certificate does not verify these new per-pair or per-fiber claims.

## Later continuation: actual bad fibers and completed compact intervals

[ADDENDUM_CROSS_FIBER_OBSTRUCTION.md](ADDENDUM_CROSS_FIBER_OBSTRUCTION.md) proves an actual strict real rank-two family with a negative complete conditional curvature fiber, including a family with both marginal blocks correlated. For its explicit `A=I/2, C=I/2+rX, B=sqrt(r/2)[[1,1],[1,-1]]`, `0<r<=1/8` family it also proves `H''(t)<=-(700/19)r^2t^2` on every t in [-1,1]. Thus cross-fiber compensation is genuinely necessary in general; positivity of each fiber is not a universal DPP mechanism.

[ADDENDUM_CORRELATED_NEIGHBORHOOD.md](ADDENDUM_CORRELATED_NEIGHBORHOOD.md) proves a coefficient-space radius 1/40000 around the r=1/8 member on which `H''(t)<=-t^2/2` for all |t|<=1 while a negative complete fiber persists at s=1/2. It supplies an explicit rational matrix with `A12=1/100000, C12=1/8` and dense rank-two B, proves its membership, and extends the phenomenon to nonempty relative-open classes in arbitrary fixed finite block dimensions. The higher-dimensional radius is existential; the 2+2 coefficient radius is quantitative. These compact intervals are not asserted to be the maximal legal interval.

The later checker `code/verify_fiber_obstruction_family.py` reimplements all 64 events of the old s=9/10 object and uses rational atanh remainders plus outward dyadic rounding. It forms the window expression as `4Ev^2/qmax + lambda(qmin)(Ez-E|z|) + lambda(qmax)(Ez+E|z|)`, so its displayed endpoints really enclose that expression; it does not reuse the old invalid upper-endpoint convention. The input's 30 rational entries and mask convention were compared on the author side to the immutable PR58 source above, blob `09ab0a14f2a096e732730561c194fd730a14a8ef`. This does not replace independent source binding.

The new `output/verify_fiber_obstruction_family.txt` and `output/verify_coefficient_neighborhood.txt` files are literal saved stdout from the new author executions. They are still author evidence, not independent C2 certificates. The first development execution's SymPy structural-equality assertion and its repair are disclosed in the cross-fiber addendum. The final continuum proofs are analytic, not grid scans.

The later cross-fiber note also corrects the earlier overcorrection of Theorem 4.1: the finite global strictness conclusion is valid through a positive diagonal pair, even though positivity of one off-diagonal first line alone does not prove strict Cauchy slack. Under the cone hypothesis at s>0, equality for a real path is equivalent to B=0. At t=0 analyticity gives H''(0)=0. Existing C1 text repairs in RESULT.md are retained, not reverted.

The unresolved ratio-test wording in the historical RESULT Sections 7–8 is superseded by the actual author 75/66 negative-pair checks and these later notes. The ratio cone is not applicable to the original s=.9 fixture; all sixteen original fixture fibers remain positive by the later author directed-enclosure calculation. The complete interval of that original 3+3 fixture is not newly certified.

No new C2 contract is supplied here. No author module is an independent computational implementation. No whole-chord job, PR70/77 budget expansion, formal check or novelty claim follows from this continuation. New review work is REQUESTED only until an actual issue claim; existing freezes are not silently advanced. General dense correlated rank-two whole-chord curvature and novelty remain unresolved by this packet.
