# S2 独立有限证书：PR95 / issue 96

状态：**MACHINE_PASS（477/477）**。

本包只认证冻结作者头 `54d9803b29f73669b9028e3519d17493e1b81be3` 的有限/source 部分。它不是数学 FIRST 或 SECOND，不认证整个移动端点定理、熵率、新颖性，也不授权合并。

## 独立性与来源绑定

- PR95 输入中的 30 个有理矩阵条目逐项绑定到 PR58 不可变来源 `89aa874c24dd5a3ea98f8474826392560b1d0397` 的 `research/I05-23-middle-20260909/ADDENDUM_JOINT_ADDITIVE.md`，30/30 一致。
- 正式运行前没有读取、导入或执行 PR95 的作者 checker，也没有读取作者系数表或有理证书。
- 独立实现从全部主子式作 Möbius 求和，重建 64 个完整事件；另以移位六阶行列式恒等式逐事件交叉检查，再以两个 2×2 Schur 系数逐事件核对 `a,b`。
- 每个事件的奇次项和秩二所禁止的六次项严格为零；完整边际、全局及两侧条件系数消去均以有理数精确检查。

正式运行结束后才读取作者的两个输出作差异检查：以 `(S,T)` 为键的 64 行 `mu,a,b` 为 0 个不一致；三段的 `q_min,q_max,V_min,Z_min,N` 与 `Gamma` 共 18 个精确有理字段为 0 个不一致；稀有事件的 `mu,a,b,155/1196` 也全部一致。这个事后比对不参与正式运行的判定。

## 三段连续区间

极值只取二次式的端点与落在区间内的顶点，不使用参数网格。下表是独立精确分数的向外十进制显示；`N` 是逐事件负部最大值的加权和。

| 区间 / 事件 | `q_min` | `q_max` | `V_min` | `Z_min` | `N` | 采用的下界 |
|---|---:|---:|---:|---:|---:|---:|
| `[0,9/10]` / 64 | 0.034262558347522464… | 2.838260765883187930… | 0.550051427542580344… | 0.550051427542580344… | 0.115244632565240287… | `8287/15000 > 1/2` |
| `[9/10,99/100]` / 64 | 0.002747503342768219… | 3.072571446626742226… | 0.837946913012560788… | 0.982702550533286983… | 0.134113304674964766… | `4177/7750 > 1/2` |
| `[99/100,1]` / 其余 63 | 0.024683091733607919… | 3.099172572816601070… | 0.897267385836438459… | 1.098321651709216927… | 0.110590226982404807… | `110979/77500 > 7/5` |

每一行还在参考极值上分别扣除/加入 PR95 声称的 `10^-6` 扰动罚项：`q` 为 2，`V` 为 280，`Z,N` 为 421；所有表门槛仍严格通过。

六个对数比较均由 40 项有理 atanh 级数及几何余项定向包络决定：`log 3>1`、`log 30<87/25`、`log(31/10)>21/20`、`log 400<6`、`log 50<4`、`log 300<6`。没有用浮点对数决定符号。

## 稀有全事件与简单端点

对完整事件 `(7,7)`，独立系数为

- `a=4181050254735181419359/2416512579905515000000`；
- `b=4847471688450288382004871/6638770823916250000000000`；
- `min_[.99,1](a-2sb)=81511358192921434657431739/302064072488189375000000000 > 1/4+3·10^-6`；
- `q(.99)=16598440977829642401181401061/6041281449763787500000000000000 < 1/300-2·10^-6`；
- `q(1)=-14495058440362077306739/604128144976378750000000000 < -2·10^-6`。

`|z|<3-37·10^-6`、`b>10^-6` 也严格通过。公开的 `s_*` 与 `t_*` 两个十进制括区分别在两端给出相反的精确有理符号，且上述 `v` 下界使根在末段简单且唯一。端点 Fisher 余项的有限常数精确为

`1/4 - 36/299 = 155/1196 > 0`。

这只认证 PR95 所用的稀有事件有限不等式与根括区；从这些数值到“实际最大合法端点”及移动端点稳定类的逻辑仍属于数学审稿范围。

## 谱、秩与非零坐标数据

- `A-3I/10`、`7I/10-A`、`C-3I/10`、`7I/10-C` 的全部领先主子式为正。
- `||U||_F^2<81/100`，`||V||_F^2<361/100`。
- `U,V` 顶部二行列式分别为 `17191/250000`、`655143/1000000`，严格大于 `4·10^-12+2·10^-24`。
- `B=UV^T` 有非零 2×2 子式，故秩为二；九个条目的最小绝对值为 `7337/250000`，严格大于 `9·10^-12`。

## 运行记录

- Python 3.12.3，标准库；单进程、1 CPU 线程、2 GiB 地址空间、600 秒绝对墙钟。
- 唯一运行：2026-09-10 05:46:22–05:46:24 UTC；实际墙钟 2.24 秒，峰值 RSS 15404 KiB，退出码 0，标准错误为空。
- 算术 PID 176618 在封装器结束时和收集后再次确认不存在；没有修复、重跑、预算重置或扩张。

原始证据见 `outputs/run01/`；精确结论以分数为准，十进制仅供阅读。
