# Issue 96 / PR95 独立有限证书合同

- 作者冻结头：`54d9803b29f73669b9028e3519d17493e1b81be3`
- PR58 原始矩阵来源：`89aa874c24dd5a3ea98f8474826392560b1d0397`
- 角色：S2 独立机器/source；不承担数学 FIRST/SECOND，不评估新颖性，不合 main。
- 输入：PR95 `fixture.json` 与 PR58 `ADDENDUM_JOINT_ADDITIVE.md`；不读取、不导入、不执行作者 checker。
- 门槛：30 条目绑定；64 完整事件经 Möbius、移位行列式和 Schur 系数交叉一致；三段精确连续极值及 `10^-6` 扰动预算；六个有理对数比较；稀有全事件简单端点与 Fisher 尾常数；参考块谱界、秩和非零条目的 `10^-12` 盒持久性数据。
- 外层授权为 1 CPU、4 GiB、2400 秒；按 S1 最新有限输入请求收紧实际运行至单进程、1 CPU 线程、2 GiB 地址空间、600 秒绝对墙钟，仍只运行一次。
- 停止：首个精确不一致、输入缺失、程序失败或绝对截止到达即停止；不修输入后重跑。
- 输出：完整 64 行系数、逐项检查、精确有理证书、运行账本与原始 stdout/stderr。
