# Superseded local contract — alpha=1/10 joint Fisher/acceleration certificate

Status: **SUPERSEDED / NOT STARTED / NO COMPUTATION RESULT.**

This historical 2-hour, 8-GiB draft was replaced before execution by

`LOCAL_CONTRACT_JOINT_KERNEL_ALPHA01.md`.

Only that later file is the operative local handoff contract. No process was started under this draft, no budget was consumed, no checkpoint queue or output exists, and no result may be inferred from this file.

The replacement contract retains the same complete-event Shannon target and true physical `K`-affine path, but specifies the final one-process, one-CPU, 16-GiB, six-hour envelope and checkpoint/recovery rules. A future local run remains author-machine evidence until independently reproduced and reviewed.
