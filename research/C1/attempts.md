# Attempts, failures and remaining target

The universal B0 statement is unchanged. This is the full route ledger, not just successful experiments.

1. **Precheck and calibration.** Rebuilt all eight events, jets, complete Fisher and covariance projection. Verified exactly three fixed kernels, including the old root midpoint and a connected one-zero-edge example. The old scalar 0.661527925989... is calibration only.

2. **Fixed full-support rank-one geometry.** The tempting mechanism was d alpha tending to one near a rank-one face. A uniform asymptotic analysis with comparable soft rates instead gives beta<0. This excludes a whole collar and the jointly small (epsilon,t) corner of the complement-signed affine family. Its relevance is zero-set exclusion; it does not solve the zero set globally. The boundary d alpha limit for equal soft rates was already present in the source.

3. **Exact exponential-tilt stationarity.** Original information-geometry sources suggested using item-quality tilts. These give three exact mixed complete-Fisher identities and the reduced stationarity equations (T), derived in main/reconstruction.md. They preserve all scores and never substitute a curved L path for K-affine curvature. They do not determine the remaining sign. Treating those equations as an entropy-concavity theorem would be an equivalent restatement without a new bound.

4. **Joint sparse mechanism.** Let one rank-one coordinate weight be kappa times epsilon. Then two conditional log-odds stay finite while the third grows logarithmically. This invalidates the uniform full-support collar. The geometry unit develops a distinct limiting optimizer system; its final proof and review status are recorded separately.

5. **Failed sparse cross coefficient, preserved.** The first limiting two-variable system omitted the common empty/singleton events in the radial/off-diagonal mixed Fisher entry. The missing contribution is +2 sqrt(kappa)/(1-lambda). It is leading order for that cross entry, even though those events give lower-order contributions to some self-energies. Geometry's direct coefficient check and main's independent eight-event expansion found the same omission. main/sparse_limit_algebra_failed_v1.py and its JSON retain the algebra of the wrong system; that script is explicitly not a model of the true Hessian. main/sparse_limit_algebra.py includes the term and symbolically verifies the corrected coefficient. Algebraic agreement alone does not prove the asymptotic reduction.

6. **Excluded old routes.** No three-conditional-projection or locked-odds dominance claim was revived. No beta tolerance was treated as an exact zero. No random sample count was automatically enlarged.

The global remaining obligation is complete Fisher/cofactor control at the actual M-stationary trace direction on every exact beta-zero kernel. The scalar inequality d alpha<=1 is equivalent to that obligation, not a new lemma. Boundary subfamilies and exact zero existence can constrain the route but do not complete that inequality. Final stop and precise handoff are recorded in RESULT.md and HANDOFF.md.
