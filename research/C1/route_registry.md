# C1 route registry

| Route | Structure | First test | Current status / blocker |
|---|---|---|---|
| R0 | Eight-atom determinant jets, weighted-score decomposition | Three fixed kernels including one zero edge | Exact reconstruction plus finite checks; inherited mechanism |
| R1 | Joint near-rank-one degeneration, Schur inverse asymptotics | Derive optimizer scale before a finite parameter table | Full-support exclusion and sparse exact-zero sharpness accepted within scope |
| R2 | Implicit beta-zero affine family with jointly sparse weights | Reproduce old root, then bounded deterministic brackets | Exact rational finite zero certified with d alpha<1; no violation |
| R3 | Exact exponential-tilt mixed stationarity | Reconstruct tilt score and all three mixed Fisher identities | Exact equations (T); missing sign remains EQUIVALENT_BLOCKER |

Three initial direct children, isolated first units, no descendants. No conditional-projection/locked-odds dominance route and no automatic random-scan extension. Main handles R0/R3 while children handle R1/R2 and independent audit.

## ROUTE-stop

共同瓶颈: outside the frozen restricted families, complete Fisher at the true
six-dimensional beta-zero optimizer has no proved global cofactor bound.
R1 asymptotic control and R3 exact exponential-tilt stationarity are
substantively different mechanisms; neither supplies that universal sign.

最快证伪测试: the next unit must freeze a specific joint lambda-to-one sparse
scaling or a new region before computation; it cannot reuse the fixed-lambda
remainder outside its range or merely increase old sample counts.

工作单元: completed formula, full-support collar, sparse limiting optimizer,
one finite exact-zero certificate, and independent reviews. No further unit
is scheduled; HANDOFF.md lists the only two proposed continuations.
