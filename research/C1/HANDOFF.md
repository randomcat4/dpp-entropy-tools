# Handoff — at most two concrete tasks

B0 is still open. Do not repeat the discarded conditional/locked-odds bounds or enlarge old scans. This run schedules no continuation.

1. **Resolve a new nonuniform endpoint of the exact sparse-zero family.** In geometry/sparse_proof.md, keep the true six-coordinate optimizer and investigate lambda approaching 1 together with epsilon and kappa. The leading zero has kappa_*=(1-lambda)/lambda [exp((2lambda-1)/(lambda(1-lambda)))-1], so the current fixed-parameter remainder is not uniform and kappa_* epsilon may cease to be small. Freeze one joint scaling, derive its full Fisher/cofactor balance, and prove subcriticality or certify an exact violating zero. Reusing the fixed-lambda expansion outside its scope is not progress.

2. **Close the actual stationary Fisher inequality on a genuinely new region.** With h=M^-1 eta and beta=0, combine the exact equations (T) in main/reconstruction.md with the remaining three stationarity equations to prove F(D_M,D_M)>=2 tr(N adj D_M), or find a certified failure. State the region and the information retained. The scalar d alpha<=1 is equivalent, so simply rewriting it or imposing an arbitrary Lambda tangent does not complete this task.

The new sparse partial result, accepted by both recorded nonauthor reviews, rules out any strategy requiring a fixed positive safety margin on the full beta-zero set. Full-support comparable-rate collars are beta-negative; they do not contain the exact zeros used for sharpness.
