# A2: moving real frames beyond the fixed projection hierarchy

Status: RUNNING; no counterexample or new theorem is yet certified.

For finite real symmetric positive contractions K, use the full exact-event
DPP law p_K(S)=sum_{T superset S}(-1)^(|T|-|S|) det K_T and natural-log
Shannon entropy H(K)=-sum_S p_K(S)log p_K(S), with 0 log 0=0.
The only counterexample sign is
Delta=(H(K_-)+H(K_+))/2-H((K_-+K_+)/2)>0.

This route extends the explicit scope boundary of PR #11, whose fixed-data
two-term hierarchy is taken as a scoped reference, not a global theorem.
The first milestone is a checkable moving-frame candidate outside that exact
hierarchy, or a precise new path-reduction/obstruction theorem.

The physical observation coordinates stay fixed. General orthogonal
conjugation does not preserve this entropy. Every parameter value must use
two feasible endpoints and their actual arithmetic midpoint. A nonlinear
curve derivative is not an affine-chord Hessian.

Scope excludes stationary entropy rates, path-L/grouped high-dimensional
routes, and another run of the historical 892 fixed-gate trials.
