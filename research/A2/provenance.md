# Provenance

Question and scope were user-directed. A2 normalizes definitions and owns
frozen statements. Three direct roles are separately assigned construction,
degenerate asymptotic analysis, and independent audit; none may delegate.
Construction and asymptotic roles use GPT-6 Astra high; the audit role uses
GPT-5.5 xhigh. The main route is configured GPT-6 Astra xhigh.

At initialization: zero selected candidate families, zero search trials,
zero certified signs. Maximum concurrent contexts is four including the main.
Compute is bounded by eight CPU threads and 32 GiB in aggregate, no GPU;
initial symbolic jobs will use one thread each. Public artifacts contain no
connection details, credentials or private conversation.

Historical sources are read-only public PR #11, frozen v1/v2, boundary theorem
maps v2/v3, component-pair proof and both Round-3 review reports. The historical
892 trials are not repeated and are not included in A2's denominators.

Correctness and novelty are separate. No novelty certification is claimed.

## Actual first-unit contributions

- Frame role: explicit family, nonrepresentation proof, 16-event algebra,
  and compact-parameter uniform entropy proof. The main route selected the
  rational rotation convention and supplied an equivalent anticommuting
  matrix formulation after the author had independently derived it.
- Asymptotic role: zero-safe and matrix error bounds, uniform mixed-family
  estimate, explicit shrinking-direction crossover and failure ledger.
- Main route: frozen statements, independent exact arithmetic implementation,
  seven finite chord enclosures, symbolic cross-checks, and partial Lean
  matrix-algebra check. The main route does not certify its own proof work.
- Audit role: no proof creation or revision; final immutable-packet review.

Denominator at candidate freeze: two geometric versions, one selected
four-dimensional sign family, four auxiliary analytic objects, seven distinct
rational chords, 48 symbolic event functions, zero positive counterexamples.
Three direct child contexts were used, with no descendants. The two proof
roles each completed one analytic unit; the audit role first prepared a
non-authoring checklist and then reviewed the frozen candidates. Monetary
cost is unavailable. No novelty search beyond the stated primary and upstream
checks is represented as exhaustive.
