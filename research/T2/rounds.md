# Work units and coverage

R0: Protocol and public issue inspected. Three isolated subagents actually started. Status RUNNING. Scope selected: finite-block entropy-gap transfer only.

Research computation: none completed at scaffold stage. Runtime probe: Python 3.12.3; pre-existing scientific runtime provides NumPy 2.1.2. Default Python lacks NumPy (environment probe failure, not a theorem failure). No packages installed. No Lean executable found on PATH and repository has no Lean project; no mechanized-proof claim.

Transport failures: direct Git access unavailable in this environment. Public protocol was read through authenticated local API, assembled as a public-only snapshot and transferred to isolated research checkouts. No credentials transferred. Public candidate commits will be parented to the actual upstream commit via the local authenticated Git API. This snapshot history is not represented as upstream history.

Planned mathematical units: construction, boundary family, definitions audit; each started, zero certified so far. Only a checkable candidate warrants a fresh final audit. Prior numerical non-hits: none; large scan: not started.

R1: complete frozen candidate produced. Main exact helper run: 13 checks, two listed chain evaluations, 0 failed, no random draws; 0.196 seconds measured for that combined run. Execution metadata in artifacts/demo_results.json. The exact enclosure's mathematical interpretation still depends on the pending theorem audit. Boundary role completed ten exact-law cases and five numerical diagnostics. Construction used no numerical computation. Candidate freeze and fresh verification are next.

R2: fresh final audit completed on public `31143e0925f95e1c05b2165a71c0d541a20be3ef` (independent report under verifications/). Candidate source files unchanged. Definitions audit integrated as a historical prerequisite artifact, not final certification. First bounded tool milestone VERIFIED; no scan enlargement authorized by this result.

Coverage states: planned/started/completed = one construction proof, two boundary families, one definitions audit, one exact helper and one fresh candidate audit; main-theorem revision failures = 0; explicitly disproved shortcut templates = 2 (B1/B2); certified mathematical objects = the frozen theorem and the two specifically quantified boundary negations, per independent report. Main exact run details remain in demo_results.json. Boundary diagnostics remain non-interval. Verification reruns and coverage are recorded in the independent report.

Structure validator: PASS in research mode (one proof, one CORRECT report, zero errors). One lexical warning: the frozen file says may not rather than the validator keywords must not; its explicit premise-freeze instruction is present. Frozen files were kept unchanged.
