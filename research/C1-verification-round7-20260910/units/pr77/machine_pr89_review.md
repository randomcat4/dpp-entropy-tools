# PR89 machine finite-evidence FIRST review — PR77 fixed harmonic packet

Verdict: `ACCEPTED_SCOPED` for the PR89 finite-evidence gate. The PR89 raw evidence closes the earlier `PENDING_C2` obligations for the fixed midpoint Jensen certificate, the three fixed point-curvature certificates, and the quantitative finite Fisher covariance polynomial, when combined with the already reviewed original analytic bridge and the 2564 narrowing that made Section 9 non-load-bearing. This is not a machine-PASS rubber stamp: the conclusion rests on the frozen input binding, independent implementation structure, execution ledger, A-D compact evidence, raw manifests, and 2772 ordered check rows.

The closure is exactly scoped. It covers only the fixed object `t=1/2,1,3/2`, depth `r=18`, and complete-event lengths `n=18,19` specified in `inputs/REQUEST.md` (`source-snapshots/pr89_machine/inputs/REQUEST.md:11-17`). It does not prove the whole-interval curvature theorem, any continuum sign between those points, any deeper-depth extrapolation, or the old Section 9 true-rate derivative bridge.

## 1. Binding and frozen input

Status: `ACCEPTED_SCOPED`.

The source binding fixes the author head, preparation head, machine evidence head, and public prefix (`source-snapshots/pr89_machine/SOURCE_BINDING.json:2-5`). It also records 199 public files with README/STATUS excluded from the raw evidence set, leaving 197 raw review files, and says the implementation/inputs did not change after preparation (`source-snapshots/pr89_machine/SOURCE_BINDING.json:1387-1394`). The fixed input is the original PR77 object: author head `6ebe38dc6503120d47e9d644cfac78cfb43666f5`, parameters `1/2,1,3/2`, depth 18, lengths 18 and 19, Toeplitz kernel entries, precision 100, `1e-90` logarithm widening, and `1e-40` finite-width target (`source-snapshots/pr89_machine/inputs/fixed.json:2-9`).

The input file also carries the exact constants, point thresholds, Fisher covariance polynomial literals, and small-check limits that the C2 program must reconstruct before comparing (`source-snapshots/pr89_machine/inputs/fixed.json:10-33`). The echoed run input matches the same object and keeps the comparison note that author literals are only targets after independent construction (`source-snapshots/pr89_machine/outputs/run01/input_echo.json:1-149`). This closes the prior concern that the finite evidence might certify a live or later PR object.

## 2. Execution discipline and raw evidence integrity

Status: `ACCEPTED_SCOPED`.

The request requires one single recorded run, no continuum work, no author checker import, no pilot/rerun/precision adaptation, and publication of raw implementation, inputs, outputs, hashes, and execution metadata (`source-snapshots/pr89_machine/inputs/REQUEST.md:3-7`, `source-snapshots/pr89_machine/inputs/REQUEST.md:60-66`). The run plan repeats those constraints and says the public preparation freeze precedes arithmetic, while execution goes through the run guard with one CPU thread, 16 GiB cap, no GPU, and a single 2700-second deadline (`source-snapshots/pr89_machine/execution/RUN_PLAN.md:1-9`).

The run guard starts or reuses one deadline, exports one-thread numerical environment variables, sets the memory cap, records start/deadline/CPU/PID files, and writes exit status after the guarded process returns (`source-snapshots/pr89_machine/execution/run_guard.sh:21-35`, `source-snapshots/pr89_machine/execution/run_guard.sh:39-54`). The ledger records PR70 PID absence before launch, run start/deadline/finish times, process ids, 110.486 seconds elapsed, one CPU, 16 GiB cap, no GPU, CPython 3.12.3, decimal/libmpdec versions, `MACHINE_PASS`, exit 0, 2772 ordered checks, zero repairs, zero prior arithmetic attempts, and closed budget with no reset/extension/rerun (`source-snapshots/pr89_machine/execution/RUN_LEDGER.json:8-35`). The final pid absence file records the arithmetic pid as not present after completion (`source-snapshots/pr89_machine/outputs/run01/pid_absence.json:1-1`), and stderr is empty.

The output hash manifest contains 179 generated entries, including a private invocation file that is not in the public source binding. The ledger explains that 178 public manifest files were copied byte-for-byte by SHA-256 comparison and the private invocation file was omitted from public outputs (`source-snapshots/pr89_machine/execution/RUN_LEDGER.json:34-35`). This repairs the earlier public-certification problem: the original PR77 author packet had abbreviated summaries and a point-run record omission, but PR89 supplies an independently bound replacement evidence package.

## 3. A: exact constants, tails, and Fisher covariance algebra

Status: `CLOSED_BY_PR89_EVIDENCE` / `ACCEPTED_SCOPED`.

The implementation constructs Phase A from rational arithmetic, not decimal approximation: it computes symbol legality, uniform residuals, `Cstar<C0`, derivative constants, point-specific disk residuals and tails, and the covariance polynomials using both Laplace and permutation determinants (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:165-270`). The algorithm README says the covariance polynomials are algebra evidence for the full-Fisher projection only, not a sign claim about response terms (`source-snapshots/pr89_machine/implementation/README.md:5-6`).

The compact outputs retain the exact rational data. Uniform comparison gives the residuals, dominance, `C`, factors, `Cstar`, `C0`, and a positive strict difference (`source-snapshots/pr89_machine/outputs/run01/A/uniform_comparison.json:2-19`). The derivative/tail output records `M2,M3,M4,U1,U2`, Bregman constants, `E18`, uniform curvature tail, and geometric formulas (`source-snapshots/pr89_machine/outputs/run01/A/derivative_and_uniform_tail.json:2-35`). Each point output records its radius data, residuals, factors, `Cstar`, `A0_A1_A2`, exact `Tail18`, and threshold (`source-snapshots/pr89_machine/outputs/run01/A/point_1_2.json:2-33`; `source-snapshots/pr89_machine/outputs/run01/A/point_1.json:2-33`; `source-snapshots/pr89_machine/outputs/run01/A/point_3_2.json:2-33`).

For Fisher, the inclusion determinant file gives the four required site-set determinant polynomials (`source-snapshots/pr89_machine/outputs/run01/A/inclusion_polynomials.json:2-50`). The covariance output gives `m,c0,c1,c2,c3,V`, the `t`-polynomial normalization, endpoint variances, monotonicity numerator, `m'_t`, and the lower bound `16/286141` (`source-snapshots/pr89_machine/outputs/run01/A/covariances.json:2-63`). The ledger rows pass the load-bearing gates: symbol legality at row 2, uniform strict bound at row 9, point-tail positivity at rows 26/36/46, `covariance_literal_V` at row 57, and `fisher_bound_literal` at row 60 (`source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2`, `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:9`, `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:26`, `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:36`, `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:46`, `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:57`, `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:60`).

This closes the finite algebra part previously marked `PENDING_C2` for the Fisher covariance polynomial and rational lower bound. It does not turn the Fisher projection into a curvature-sign proof; the original FIRST restriction that Fisher/acceleration/response terms must stay separate remains in force.

## 4. B: independent complete-event implementation checks

Status: `CLOSED_BY_PR89_EVIDENCE` / `ACCEPTED_SCOPED`.

The request demanded independent complete-event construction, direct determinant checks through `n=8`, Mobius inclusion checks through `n=6`, and independent first/second jet checks through `n=6` at all three t-values (`source-snapshots/pr89_machine/inputs/REQUEST.md:29-34`). The implementation meets this in static structure. It has an exact pivoted Bareiss determinant with division-remainder checks (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:273-297`), a direct signed event matrix route (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:300-315`), row-replacement derivative determinants for physical `t` (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:317-330`), and a width-two frontier recurrence with value/first/second jets (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:333-370`).

The Phase B driver compares recurrence values to direct determinants for every word through `n=8`, compares Mobius inversion and prefix harvesting through `n=6`, compares row-derivative jets through `n=6`, writes per-parameter hashes, and records coverage (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:605-652`). The coverage output records 378 Mobius word cases, 1530 direct word cases, and 378 jet word cases, with the jet parameter explicitly stated as physical `t` and first off-diagonal derivative 2 (`source-snapshots/pr89_machine/outputs/run01/B/coverage.json:2-10`). All 24 small per-`t,n` output files record `all_agreements: true`; the row `small_coverage_counts` passes at CHECKS row 2725 (`source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2725`).

This is enough for the C1 finite gate. It does not prove the production recurrence by a formal proof assistant, but it satisfies the requested independent finite cross-check structure and avoids using the author checker as an input.

## 5. C: production events, normalization, rare events, and aggregation schema

Status: `CLOSED_BY_PR89_EVIDENCE` / `ACCEPTED_SCOPED`.

The request required exact enumeration for all six `(t,n)` pairs, positivity of every emitted event, event count `2^n`, `sum N=32^n`, `sum N'=sum N''=0`, and a histogram sufficient for value, Fisher, and acceleration reconstruction (`source-snapshots/pr89_machine/inputs/REQUEST.md:35-44`). The implementation records both depth 18 and 19 from one depth-19 traversal, rejects nonpositive `N`, aggregates by `N` into count/jet sums/squared first-jet sums, checks exact totals and aggregation totals, and writes deterministic gzip chunks plus manifests (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:697-760`).

Each total file records the event count, normalization, positive minimum `N`, distinct `N`, deterministic word-jet SHA-256, enumeration order, and depth-18 harvest explanation. The six totals are present for `t=1/2,1,3/2` and `n=18,19` (`source-snapshots/pr89_machine/outputs/run01/C/t1_n18_totals.json:2-12`; `source-snapshots/pr89_machine/outputs/run01/C/t1_n19_totals.json:2-12`; `source-snapshots/pr89_machine/outputs/run01/C/t2_n18_totals.json:2-12`; `source-snapshots/pr89_machine/outputs/run01/C/t2_n19_totals.json:2-12`; `source-snapshots/pr89_machine/outputs/run01/C/t3_n18_totals.json:2-12`; `source-snapshots/pr89_machine/outputs/run01/C/t3_n19_totals.json:2-12`). The histogram manifests declare the schema `[N,count,sum_Nprime,sum_Nsecond,sum_Nprime_squared]`, gzip level 1 with mtime 0, ASCII JSONL, ascending integer `N`, chunk row counts, byte sizes, and SHA-256 hashes (`source-snapshots/pr89_machine/outputs/run01/C/t1_n18_histogram_manifest.json:2-49`; `source-snapshots/pr89_machine/outputs/run01/C/t1_n19_histogram_manifest.json:2-79`). The same manifest pattern is present for the remaining four production pairs.

The CHECKS ledger rows 2726-2749 pass all six production-count, production-normalization, production-minimum, and aggregation-normalization gates (`source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2726` through `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2749`). A static parse of the complete JSONL ledger found 2772 rows and no failed row; the ledger phase counts are entry 1, A 59, B 2665, C 24, and D 23. This closes the earlier missing raw event/histogram evidence for the fixed finite certificates.

## 6. D: directed logarithms, finite intervals, Jensen, and point curvatures

Status: `CLOSED_BY_PR89_EVIDENCE` / `ACCEPTED_SCOPED`.

The request required natural logs, precision-100 `Decimal.ln`, explicit nearest-rounding plus `1e-90` widening, directed floor/ceiling arithmetic, separate Fisher and acceleration sums, `h18=H19-H18`, curvature `h18''=H19''-H18''`, finite width at most `1e-40`, midpoint tail subtraction only at the midpoint, and point-specific curvature tails (`source-snapshots/pr89_machine/inputs/REQUEST.md:45-58`). The implementation follows that structure: `log_integer` treats `N=1` exactly and widens the correctly rounded nearest log by `1e-90` (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:416-422`); finite accumulation keeps weighted entropy, Fisher numerator, and acceleration numerator separately (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:425-460`); Phase D derives `h` and `hsecond` as 19-minus-18 differences, subtracts `E18` only from the midpoint Jensen lower bound, and adds each point's own tail to the finite curvature interval (`source-snapshots/pr89_machine/implementation/independent_pr77_checker.py:463-529`).

The decimal contract records precision 100, `1e-90` widening, `1e-40` width limit, CPython decimal/libmpdec versions, exact int conversion, directed rational conversion, `copy_negate`, and the log-error derivation (`source-snapshots/pr89_machine/outputs/run01/D/decimal_contract.json:2-15`). The official Python Decimal documentation states that `Decimal.ln` returns the natural logarithm rounded with `ROUND_HALF_EVEN`; I checked that primary source separately. CHECKS row 2750 records the PR89 range argument that `32^19<10^29`, log bound 87, half-ulp upper `5e-99`, widening `1e-90`, and exact `N=1` handling (`source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2750`).

The six finite files retain the interval outputs for `H` and `Hsecond`, along with separate Fisher and acceleration probability-scale intervals and the `log32` normalization note (`source-snapshots/pr89_machine/outputs/run01/D/t1_n18_finite.json:1-47`; `source-snapshots/pr89_machine/outputs/run01/D/t1_n19_finite.json:1-47`; `source-snapshots/pr89_machine/outputs/run01/D/t2_n18_finite.json:1-47`; `source-snapshots/pr89_machine/outputs/run01/D/t2_n19_finite.json:1-47`; `source-snapshots/pr89_machine/outputs/run01/D/t3_n18_finite.json:1-47`; `source-snapshots/pr89_machine/outputs/run01/D/t3_n19_finite.json:1-47`). The three conditional files retain the `h18` and `h18second` intervals for the exact points (`source-snapshots/pr89_machine/outputs/run01/D/t1_conditional.json:1-14`; `source-snapshots/pr89_machine/outputs/run01/D/t2_conditional.json:1-14`; `source-snapshots/pr89_machine/outputs/run01/D/t3_conditional.json:1-14`). CHECKS rows 2751-2768 pass all finite and conditional width gates (`source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2751` through `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2768`).

For the midpoint true-rate certificate, PR89 records the midpoint lower interval, endpoint upper intervals, exact and outward `E18`, true gap lower bound, threshold `1/10000`, strict positive margin, and the note that only the lower gap is used (`source-snapshots/pr89_machine/outputs/run01/D/true_rate_Jensen.json:1-29`). CHECKS row 2769 passes the strict Jensen margin (`source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2769`). This closes the original fixed midpoint finite evidence gap.

For the three point curvatures, PR89 records each finite `h18second` interval, exact/outward point tail, true-curvature interval, threshold, and strict margin (`source-snapshots/pr89_machine/outputs/run01/D/true_curvature_t1.json:1-22`; `source-snapshots/pr89_machine/outputs/run01/D/true_curvature_t2.json:1-22`; `source-snapshots/pr89_machine/outputs/run01/D/true_curvature_t3.json:1-22`). CHECKS rows 2770-2772 pass all three strict curvature-margin gates (`source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2770` through `source-snapshots/pr89_machine/outputs/run01/CHECKS.jsonl:2772`). This closes the original fixed point-curvature finite evidence gap.

## 7. Claim-by-claim closure table

| Prior item | PR89 finite-gate status | Reason |
|---|---:|---|
| Fixed midpoint Jensen finite evidence | `CLOSED_BY_PR89_EVIDENCE`; `ACCEPTED_SCOPED` | Raw PR89 C/D evidence supplies event totals, histogram manifests, directed intervals, exact `E18`, and a positive strict Jensen margin. |
| Three fixed point-curvature finite certificates | `CLOSED_BY_PR89_EVIDENCE`; `ACCEPTED_SCOPED` | Raw PR89 A/C/D evidence supplies point-specific tails, finite curvature intervals, true-curvature intervals, thresholds, and positive strict margins. |
| Original author PASS summaries lacking raw intervals/hashes | `SUPERSEDED_FOR_CERTIFICATION` | The old packet remains abbreviated if viewed alone, but PR89 supplies independent raw evidence, manifests, source binding, and run ledger. |
| Original point-curvature run-record omission | `SUPERSEDED_FOR_CERTIFICATION` | PR89 does not edit the old author run record, but its independent ledger records one run covering A-D and all generated outputs. |
| Quantitative Fisher covariance polynomial and `16/286141` bound | `CLOSED_BY_PR89_EVIDENCE`; `ACCEPTED_SCOPED` | Phase A reconstructs determinants by two polynomial determinant routes and records the assembled variance polynomial and bound. This remains only a Fisher lower-bound certificate. |
| Section 9 beam-splitter true-rate derivative bridge | `UNCHANGED`; `NO_LONGER_LOAD_BEARING` | PR89 is finite evidence for the fixed certificate route. It does not prove the old missing uniform true-rate derivative bridge, and 2564 already narrowed that statement. |
| Whole-interval curvature theorem | `STILL_INCOMPLETE` | PR89 covers only three fixed points and one midpoint chord. It supplies no continuum interval subdivision or proof between points. |
| Formal verification | `NOT_PERFORMED` | No proof assistant or formal checker was used by this FIRST review. |
| Novelty / priority | `NOT_ASSESSED` | This report is source/evidence finite closure only. |

## 8. Final finite gate conclusion

PR89 satisfies the original finite C2 contract for the fixed PR77 numerical claims at the evidence level. As a FIRST reviewer, I therefore upgrade the previously `PENDING_C2` fixed midpoint Jensen and three point-curvature finite/numerical gates to `ACCEPTED_SCOPED`, with the same analytic-scope limits already stated in the original PR77 review and the 2564 narrowing report.

No further C2 finite-evidence task is required for these exact fixed PR77 claims from this FIRST gate. Mathematical publication still needs the coordinated isolated SECOND/C3 process on the same artifacts. The machine `MACHINE_PASS` label remains evidence metadata, not an automatic mathematical verdict.
