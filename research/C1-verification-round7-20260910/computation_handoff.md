# Bounded independent finite-evidence handoff

No computation is launched by this document. C3 and C2 are converging the
minimal independently frozen contracts. The FIRST reports identify the
finite claims below; C2 must record its actual narrower execution scope
before running them. C1 performs source/evidence review only.

## PR70: one fixed auxiliary object

Source head: `f7be60759fd4d65184803b6585965dc7e5ccd624`.
Use only the literal rational K and D in `post_checkpoint.md:14-19`, the
eight-event formulas in `proof.md:7-21`, the declared mask order, and
`tau=1/100000`. The complete obligation is in
[the FIRST report](units/pr70/review_report.md#c2-finite-evidence-obligations).

Independently reconstruct all eight probability/first/second jets and
both sides' marginal jets. Retain exact quotient second derivatives and
the exact negative paired-resolvent value. Verify strict legality of K
and the two endpoints, including complements; convexity then covers the
whole short chord. Publish rigorous outward enclosures of both side
curvatures, conditional/full entropy curvatures, and the full entropy
Jensen expression, with the complete log argument/bound inventory.

Acceptance is separate for each exact or interval assertion. The negative
paired-resolvent value refutes only that auxiliary universal convexity
claim. A negative full-entropy Jensen expression is in the concave
direction. Neither is a positive entropy-concavity counterexample.

This contract does not include the 273 exploratory inputs, filament
search, a general determinant sign, or any issue73 job. Unavailable
private author scripts and old PR60 certificates are not evidence.

## PR77: the existing fixed midpoint and three fixed points

Source head: `6ebe38dc6503120d47e9d644cfac78cfb43666f5`, exactly 18 files.
The literal path is
`f_t=1/2+(1/4)cos(4 pi theta)+(t/8)cos(2 pi theta)`.
The necessary finite gates are detailed in
[the FIRST report](units/pr77/review_report.md#finite-computation-gate-and-c2-handoff).

1. Independently verify the rational comparison residuals, propagation
   and derivative constants actually used by the chosen certificates.
2. For `t=1/2,1,3/2`, independently reconstruct complete-event values
   for lengths 18 and 19. Retain exact event counts, positivity and mass
   normalization, auditable aggregated determinant/weight evidence,
   outward entropy/conditional-entropy intervals, and the exact tail.
   The true midpoint lower bound must exceed `1/10000` strictly.
3. At those same three points and lengths, independently reconstruct
   value/first/second event jets. Retain the exact zero derivative-sum
   identities, outward Fisher and acceleration contributions, finite
   conditional curvature, point-specific exact tails, and final true
   curvature intervals. The upper bounds must be strictly below
   `-1/2500`, `-1/1000`, and `-1/500`, respectively.

The two finite certificates may share a C2-owned event implementation
once its source and small independent determinant/Mobius checks are
frozen. Reusing the author's generator as the only check is insufficient.
Every used logarithm interval needs an outward justification, precision,
rounding policy, source/output hashes and a run record. The author
summaries lack these raw margins and omit the point-curvature execution
from the consolidated run record; C2 evidence must be independently
traceable. Merely publishing another author PASS does not close the gate.

The adjacent-pair Fisher constants are a separate, small optional unit
if C3 elects to accept the advertised quantitative bound. They are not
required to establish the fixed midpoint and point-curvature claims.
No continuous interval, mesh certification, response-norm search, issue74
job or later PR77 head is included.

## Failure and publication rules

Raw exact identities, source bindings, outward intervals, accepted
threshold margins, environment and exit records must remain reviewable.
A first discrepancy must be retained. An interval that does not exclude
zero or does not reach the advertised threshold leaves the claim
unresolved; it alone is not a mathematical refutation. A refutation
requires rigorous contrary evidence for the particular assertion.

Any resulting C2 evidence is a new frozen review unit. It does not create
a new C1 arithmetic run, an inherited machine PASS, a SECOND verdict, or
permission to integrate a theorem. C3 owns SECOND and sole integration.
