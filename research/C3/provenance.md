# Provenance and ownership

Task I05-C3-20260909 was explicitly authorized as a new claimed C3 run.
The main instance uses the GPT-6 Astra role; exactly three initial direct
children were requested and started: mechanism (Astra high), rate (GPT-5.5
xhigh), and review (GPT-5.5 xhigh). None spawned descendants. After initial
children finished, one fresh GPT-5.5 xhigh context replaced a released slot
for the second theorem review. At most three direct children were active.

- Main: froze definitions, derived the gauge identity, authored the entire
  product-replacement/radial proof, ran the small server preflight, compared
  primary sources, integrated files and published the draft result.
- Mechanism child: independently derived phase shear/cycle flux and a
  finite-window weak-coupling obstruction; chose one fixed candidate and
  computed all its n=8 events. It owns its own analytic and numeric work.
- Rate child: audited S1 applicability, derived the long-tail residual
  distinction, adapted and executed one n=4 true-rate certificate, and
  performed its own arithmetic audit. That audit is explicitly author work.
- First review child: independently reconstructed exact event and gauge
  identities, then nonauthored radial theorem audit; subsequently a separate
  nonauthor check of the fixed numerical rate certificate.
- Fresh replacement review child: only frozen v2 statement, anonymous proof
  and hazards; no theorem authorship, no previous-review conclusions.

The main did not modify the frozen accepted radial proof after review.
One rate-report convention paragraph was editorially corrected: negating
the sine arrays when switching Fourier conventions reproduces the same
kernel. Numeric artifacts are preserved byte for byte.

Source local commit 3827c21662bf6171ea1fbf0d84dcd546989883fa and public PR22
head 60645b4ea9e3f3a79d842b6fe039a33f9daaf7df were verified to have tree
5ed568031bb26ef6bbb99382fa5907582f8f1c40. Source was read-only. The final
publication uses the public main base and contains only research/C3 output,
so it does not duplicate the source PR's unmerged research history.

All substantive numerical runs used the authorized exclusive server route,
one CPU thread each, no GPU. Complete inputs, outputs, versions and process
IDs are retained. SSH access information, credentials and private chats are
not included. No shared claims registry or unrelated instance files were
modified, no other process was stopped, and no automatic retry was created.
