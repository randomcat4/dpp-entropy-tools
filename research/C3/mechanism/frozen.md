# Frozen mechanism, before numeric execution

Status: INCOMPLETE. One independent bounded research unit.

For a fixed real scalar trigonometric symbol with c0=1/2, c1=3/25,
c2=(3/25)(99+20i)/101, c_-k=conj(c_k), define
f_t(theta)=1/2+2 Re(sum_{k=1}^2 c_k(1+i t a_k) exp(-2 pi i k theta)),
where (a1,a2)=(1,-1), t in [-1/4,1/4]. All three fixed symbols have
margin at least tau=1/2-3 sqrt(17)/25>0, since sqrt(17)<25/6.

Test only the complete directional jet at window n=8 and this fixed chord.
This is a pair-score-null but cycle-active direction, not a gauge tangent:
the triangle flux is 2 a1-a2=3. The centre is non-real, so its full event
Fisher information is not assumed zero.

The affine jet equals the fixed-modulus phase jet plus the differential in
the radial direction E (all off-diagonal coefficients unchanged). Thus
H_affine'' = H_phase'' + dH(E). Radial entropy derivative is nonpositive
by coordinate resampling, proved in mechanism.md. Positive phase curvature
alone cannot establish positive affine curvature.

No finite result is a true entropy-rate certificate. The rate target is
INCOMPLETE unless separate rate intervals pass the required gate.
