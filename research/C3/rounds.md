# Version and work-unit record

1. Read source verdict/checkpoint/provenance and relevant proof/review.
   Confirmed source local/public tree equality without repeating past scans.
2. CLAIM.md froze the exact score-null gauge identity and a low-Fisher
   cycle mechanism. Three direct children started with disjoint ownership.
3. Server preflight computed 8 exact rational gauge event identities and
   6 radial channel/curvature diagnostics. No positive finite sign was claimed.
4. frozen_statement_v1.md added the fixed-mean radial theorem candidate.
5. frozen_statement_v2.md removed fixed-mean/zero-diagonal restrictions after
   the product-reference cancellation was proved for arbitrary input marginals.
   v1 remains intact. proof.md was frozen before independent theorem audits.
   frozen_statement.md now reproduces the final v2 statement for the bundle.
6. A mechanism child tested one fixed nonadditive two-harmonic phase shear,
   with positive phase gain and larger negative radial cost. It then stopped.
7. The first nonauthor theorem audit passed. A new GPT-5.5 xhigh context was
   started only after initial child completion, to independently audit v2.
8. The rate child applies one three-symbol certificate to the fixed mechanism
   candidate. The radial theorem does not depend on that certificate.

No math-ultra, descendant agents, autonomous wakeup, new tool-building route,
or continuation of the old full-Hessian phase scan was used.
