# Failure, method, and evidence ledger

1. The pointwise-resolvent route remains disproved by the exact accepted `Phi_r''<0` witness. Nothing in this packet asserts positivity before the logarithmic integral is taken.

2. The new result is not the equal-strength theorem and is not a small-edge continuity theorem. It covers every pair in the two-dimensional normalized square `1/4<=a,b<=4`, including all unequal pairs there.

3. A direct positive-kernel factorization using the positive double integrals for `H` and `J` was attempted but not obtained. The determinant is nonlinear in those integrals, so inserting their integrands separately would not be an identity. No unproved pointwise rational-kernel sign is used.

4. The 32-by-32 grid is not a finite sign sample. Each grid cell is a closed continuum box. Exact logarithm intervals, outward arithmetic, rational congruence, and strict diagonal dominance certify every parameter point in the box.

5. The floating midpoint Cholesky is candidate generation only. Each proposed preconditioner is converted to an exact dyadic upper-triangular matrix, proved nonsingular, and used only inside a subsequent exact interval inequality. Floating error cannot create a false PASS.

6. The exact certified conclusion is `M(a,b)>0`, hence integrated `G1''>0`, on `[1/4,4]^2`. No claim is made for the entire positive quadrant or for an optimal box.

7. The complete Shannon corollary additionally requires the same normalized bounds relative to `qbar`, so that complementation certifies `G0''`. A one-sided result is not silently renamed a complete-entropy result.

8. The physical path is `K+tD`, all six symmetric directions are retained, and the formulas include the four selected-event Fisher terms and every acceleration. The vacant events and leaf marginal re-enter through the exact full-law identity.

9. The final fixed-input execution was short and opened no long-computation contract. No old computation, issue budget, or predecessor output was reused. Development test boxes are not evidence; the retained 1024-box output is the author certificate.

10. Independent mathematical/code review, novelty, and formal verification are pending/not assessed. Unequal leaf diagonals beyond the previously reviewed local statement, all-strength half-leaf concavity, general missing-edge concavity, and general real three-point concavity remain open.
