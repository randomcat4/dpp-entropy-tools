# S1 FIRST for the PR102 general multiple-boundary analytic unit

This packet records an independent, version-bound mathematical/source FIRST for PR102 author head

`16a25c3810977d67207a95f935990df0a807bff9`.

## Verdict

`ACCEPTED_SCOPED` for:

1. the general finite true-affine DPP endpoint theorem in Sections 1--2 of `RESULT.md`;
2. the rank-two vanishing-order refinement in Section 3 of `RESULT.md`;
3. the general rank-two two-scale moving-endpoint stability theorem in `ADDENDUM_TWO_SCALE_STABILITY.md`.

The proof correctly shows that every finite affine DPP path entering a genuine legal boundary from a strict interior satisfies

\[
H''(t)\longrightarrow-\infty.
\]

Repeated zero eigenvalues, repeated one eigenvalues, and simultaneous `K`/`I-K` activity are included.  In rank two, a dangerous double atom has only bounded Fisher information and an adverse logarithmic acceleration term, but the true cardinality law forces a simple-order complete-event group whose Fisher pole dominates it.  Thus the pure-double mechanism is unrealizable for such a DPP path; it is not an entropy counterexample.

The two-scale theorem combines full/empty-event Fisher information with the adjacent true-cardinality group to obtain a uniform `delta^(-1/2)` endpoint lower bound after a repeated rank-two endpoint splits.  This is sufficient for moving-maximal-chord stability around a seed with an independently established positive compact-interior margin.

The explicit 3+3 fixtures, their whole-chord inequalities, all finite arithmetic and code/output are excluded from this FIRST.
