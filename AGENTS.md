# 工作约定

- 遵守用户最新决定：先造工具；不启用 math-ultra；不自动启动平稳或实反例的大规模搜索。历史预算评估不构成执行预算。
- 每路确认实际工作目录，只写本路线目录和分支。你不是唯一工作实例：不撤销他人修改、不共享可写 checkout、不停止他人进程。
- `C:\canglan\` 不属于默认工作区，除非用户点名具体子路径和范围，否则禁止搜索、遍历、修改；委派时必须传递此边界。
- 服务器连接信息、凭据、私有对话、私库文件不提交到公开库。使用用户提供的私下启动信息连接服务器，在自己的隔离目录工作。
- 常规选择、修复、验证安排由路线主实例自行处理；跨路依赖通过 issue/PR。只在关键新结果、旧结论被推翻、共享资源冲突无法解决或需要用户决定研究范围时升级。
- 每路默认主实例加至多一个按需工作/验证上下文，子实例不继续派生；三路默认总计不超过六个活跃研究上下文。没有产物时不扩容。
- 首批重计算每路最多 8 个 CPU 线程、32 GiB 内存，不用 GPU；这是上限，不是配额保证。先检查实际占用并限制 BLAS/OpenMP 线程，资源紧张时主动缩减。任何扩容先用 issue 协调，不影响既有作业。
- 不安装系统级依赖、不改 SSH 或全局环境，依赖用各自虚拟环境。保存作业 PID、命令、版本、退出状态，恢复前先查已有进程，防止重复跑。
- 路线主实例拥有冻结命题；证明者不得改前提。保留失败版本与缺口。有限数值未命中不等于普遍性定理，解析递推不等于严格数值证书。
- 一个工作单元无可检查进展则重审；同一瓶颈连续两个单元无进展则暂停该子路线并在本路 issue 记账，不自动打扰总协调者。
- 研究通过 Draft PR 提交。完整候选固定 commit，新上下文只读验证。开放主结论首审通过后再由第二名独立验证；数学、计算覆盖和新颖性分开裁决。不要凭自检合入认证主张。普通文档与明确标记的未完成产物可正常审查整合，投稿、联系作者、署名不在本次授权范围。
